# --- APC IGNORES ---

# plugin folder
plugins/AngelGrain
plugins/gainknob
plugins/CloudWash/Org_Code

# _tools folder
_tools/DebugView/

# Design Libraries (examples)
design_library/_exclude/

# Build Artifacts (The Dirty Zone)
build/
cmake-build-*/
.vs/
out/
*.obj
*.tlog
*.pdb
*.idb
*.exe
*.vst3
*.lib
*.exp

# github
github/

# User Specific
PRESS_RELEASE.md
.vscode/
*.user
*.suo
nul

# plans
plans/

# System
.DS_Store
Thumbs.db

# Logs & Crash Reports
logs/
APC_CRASH_REPORT.txt
build_log.txt
*_log.txt
cloudwash_crash_log.txt

# Backups
_backups/
dist/
# Local worktrees
.worktrees/

# clangd / IDE tooling
.cache/
compile_commands.json

# Visual brainstorm sessions (Claude Code superpowers)
.superpowers/

# CMake build output (live build dir on this machine)
build-release/
build-base/       # fb527 -- cert lanes build a HEAD reference here; never commit it (175 MB slipped in once)

# fb346 — agent verification scratch (Bode Hilbert checks etc). Keep on disk, out of git.
scratchpad/
