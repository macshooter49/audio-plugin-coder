# Terrain — state for the next session (2026-09-15, evening)
- Branch feature/terrain-instrument. tp6 … tp12 (everything below) is COMMITTED in this commit, on top of 15b000a, and pushed.
- The factory one-shot library (Resources/Samples, ~1.2 GB) is NOT in git — rebuild it with scripts/sample-library/ (README). CMake copies it into each bundle when it is present.
- tp6 = the Patcher's fourth read: presence-based nodes (no power dots), the terrain map as the Randomizer, the Tape category (6 nodes), the deck restyle, FLOW tiles that extend, crisp zoom, the eye re-lays out, waterfall always, the dice (compressor last, Modal, no dead nodes, glide-tidy), Modal back with character families, nodes = an info row.
- Harness: Tests/_tp6_test.js (all green) + Tests/_tp6_home.js (the synth page is identical after Patcher round trips — the flow-wrap leak is fixed). Build: VST3 + AU from this tree, installed fresh + re-signed.
- NOT built: oscillators E–Z (measured — see the scope doc), the filter-table research (owed), the mini chop / Robin (Max: later).
- tp7 (2026-09-15): the dice rewritten (unison ≤7, both filters, FX types/back panels/per-osc subsets, 1–7 LFOs any shape with real depths, env 2/4/5 routes, Glitch fully re-rolled, compressor last); the old dice's hidden LFO→cutoff routes explained "the filter moves on its own"; the Patcher restores the waterfall and card bypass on return (HARD RULE: it never changes the synth page). Harnesses: Tests/_dice_test.js, Tests/_tp6_home.js, Tests/_dnd_probe.js — all green.
- tp8 (2026-09-15 ~11:30): zoom fixed the mockup way — no layer triggers inside the world (the glass blur made WebKit stretch each node), painters size from layout px with a max(1, zoom) bitmap, no resize events; Voice, Deck and the oscillator X removed; layout v4 = one tidy plan (signal left→right, modulation row under the synth). Probes: Tests/_zoom_probe.js, _tp6_home.js, _dnd_probe.js — green.
- tp9 (2026-09-15 ~12:30): menu z-order (underlines hide under Patcher menus), menu font = the preset menu's, preset browser without environments (5 glyphs, no Env tags/count), drag from anywhere on displays, tighter layout v5, eye fits without stretching, FX cards animate on the Patcher (rack drivers see them), Patcher on the page's one clock (0 frames at rest), wind-down + fades, tape/deck knobs = the synth ring, more Filters via the rack Filter card. WebKit testable (Tests/_webkit_zoom.js). NEXT BIG ONE (Max): engine pools — many Glitch/Arp/Robin/Chop/tape machines/filters, osc E–Z, per-branch routing.
- tp10 (2026-09-15 ~13:40): FX cards play on the canvas (the rack's own listeners recorded at boot and replayed there), LFO dots/paintbrush live + every node drags from its edges, no node menus (module right-click runs; Flow tiles open in place), no menu on a dropped cable, one menu at a time, dice menu without captions + Percussion (Modal), the eye scales instead of resizing (nothing vanishes), solid dark glass, FX pills centred/stacked, the tape card named Studio/Cassette with its preset centred. Probe Tests/_tp10.js. NEXT: the engine pools.
- tp11 (2026-09-15 ~15:00): engine — the Patcher (uiPage 5) gets the analyzer push (its filter spectrum froze); ModDest TapeSlot1..3 = 1887..1889 (the selected tape machine's three knobs, global pass, NumDests 1890). UI — painters key on data-dev (FX pictures vanished after a roll), pane equaliser every open in layout px (tabs moved the card), tape/knob painters draw at the canvas's own scale (no zoom drift), padded modules + layout v6, Randomizer bottom right with cables (rlinks) + Light/Medium/Heavy/Wild, port right-click/⌫ unpatch, cable click + ⌫, cards drag from blank space, menus weight 400, chain chip white, the eye blinks/unclips/screens. Probe Tests/_tp11.js + home/dnd/zoom/tp10 green.
- tp12 (2026-09-15 ~17:00): THE FACTORY ONE-SHOT LIBRARY — Max's kits → Resources/Samples/<Category>/<Category NN>.flac (tuned to C with rubberband, lead-in trimmed, −1 dBFS, deduped; Textures = the 270 CC0 Freesound files). NOT in git (~1 GB): rebuild with scripts/sample-library/ (README). CMake copies it into each bundle; native scanSampleFactory + sampleFactoryRoot(); the sample browser reads it (melodic first, Textures last). The dice: Sample/Granular/Resynth with melodic one-shots only (Modal may take Textures), sample knobs + blend slots rolled, detune never, compressor untouched (no params, no LFOs), tape knobs up + an LFO on them. Probe Tests/_tp12_dice.js; tp11/home/dnd/tp10 green.
- tp12b: THE RACK LEAK — with a LADDER filter the rack did not process the filtered sound (the pull was a master subtraction of a base-rate mirror; the Ladder main path is 2x oversampled → phase mismatch; rack LP after Ladder = −2.8 dB, SVF = 94 dB). Fixed in SynthVoice (exKeep_: pulled sources never enter the main buses) + the processor (no exclusion bus / subtract). Phase 180° / Rand 100 / Random on rolled + created oscillators.
- tp12c: PHASE — every oscillator the plugin summons sits at 180° in the ALIGNED mode (PHASE_MODE 0, resolvePhase case 0 = wrap(PHASE/360) every note; PHASE_AMT is ignored there). tp12 had written MODE 2 = Random by mistake ("a random 180 is not 180"). phase180() in index.html; the dice writes it to all four oscillators, off ones included.
- tp12c (cont.): the SHIPPED DEFAULT SYN_OSC_x_PHASE_MODE 2 (Random) -> 0 (Manual/aligned) on all four oscillators, so a fresh instance / Init also sits at 180 deg (PHASE already defaulted to 0.5). Saved patches keep their stored value; a factory preset that omits PHASE_MODE now inherits 0.
- tp13: the dice — melodic one-shots ONLY (Bass/Bell/Keys/Lead/Pad/Pluck/Synth; no Accent/Atmosphere/Chord/Vocal/808/FX/Textures/Drums), drums only in a percussion roll (layered, one per osc), GRANULAR OFF (Sample + Resynth only, Resynth given a real spectral window), the whole 118-filter roster, filter 2 at medium .45/heavy .8/wild .95 with a guaranteed route and NONE when off, LFOs 3-10 with back-panel targets (FX knobs 4-11, filter back dests, osc spectral/warp-var/unison/sub/FM/Modal, blend depths). Probe Tests/_tp13_scan.js.
- tp14: RANDOMIZE CHAIN — a dice row above the FX rack (in .rr-left, outside #fxr-rack) with a Light/Medium/Heavy/Wild reach chip (localStorage fxrDiceLvl, house menu). Purely effects: devices/types/knobs/back panels/routes/order only; no osc/filter/env/LFO param and no mod route (probe Tests/_tp14_fxdice.js proves it), compressor last and untouched. Hooks window.__fxrDice(level) / __fxrDiceLvl().
- tp15: RANDOMIZE MODULATION — second dice on the same row (Light/Medium/Heavy/Wild/CRAZY, localStorage fxrModLvl). Rolls LFOs, envelopes (AMP only at heavy+), macro values, and routes from every matrix family (lfo/env/mac/key/vel/whl/at/bend/rnd/alt/fol) into osc+back panel, both filter slots, rack knobs 0-11 (never cmp), tape slots, LFO amount/rate/phase, envelope stages and the macros. Never pitch/detune/phase. New export window.__tiAddSrc(srcObj,dest). Probe Tests/_tp15_moddice.js. TRAP: a hoisting order bug made bind() throw inside the rack's try/catch and silently killed every hook below it — parse checks cannot see this; check surviving globals.
- tp16: THE DICE MENU IS THE ONLY HOME (Max). The .fxr-dice ROW above the rack is DELETED (css/markup/chips/pickers/bind). Randomize chain + Randomize modulation are now rows in the Patcher's dice-tool right-click menu (openAimMenu -> rollRack), under ONE reach: layout.dlevel, LEVELS = light/medium/heavy/wild/CRAZY. crazy is a real tier everywhere (REACH.crazy 25 keys, SAMPR/BLENDR, chain POOL/CNT, every big=/wild test). TRAP: the rack dice's setType used rack.querySelector — with the Patcher open the cards are adopted onto ITS canvas, so a roll from the menu set no types at all; it is document.querySelector now. After a chain roll from the canvas: __fxrRestoreAll then adoptFx/syncPresence/sync/commit/glideTidy. Probes Tests/_tp16_menu.js + Tests/_tp16_roll.js; _tp14/_tp15 rewritten for the row's absence; tp6_home/dnd/tp10/tp11 green.
- tp19 (2026-09-15 night): THE RACK GATE. Tape/Multiband/Widen routed+powered rendered -180 dBFS without a message thread (Tests/au_rack_gate.cpp): the voice pulled a source on the PILL union (exKeep_) while a pooled send only lights when its FilterSlot pair is built by the message-thread timer (poolOn). Fixed in SynthVoice.h: exKeep_ derived per block from RUNNING sends (lit pool pairs + live legacy sends), capped by the union; unbuilt pair = bypass. Golden bit-identical to tp16 (Tests/pool_identity.cpp). Page: __tiBatchBegin/End — the modulation dice and the Patcher dice push the matrix ONCE (was up to 32x, starving the message thread = the UI lag AND the gate). Widen picture is audio-driven; painter fine (_tp19_widpaint.js).
- tp18 (NOT shipped): rack pool 6->8 saved as Design/pool-widening-tp18.patch — builds, UI hands out 8, 96 unique mod dests, but SIGSEGV in buildStateTree at instantiate (suspect: isFxModDest spans 6 appended instances while only 2 exist -> fxModDecode inst 8..11 into an 8-deep fxModRef_). 12+ blocked by FxChainTopology kMaxSlots=128 (SlotMask width). Flow 1->4 and osc E-H: mapped, not built.
- tp20 (2026-09-16): THE POOL IS BUILT. (a) OSCILLATORS E–H = bank 1: a second UnisonSynth of unmodified SynthVoices (a bank-1 voice's A..D ARE E..H); the block gather runs once per built bank — bank 0 = the old text (bit-identical, Tests/pool_identity.cpp), bank 1 through rawParamB() (A–D id pointer -> E–H twin, OscBankIds.h GENERATED by scripts/gen_osc_bank_ids.py), its own mod sums (destForBank; mirror block OscBank2Base=1890+d), per-bank gates, per-osc stores [8], no noise/Robin/global taps. Built lazily on the message thread (ensureBankB) when any SYN_OSC_E..H_ENABLE is on; no message thread = no bank (documented). Routing: 10-bit FxChainTopology masks (E–H = bits 6..9), routeSnapB_. Params CLONED via a LayoutTap in createParameterLayout (osc B -> E..H, every _SRC_B -> _SRC_E..H, Flow cards -> instances 2..4): 4072 -> 5655 params, +4 MB. (b) FLOW × 4: FLOW_CHAIN_1..16 + FLOW_CHAIN_INST_n; resolveFlowChain(slots,insts,16,legacy) dedupes on (kind,inst); Robin stays ONE; stages read fid(inst,id)/fd(inst,dest) (FlowInstBase=3780 + (n-2)*473 + (d-8)); Arp chain = MIDI -> Arp a -> Arp b -> synth. (c) PAGE: window.Juce.getSliderState wrapped — pool ids get a pool slider state over ONE batched getSynParams native; E–H devices = clones of B parked in #osc-pool-home, adopted by the Patcher when on; KNOBDEST +1890 mirror; MODDEST flow mirror (__flowModDestOf); rack 10 pills (E–H hidden until body.tp-pool-awake); chain entries 'chop'/'chop2'; cards are factories (__flowCardOf(kind,inst), .ti-inst2, TIC.presets kind/folder); card-only ?card=chop2. Gates: Tests/au_osc_pool.cpp (11), Tests/au_flow_pool.cpp (11), fxtopo_test (+E), FlowChain_test (+5), golden bit-identical, tp19 rack gate, probes _tp20_pool.js/_tp20_canvas.js + tp6/10/14/15/16/19 green. NOT DONE: E–H live scope overlay (oscScope[4] walks bank 0 only — the waterfall works), rack 6->8 (tp18 patch), Robin x1 by design, filter CPU (parked). Max has not heard it.
- tp20 SIZE SETTLED (Max, on seeing it): "I think this would be enough fr, 8 oscillators, 16 flow cards." The pool is DONE at 8 oscillators (2 banks) and 13 Flow cards over 16 chain slots (Arp x4, Chop x4, Glitch x4, Robin x1 by design). Do not widen either further unless he asks again.
- tp21 (2026-09-16): THE FILTER'S 2x NOW ASKS WHETHER THERE IS ANY DISTORTION TO TAME. 26 of 118 types ran their core TWICE per sample always; e47f140 priced it (Ladder +4.87% of a core at an 8-note chord vs SVF +0.21) and left the lead that a Ladder at DRIVE 0 measures 0.10% THD. Tests/filter_alias_ab.cpp is the GATE that had to pass first: it drives the real FilterSlot through the real HalfBandUp2x/Down2x across (type x cut x res x drive), splits the spectrum into harmonics (exact FFT bins, a sine at bin 1500) vs everything else = aliasing, and also nulls the two paths against each other. FINDINGS (data kept: Tests/fixtures/tp21_filter_alias_ab.csv): (a) below drive 0.30 EVERY oversampled type keeps aliasing under -57 dB and most under -120 — the SVF, which never oversamples and ships that way, reaches -51.1 dB at its own worst cell, so those were already inaudible BY THE PRODUCT'S OWN STANDARD; (b) the two paths differ by a LEVEL of at most 0.49 dB (median 0.10) — gain-matching the spectra drops the residual to -41..-108 dB, so it is a level, not a shape; (c) a COMPLEX null is useless here (it measures the 2x's ~2.2-sample group delay and reads -1 dB everywhere) — use magnitudes; (d) RING MOD and BODE SHIFT cannot be judged by a harmonic/alias split at all, they are inharmonic BY DESIGN — left alone; (e) ⚠️ the WAVESHAPER is made WORSE by oversampling in 30 of 42 cells (base -105.3 vs 2x -75.8 at cut 8000 res 0 drive 0.30) — the drive gate bypasses 16 of them, 14 remain and it is a LEAD, not shipped (a shaper's 2x may be character, not aliasing; Max's ear, not a spectrum analyser). THE FIX: FilterSlot::oversamplingWanted (drv, res) = needsOversampling() && (drv > 0.22 || res > 0.90); needsOversampling() KEEPS its old meaning (the converter-reset bookkeeping and FilterFxEngine's coefficient rate still ask it). The choice LATCHES per note (osOsLatch_, set in startNote) and only ever moves UPWARD, so a drive knob or an LFO on it upgrades ONCE mid-note and never chatters, and the cheap->expensive step lands where the user is already changing the sound; the latch drops only if the TYPE leaves the family. MEASURED (au_filter_types, drive 0): Ladder +4.87 -> +2.02, Waveshaper +4.38 -> +1.85, Xpander +2.55 -> +0.78, Diode +2.23 -> +0.79, Acid +2.24 -> +0.85, Polivoks +2.28 -> +0.70 — about 60% of the filter's cost, and an undriven Ladder now costs exactly HALF a driven one (one core run, not two). Gate: Tests/au_filter_os_gate.cpp (7 checks, in CPU, both directions). Scanned Max's 115 saved patches: 45 use an oversampled type, 39 of them sit at drive <= 0.30 on every such slot (median drive 0.079) and go cheap. COST: those 39 patches now render up to 0.49 dB different on the filtered path. Golden bit-identical (the default patch is filter NONE), pool/flow/rack gates green.
- tp22 (2026-09-16): THE FILTER TABLE — filter type 118, whose response curve IS a wavetable's harmonic content. Reference CHECKED (Kilohearts Filter Table, NAMM 2025): harmonic content of one FRAME = the shape, scanning morphs it, CUTOFF places harmonic 24, RESONANCE is a CONTRAST control (not a Q), four phase modes (=> theirs is convolution). We took the anchor + the resonance semantics; NOT the implementation — a kernel's response is fixed at bake time and CUTOFF is the most modulated parameter here, so ours is 32 log-spaced BANDS (BellEQ, recycled) whose gains come from HarmTableSource::bake -> FilterTableSource::bake (dB relative to each frame's own mean, so scanning changes SHAPE not level), published via the harmTable_ contract (double-buffered/atomic/cooldown) so 96 voices x 10 slots share one read-only bake. Linear => not oversampled. Params: SYN_FILTER1/2_TBL (choice over wtRoster) + _TBL_FRAME (modulatable; mod dests FltTblFrame1/2 appended at the TAIL after the pool mirrors, NumDests 5201). ⚠️ HAZARD 1: FilterSlot::setParams memoises on 8 inputs — the table + frame are a 9th and 10th, and without them the bells compute once and FREEZE. ⚠️ HAZARD 2: a band past Nyquist must be BYPASSED not clamped (BellEQ pins fc, so a high cutoff stacked a dozen bands on the ceiling and drove the curve the WRONG WAY). ⚠️ HAZARD 3 (in the TEST): a mean dB diff over every FFT bin is dominated by the noise floor and read 9.5 dB between two IDENTICAL renders — mask to peak-60 dB. The drawn curve is the DSP's own numbers (native getFilterTableCurve + mag()'s 'tbl' branch multiplying the same 32 bells; 'tbl' in MAG_ANIM because FRAME is modulatable). Gates: FilterTableSource_test (18), au_filter_table (5, magnitude-spectrum only per fb283), flt_cardinality 8/8 across all TEN roster sites, flt_ui 119/119, golden bit-identical. OWED: the table PICKER (no control selects the table yet — layout is Max's call); the 454 factory .flac tables in 13 categories (today's source is the 46-entry wtRoster; the 454 are files via the import path); the rack Filter card's own table params (6 instances).
