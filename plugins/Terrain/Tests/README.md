# Tests — committed harnesses

Every build bible in `Design/` cites harnesses by paths that do not exist: they were written in
per-session scratchpads under `/private/tmp` and evaporated. These do not.

```sh
cd plugins/Terrain
clang++ -std=c++17 -O2 -Wall -o /tmp/fxtopo_test Tests/fxtopo_test.cpp && /tmp/fxtopo_test
clang++ -std=c++17 -O2 -I Tests/shim -I Source -o /tmp/flt_cert Tests/flt_cert.cpp && /tmp/flt_cert
```

| Harness | Covers | Gates |
|---|---|---|
| `fxtopo_test.cpp` | `FxChainTopology` — per-osc routing, merging, the serial chain, the 50-device deep chain | 143 |
| `flt_cert.cpp` | `FilterFxEngine` — unity, mix law, engine distinctness, every knob, stability | 34 |
| `send_mode_test.cpp` | fb414/415 SEND — which oscillators leave the main mix, and the first-slot law | 19 |
| `fx3_sync_test.cpp` | the SYNC LAW across chorus + flanger + phaser AND the UI's own label | 21 |
| `fx3_ui.js` | the fb413 cards: 6 instances each, route pills, the strobe law, no-doubles, the C++ push | 48 |
| `grn_click_audit.cpp` | fb416 granular — overlap/duty, the slope detector, the FFT band. **Reports, does not gate.** | — |
| `fx3_audibility.cpp` | fb417 "can you hear it" — spectrum distance knob-0 vs knob-100. **Reports, does not gate.** | — |
| `wt_list_gate.py` | fb530 — the WAVETABLE ROSTER's **ten sites**: the enum, `specForPreset`, four `StringArray`s, four `<select>`s. Index for index, label for label. | 24 |
| `harmonic_ceiling_gate.py` | fb530 — **THE ONE LAW**: no generator may carry a harmonic-COUNT ceiling. The 25 capped legacy generators are FROZEN (the debt, written down); a new ceiling anywhere fails the build. | 8 |
| `fb563_menu.js` | fb563/fb564 — THE CONTROL MENU: every destination opens exactly one house menu; the Modulate picker (six families, multi-select, a double-click ASSIGNS and closes); route rows (tabular depth · curve · remove · bypass · scale by); registry rows; Reset; Copy/Paste; MIDI Learn; the codec; **fb564 THE GRID** (no header, no rules, one left rail, ⤢ · ✕ · › identical 16 px boxes on one right rail, ✕ red on hover); a macro's own menu (Rename · Reset · MIDI Learn, names persisted); Copy oscillator with modulators; a macro as a destination (fb565: nine macros, 3×3, Modulate › on a macro, its own picker hides itself). fb566: the hand-driven comet interpolates between feed pushes. Six mutations. | 54 |
| `fxmod_cert.cpp` | fb453 — the FX rack's modulation destinations and the SHIPPED per-block math (`FxModValue.h`, JUCE-free); **fb566** every family reaches the rack (macro · wheel · velocity · bend add, follower · Key own), Scale by and the connection curve apply, a source with no view is dropped. `clang++ -std=c++17 -O2 -I Tests/shim -I Source Tests/fxmod_cert.cpp` | 46 |
| `mod_src_cert.cpp` | fb563 — the six new SOURCES reach the audio on the installed AU (macro · wheel · aftertouch · bend · random · alt), the door drops unknown codes, depth sign, a MACRO AS A DESTINATION (wheel → Macro 1 → Level A; fb565), Macro 9 on wire 228, **fb566** a macro (and the whole wheel → macro chain) into the RACK's reverb mix, the Free LFO RUNS while a note sounds and PARKS across silence (A/B), the connection curve, bypass, scale by. Level A at ZERO so the source is the only way to a sound. **fb572** bar 7b: two Random routes (Level A, Coarse A) over 24 notes draw INDEPENDENTLY (one Random; read r = +1.00 before). | **fb573** bar 14: an LFO on a POINT of a connection curve moves the level (two windows differ; amount off → match; the parked bank re-bakes nothing). | 28 |
| `macro_rename_gate.js` | fb569 — right-click a macro -> Rename works EVERY time: a RIGHT-button press on the name grip arms no drag (the phantom drag that ate the Rename click is gone), the LEFT-button drag still works, the menu carries Rename, and Rename enters edit mode. Mutation: guard removed -> bar 1 red. | 6 |
| `filter_mod_cert.cpp` | fb568 — a MODULATOR MOVES THE FILTER CUTOFF on the installed AU: macro · wheel · aftertouch · random each move Cut1, random SCATTERS it per note (Max's ask); LFO unbroken; the dry-send trap. The per-voice cut gather knew LFOs only. | 9 |
| `midi_learn_cert.cpp` | fb563 — a learned CC moves its parameter on the installed AU; the map installs and round-trips through the state; CC 1 bound to a knob still drives the wheel source. | 7 |
| `lfo_park.js` | fb567 — THE LFO PLAYHEAD in the plugin: rides the pushed phase while notes sound; NEVER creeps on a stale feed (the pre-fb567 painter simulated 342 px/s of motion in silence); fades out and RESTS (0 writes) in silence; rAF mode (the popped card) stops and restarts; a DEAD feed parks it. **fb570** bars 9/10: the fade-out runs on ITS OWN .35 s curve (still > 20 % visible at 150 ms — fb567's idle rule had lost `transition` to the base rule by specificity, a 120 ms fade that read as a click) and the curve's breathing is PAUSED, not removed. **fb636 h2** bar 10 re-based: in silence the curve FINISHES the breath it is in (`.mv-br-run`, `lfoIdle` → `mvBreath`) and rests paused EXACTLY on its .9 pose (the 3.2 s boundary, measured 0.000 ms off, opacity 0.9000) instead of freezing mid-cycle; `LFO_PARK_MUTATE=3`'s anchor follows the new idle rule (still → 9 10). Mutations 1-3. | 14 |
| `mac_idle_frames.mm` | fb567 — the INSTALLED AU, its real editor (windowless, fb521), audio rendered at real-time pace on a thread: frames the editor SHIPS per second at idle (≤ 6 — measured 0), with a note held (≥ 20 — 59), and at idle again after it (0). Reads the plugin's own beacon (`terrain-cpu.txt`, on the Mac since fb567, opt-in), whose DSP / UI lines are the Mac editor-open numbers. Before fb567: 53 / 54 / 59 — the loop never stopped. | 5 |
| `crv_popout_gate.js` | **fb570 — EVERY CURVE HOST POPS OUT.** Main page (a recording Juce stub): a warp curve's ⧉ parks its IDENTITY (`setCardState('crv', {warp,osc,slot})`) BEFORE `popOutCard`, no refusal toast, the docked copy hides; the SAME host re-opened is only re-fronted; opening a MOD curve while it floats RETARGETS (`setCardState({mod,s,d})` + `retargetCard`) with NO docked twin; Dock reopens the host it left with (mod → warp slot 2 → the Distortion), by identity alone, placed by the editor (never a body anchor at 8,8); one relay kick merges a curve the wire carries and Dock opens on it. Card-only page (`?card=crv`, relay-less like the real card window): a warp identity boots the warp host with exactly ONE 300 ms follow-poll, a point edit CAPTURES through `getParamCardinality`'s cache (`SYN_OSC_A_WARP_MODE` → 37, `setWarpDrawCurve` 129 samples) and the poll stands down, the Distortion lanes asleep; a mod identity boots the mod host and its edit lands in `setSynthMod` with `c`; a mod route with NO surface on the card page (LFO 3 Depth while tab 1 is active) still boots and its edit carries the WHOLE matrix (the review's blocker: the old mirror deleted every route it could not draw); a host that cannot mount, or a route removed from the patch, CLOSES ITS WINDOW (never a blank always-on-top window with no ✕); no identity = the Distortion. Mutations 1-5 (the fb560 toast back · identity not parked · the fb559 guard · no cardinality cache · the mirror's no-surface bail back). | 16 |
| `card_natives_gate.py` | **fb570 — THE TWO NATIVE LISTS AGREE.** Every native the curve editor, its warp/mod hosts, the mod mirror and the card-only boot call (read out of index.html, never typed here) is registered on `TerrainCardWindow`'s OWN list — the fb328/342/343 law made structural (a main-only native never settles its promise in a card window: no error, an empty field, a leaked poll). `CNG_MUTATE=drop` reds it. | 29 |
| `fb570_popout.js` | **fb570 — THE REAL POP-OUT** (a `curve_probe.mm` gesture, installed AU, real editor, real second window): ⧉ on a warp curve → `getPoppedCards` says `crv`, the identity is parked, and the POPPED PAGE reports what it booted (`getCardState('crvBoot')` = `{key:'warp', title:'Fractalize · OSC A · WARP 1 — Curve'}` — the second WebView is otherwise invisible to every harness); a mod curve opened while it floats → the popped page reboots as `LFO 1 — Curve`; Dock → the docked card comes back on that host. `CP_SETTLE=12000 CP_REPORT=<scratch>/report.js /tmp/curve_probe Tests/fb570_popout.js`. **Reports, does not gate** (read the trace). | — |
| `curve_apply_cert.cpp` | **fb572 — A CONNECTION CURVE ON A ROUTE YOU ALREADY HEAR.** Installed AU: for Env→Fold, LFO→Fold, Env→Level, Key→Level, Random→Level, Macro→Level and Macro→Reverb Mix, a straight / inverted / zero curve in FRESH order (route + curve in one push) and in MAX'S order (the route first, the curve drawn on it after). Before fb572 every per-voice destination was INERT in Max's order (modCfgEq never compared `curve`, so the voices kept a stale index) and a route could read ANOTHER route's curve after a shift; the global pass and the rack were fine. | 22 |
| `rand_hash_gate.py` | **fb572 — ONE RANDOM, ONE HASH.** The page's `randForRoute` twin and `SynthModConfig.h`'s are run over 512 (seed, dest, ordinal) triples (seeds spanning 0, 1, 2^31, 2^32−1) and must agree bit-exactly — the comet is a display of the DSP's draw. | 1 |
| `crv_ptmod_gate.js` | **fb573 — AN LFO ON A POINT OF A CONNECTION CURVE.** Modulating a point by LFO 2 sends `p` (points + {ys,ya}) beside `c` on the route; the editor reopens from the points with the mod; an LFO value pushed through the frame feed LIFTS the modded handle and rewrites the ink (−1 lowers it; a stale feed restores the static drawing); Off drops `p`, and a straight base drops `c`. Mutations 1-2 (no painter · no `p`). | 5 |
| `mark_air_gate.js` | **fb571 — THE MODULATION MARK KEEPS AIR UNDER ITS WORD.** Max: "it looks way too close and cluttered... put it down a little, one or two pixels." The line sat 2.5 px under the baseline on every surface (a pixel under the letters, touching a descender); now 4.5 px under the ink on every knob word (+2 on the 8 px words, +3 on the rack's 7 px word), the envelope shelf chips unchanged. Also: the macro mark spans the WORD (the word selector did not know `.vm-ml`, so it measured the knob+label wrapper), and the macro labels are the synth page's 8 px ("Baby" read smaller than "Macro 2"). Bar 4 measures the room under every rack mark on both faces (tightest: the Utility back panel, 3.0 px). Mutations 1-3. | 6 |
| `macros_still_gate.js` | **fb574 — THE MACROS TOGGLE MOVES NOTHING.** Max (with a screenshot): "every time we press macros, it does that padding and that resize again, which... fucks up with our LFO visualization... Nothing can move." Measured: the bottom row is content-sized and the voice column swapped its two views with display:none, so the row, the rack and every device took the height of whichever view showed (Chromium 187 → 159 px; the real WebKit page 187 → 182), and the marks — painted on the frame clock, asleep at idle — stayed where the old layout had put them (7 px under the Mix word, then 3 px INTO it). Now both views sit in one grid cell, the inactive one visibility:hidden. Bars: real clicks off → on → off keep the row, the rack, the device, the WT Pos word and the Mix word byte-identical; the marks hold with no frame ticked; both views share one rect; a routed macro's mark shows only with the Macros view (2 → 3 → 2); Mono moves nothing either; (fb575) 40 ms after each click the Always/Scaled pills carry their view's visibility, the hidden view is opacity 0 and the pills transition colours only — the real WebKit page never ended the old `transition:.14s` (= all) on the inherited visibility, measured 3 s+. `MACROS_STILL_MUTATE=1..4` prove the bars can fail. |
| `fm_ops_cert.cpp` | **fb587 — THE FM OPERATOR STAGE MOVED, AND THE MOVE CHANGED NOTHING** (offline, no plugin). Making the waterfall show FM meant the drawing had to run the operator stage — and this codebase forbids the display keeping a second copy of DSP, so the stage moved out of the render loop (where it lived inline, FOUR times) into `Source/FmOperators.h`. That is a refactor of the HOT AUDIO PATH ×4. ⚠️ AN AUDIO NULL TEST IS IMPOSSIBLE HERE: the synth is deliberately not repeatable (fb544's continuous phase and a per-note-on alternation free-run across notes — measured, two back-to-back renders of one note differ in **61440 of 61440 samples**). So the proof is made where it can be exact. 🚨 THE REFERENCE IS MACHINE-EXTRACTED FROM SynthVoice.h AND MECHANICALLY RENAMED, NOT RETYPED — retyping risks making the same slip twice, once in the header and once in the thing meant to catch it. 3 bars: the operator stage is bit-identical (**960,000 samples over 3 algorithms, 0 differ**); the modulator advance is bit-identical; and every FM control visibly deforms the drawn cycle (0.18–0.37 cycles of phase deviation, closest pair of rigs 0.34 cycles rms apart). ⚠️ Bar 3 compares CURVES, not magnitudes — mean |deviation| is blind to frequency and read an identical 0.3325 for 'index up' and 'ratio 3.5', which would not have proved the ratio knob changes the picture at all. ⚠️ Proven able to fail: three mutations red it, including merely RE-ASSOCIATING the carrier sum (`+ blendOff + qSub` instead of `+ qSub + blendOff`) — 45,724 samples differ at 8.88e-16, which is why `run` takes the carrier phase as an argument instead of returning an offset. |
| `fm_wtpage_gate.js` | **fb586 — THE WAVETABLE SET IS FM'S THIRD PAGE, AND IT IS FOUR, CENTRED** (page layout). Max: "the four buttons need to be aligned symmetrically evenly in their center and not there's one missing space onto the right or the left." That sentence is three measurable claims and this gate is all three, plus two more. 6 bars: the panel actually laid out (see below); FM's third set IS the wavetable set; FOUR knobs not five (Feedback stays on FM's own page); evenly spaced (gaps 72.75/72.75/72.75, spread 0.00); centred (row midpoint on container midpoint to 0.00 px); and ONE element per parameter panel-wide — the four are the SAME DOM nodes the WT engine uses, because a duplicated data-syn would bind twice and the mod-matrix lookup keeps only the LAST match. ⚠️ THIS GATE CAUGHT ITSELF LYING. Its first run reported "gaps 0.00 / 0.00 / 0.00, spread 0.00" and called that perfectly even — the panel had never laid out without the JUCE bridge stub, so every rect was empty. Bar 0 now installs the stub, force-shows #syn-panel, and REFUSES to assert any layout bar until the geometry is proved real. |
| `harm_table_cert.cpp` | **fb588 — HARMONICS ← WAVETABLES: the additive bank built from a table** (offline, no plugin). Max: "I want to be able to actually have additive synthesis with the wave tables." 6 bars: the bank matches the WAVETABLE OSCILLATOR on the same table far more closely than that table sits from any other (worst 6.3 dB vs 15.0 dB to the nearest other table); the PHASE came through (waveform distance 0.006–0.011 on harmonic tables — a WAVEFORM bar on purpose, because magnitude-only passes a spectral test perfectly and is still the wrong answer); SpectralDrift is alive (its amplitudes move **0.00e+00** across all 16 frames and it morphs only phase, so under magnitude-only its WT Pos knob would be stone dead — the sound still moves 1.312); the 16-voice headroom holds (**4.14×**, against 16.0 coherent and 4.0 incoherent — the per-sibling rigid rotation is what buys that); the six existing families still render; and an IMPORTED table is a first-class source through the real path, PCM → `buildFromPcm` → `toSpec` → bank (1/h tilt holds to 0.5 dB over h1–20, band ends at exactly h22 = frame 9's content). 🚨 THIS CERT CAUGHT A SILENT HALF-SPECTRUM. `WavetableSpec` stores SIGNED amplitudes and the bank has no sign to give a partial, so on Square every negative harmonic rendered at ~0.005 while every positive one matched at ratio 1.00 — 17.9 dB off the table, now 3.59. Paired with a bit-identity null: the six existing families are byte-for-byte unchanged across 108 param configs (6 families × 6 sculpt modes × 3 value sets). |
| `harm_table_au.cpp` | **fb588 — ADDITIVE SYNTHESIS FROM THE WAVETABLES, ON THE INSTALLED PLUGIN** (drives the shipping AU). A bank that only works in a test harness is not a feature. **🔁 RETUNED IN fb601, AND THE GATE WAS THE THING THAT WAS WRONG — it read 3 pass / 4 FAIL against correct code.** It picked the table parameter by FUZZY NAME (`find("Wavetable") \|\| find("WT Preset")`), so after fb601 split the roster it kept driving `Synth OSC A WT Preset` on the HARM path — a parameter the harmonics engine had stopped reading — and every distance collapsed (`[3] Square→VowelMorph 0.0 dB`, `[4] HUE 0→0.5 moves 2.0 dB`) while the bars still LOOKED like they exercised the menu. The table param is now resolved **per engine, by exact name**, the way `index.html`'s `__synTablePid()` does, with a printed PREFLIGHT and **no fuzzy fallback**: a missing name aborts the run by name rather than quietly measuring the neighbour. 7 bars: the AU offers a seventh family; it SOUNDS (−25.0 dB; case 6 renders silence on a null table, so this catches a bake lane that never published); **[2] the HARM render IS the wavetable's spectrum** — same table through both engines **0.01 dB** (Prophet Saw) and **0.00 dB** (Vowel Morph), against a **18.4 dB** cross-table scale so a pair of DEAD engines cannot read as agreement; the menu drives it (Square→VowelMorph **16.7 dB**, VowelMorph→D50Bell **26.4 dB**, with WT Preset held at Sine throughout so only `SYN_OSC_A_HARM_TABLE` can be doing it); HUE scans the frames (**25.0 / 16.3 dB**); **[5] TABLES ONLY** — all 7 stored `HARM_MODE` indices are **0.00 dB** apart on one table because fb599 pinned `mainMode = 6`, shown against a **15.1 dB** move when only the table changes; and no clicks sweeping HUE across all 16 frames (0.82x). 🚨 A CLEAN ZERO IS NOT AUTOMATICALLY GOOD NEWS — bars [2] and [5] each carry their own SCALE for exactly that reason, and it is not hypothetical: it is how the never-baked grid was found, and it is how this file itself rotted. Mutation: `HARM_TBL_MUT=1` redirects every HARM-path table write back onto `WT Preset` (the fb601 rot, re-committed) → **[2][3][4][5] red, 3 pass / 4 FAIL**, i.e. the exact report the stale gate was printing. ⚠️ Bars [2] and [5] previously asserted PRE-fb599 behaviour ("not like a procedural family", "THE SIX FAMILIES ARE UNHARMED") — the opposite of what ships; see the file header. ⚠️ The click bar's reference is the worst step across every static position the sweep VISITS, not one arbitrary standstill — the fb585 trap. |
| `harm_wtmenu_gate.js` | **fb588 — THE HARMONIC ENGINE GETS *THE SAME MENU*, NOT A SECOND ONE** (page). Max: "the same menu for harmonics mode" — a claim about WIRING, under the standing ♻️ RECYCLE law. 5 bars: the panel actually laid out; a seventh family exists and is called Table; picking Table reveals the wavetable name AND its stepper arrows; the other six still hide it (a Table-only surface, not a new permanent header item); and 🚨 IT IS THE SAME ELEMENT — the revealed control IS `#osc-a-preset-select`, the id `openWtSelectMenu` binds its glass two-pane browser to engine-agnostically, and that id appears exactly ONCE in the whole panel. Un-hiding it is the entire feature; a second copy would have been the failure. **fb601 — BARS [2] AND [3] REWRITTEN; [5] ADDED.** "The same menu" is settled; the question that replaced it is WHICH PARAM the one menu writes to, now that Harmonics selects its own table through `SYN_OSC_x_HARM_TABLE` while Wavetable keeps `SYN_OSC_x_WT_PRESET`. The old [2] ("picking Table reveals the name") and [3] ("every stored family shows the name") had collapsed into each other under fb599's tables-only rule and asserted the same visibility twice — that claim is kept ONCE as [5], and the freed slots ask the thing that can actually be got wrong: a header that writes the OTHER engine's table is a silent, saved-state-corrupting bug. The probe replaces `window.Juce.getSliderState` at ASSERT time (the page installs its own after load, and `__synTableState` consults it at WRITE time) and records every write: on `engine-harm` the click lands on `SYN_OSC_A_HARM_TABLE=0.0667` with `SYN_OSC_A_WT_PRESET` neither written nor moved (still 0.25); off it, the SAME element writes `SYN_OSC_A_WT_PRESET=0.1111`. ⚠️ **[2] REPORTS `PEND`, NOT `FAIL`, ON A BUILD WHOSE PROCESSOR HAS NO SUCH PARAM** — `PluginProcessor.cpp` and `ParameterIDs.hpp` are read off disk to decide and the line names both files and what it found, because "red for a reason nobody can see" is the same zero-information bar the project bans. The denied id is simulated by handing back a **null SliderState**, which is exactly what a pre-fb601 processor does, and the page's own `__harmTblLive()` guard then falls back to WT_PRESET — so the PENDING line is a measurement of the fallback, not a shrug. Mutations: `WTMENU_MUTATE=1` force the live arm · `=2` `__synTablePid` always answers WT_PRESET → [2] red · `=3` force the PENDING arm on a build that does register it. |
| `harm_waterfall_cert.cpp` | **fb589 — THE ADDITIVE WATERFALL DRAWS THE BANK, NOT THE TABLE** (offline, no plugin). Max: "I think the additive mode should only be waterfall or something." Settled: bars stay primary, the waterfall appears where there is a table axis to scan. Runs the SAME 16-row HUE sweep the processor bakes. 5 bars: the rotation is exact — `displayCycle` skips `std::sin` in its inner loop (1.3M calls per bake) and advances each partial by a complex rotation instead, measured at **0.0004% of peak** against a literal `std::sin` sum over 512 partials; it is a SURFACE not noise (adjacent-row correlation 0.849 on VowelMorph and 0.998 on ProphetSaw, against **−0.058 / −0.041** for the mutation that forgets to pin the seed); the sculpt row moves the picture (weakest of Carve/Lean/Shine still moves the whole grid 37.4% — drawing the raw table would fail here, which is the display law); every sculpt mode gives its own surface (closest pair 45.4%); and the table axis is real (first row vs last 109.2%, swapping the wavetable 90.9%). ⚠️ Bar 2 states BOTH halves absolutely and on two tables — a ratio tuned to one observed number is a threshold about that table, not about the code. |
| `harm_waterfall_gate.js` | **fb589 — BARS STAY PRIMARY; THE WATERFALL APPEARS ON THE TABLE FAMILY** (page). 7 bars: the panel actually laid out; bars are primary on Table with the view not engaged; the Table family can engage it (one click swaps to the wavetable canvas); 🚨 THE SIX PROCEDURAL FAMILIES REFUSE THE CLICK — a REGRESSION bar, because before fb589 the toggle had NO engine guard, so clicking a Harmonic (or Sample/Granular/Resynth/Modal) display flipped `wtWaterfall.on` and persisted `wt3dView_` through `setWaterfallView` while `.osc-wave` was `display:none`: nothing drew and the saved view silently rotted; leaving Table returns the bars and returning restores the waterfall (state persists, nothing turns off by itself); the depth axis follows HUE on Table and WT Frame elsewhere (asserted by naming the parameter id actually read — the additive bank has no WT Frame knob); and 🚨 THE SIGNATURE CONVERGES — `cachedSig`/`__wtDisp` are POSITIONAL and fb589 added a 13th slot (`hm`), and a mismatch does not make the table stale, it re-bakes at the maximum rate FOREVER. Proven able to fail: dropping the 13th slot diverges. ⚠️ Bar 5 overrides `getSliderState` at assert time — the page installs its own `window.Juce` after load, so the `evaluateOnNewDocument` stub is not the one `wtpos` reads. **fb601 — THE CHURN BAND GOT ITS BARS ([7]–[11]).** fb600 shipped the band with its fold arithmetic, its "free and invisible at churn 0" claim and its agreement with the audio path all asserted in COMMENTS and measured NOWHERE: `grep -rl "churnBand|__harmChurnBand|churnSpan|churnTrace" Tests/` returned nothing, and this gate passed only because its stub never fed a churn — absence of coverage, not coverage. The rig is `hue_track_gate`'s (a synthetic 16-frame stack on HARM/Table, waterfall engaged) and the feed is the real C++ contract `window.__harmChurnBand` / `__harmChurnRate`. `churnTrace` is INSTRUMENTED, so "the band drew" is a count and not a belief (0 · 0 · 4 · 5 per repaint across no-feed / churn-0 / band 0.25 / +drift). Measured: a 0.25 band changes **20 706 pixels** (26.4 % of the surface, need > 400); 🚨 churn 0 is **byte-identical** to no feed at all — **0 of 314 080 canvas bytes differ**, churnTrace 0/0, and the idle repaint count over a 700 ms window is 43 vs 42 (±2); 🚨 the fold is the INTERVAL — `churnSpan(1.0, 0.2)` = [0.80, 1.00], NOT [0.80, 1.20] (also 0.95 → [0.75,1.00], 0.05 → [0.00,0.25], hw 0.90 at mid → [0.00,1.00]); 🚨 and the UI fold is nulled against **HarmonicEngine.h read off disk** (`x = x < 0.f ? -x : x` then `if (x > 1.f) x = 2.f - x`, both regex-matched so a rewrite reds the bar instead of silently nulling against a stale copy) — worst |Δ| 0.0e+0, drift samples outside the band over 28 (pos, half-width) cases 0, worst band slack 0.0e+0 (the band must be TIGHT, not merely containing). ⚠️ Tightness is sampled on `s ∈ [−1,1]` **plus the exact s where the excursion touches 0 and 1**; a 720-step sine grid lands near, not on, those points and reads a 3.3e−3 "slack" that is the sampling and not the band. Mutations: `CHURN_BAND_MUTATE=1` (fold reverted to raw pos±hw) → [10][11]; `=2` (the C++ publishes `kChurnDepth·kChurnDepthFloor` = 0.054 at churn 0 — the exact mistake fb600's J3 gate exists to catch) → [9]; `=3` (transparent accent: the code runs, no pixel changes) → [8]. |
| `osc_view_gate.js` | **fb590 — EVERY ENGINE KEEPS ITS OWN PICTURE, AND THE PICTURE NEVER LEAVES** (page). Max: "I'm on sample and I'm on granular, and I still see a wave table... Unison needs to be linked to their respective engine icons, not the wave table" and "the resynth visualizer popping in and popping out... it should just be static like everything else. like the LFO doesn't disappear." 9 bars. BUG A was pure CSS: `.uni-page` force-shows `.front-only` (where the unison KNOB ROW lives) and force-hides `.sample-view` — but Sample, Granular, Resynth and Modal keep their PICTURE in that sibling container, so the unison page handed all four the wavetable oscilloscope. Harmonic escaped only because its rules key on `.osc-wave`/`.harm-view`. 🚨 BUG B WAS THE OPPOSITE OF WHAT IT LOOKED LIKE. The placeholder never appeared — the SPECTROGRAM LEFT. `.samp-drop` is opacity-driven off a `.loaded` class that nothing MIDI can touch (`clearLoaded` has exactly two call sites, both explicit user/host actions); what came and went was the image on top of it. The editor idle-skips a frame byte-identical to the last and its 30-tick keepalive is dispatch-free, so `__geodeSpectrum` STOPS ARRIVING once nothing moves, and a 1500 ms watchdog then wiped the canvas AND dropped the baked layers. Live MIDI was the only thing holding the picture up — which is exactly why it read as MIDI-driven. The stall now PARKS the animation instead: image kept, loop stopped. Measured: picture survives at 78,520 lit pixels through the watchdog and half a second beyond, and repaints go 7 → **0** over ~30 parked frames (the CPU saving Max asked for). Bar 1b holds the LAYOUT: all seven engines land on picture h=65 at y=81, unison row y=153, gap 7 — the geometry every engine already shows on its own page. Getting there needed `flex: 0 0 auto !important` on BOTH containers, because `.front-only` and `.sample-view` each carry an INLINE `flex:1` (index.html:6792/6893) that beats every selector; without it they split the space 55/55 and the 65 px picture overflowed onto the knob row (measured gap −10 px). ⚠️ Bar 1b is a REGRESSION guard, not a bug detector — it passes on the pre-fix page because the wavetable scope it measures there has the same geometry. Bar 1 is what catches the bug. ⚠️ Bars 0–4/7 force classes (honest for CSS), but 1b drives the REAL interaction — `initSetArrows`' MutationObserver resets the page index when the engine class changes, so a hand-set `.uni-page` can be undone a microtask later. ⚠️ And the stub keeps the SLIDER STATE getters, not just `getNativeFunction`: the page installs its own `window.Juce` after load, so a stub that surrenders them leaves every parameter listener dead and the engine selector repainting nothing. Proven able to fail: bars 1, 4, 5 and 7 all go red against the committed pre-fb590 page. |
| `idle_gesture_gate.js` | **fb591 — THE RACK STAYS USABLE WITH NO MIDI AND NO SOUND** (page). Max: "I can't drag the LFO, and I can't drag the multi-band areas... What if I want to have it on pause and simply move the LFO or move the EQ? ... It should never have to happen, never." 6 bars, and they are element-independent: every widget that repaints from the shared clock is a painter in the `__tiFrameReg` registry and the registry runs them in one pass, so a probe painter counts EXACTLY the frames every LFO / EQ / rack canvas gets — no selector, no pixel reading. 🚨 THE BUG WAS MINE, FROM fb581. That law says a page which has heard the C++ lane must never invent a clock from SILENCE, because silence from a live lane means the plugin is RESTING — correct, and it left NO clock for a hand. The gesture wake (`wakeBurst`) is THREE frames at 0/90/350 ms armed by pointerdown/click: right for a click, useless for a drag, and there was no pointermove, wheel or key wake anywhere. Measured on the shipped page: a 600 ms drag got **5** painter frames, a wheel **0**, a key **0**. Now 49 / 42 / 22, with rest still at **0 frames** so fb566/fb581 are intact. ⚠️ AND PAINTING FASTER WAS ONLY A THIRD OF IT. The whole fx-rack/LFO/filter viz push in PluginEditor sits behind `if (! uiQuiet)`, and uiQuiet was driven purely by AUDIO AUDIBILITY — `eqQuietTicks_`, literally the EQ analyser's silence counter, which fb567 promoted to the page's rest signal (which is why Max said "it was the EQ that did this"). The fx-rack EQ reads its node FREQUENCY only from that push, so its picture could not follow a drag at any frame rate. fb591 adds a `uiGesture` native and a `uiTouched` term, so a hand re-opens the feed for 1.5 s. Third: `lfoFrame` returned before touching the DOM on every call once parked, so everything it draws besides the head froze — it now falls through while a gesture is live, with the park itself untouched (the head still does not move). MEASURED COVERAGE: **20 of 20 registered painters** go from 5 frames to 61 across a 600 ms idle drag; **9 are fx-rack painters** (conv, dly, dst, flt, fx3, fx4, grn, rvb, tape) and `fxr-fx4` alone drives 7 device cores. ⚠️ The chain paints on rAF, NEVER inside the capture-phase listener — painting there runs every painter before the app's own handler, the re-ordering that made `all_menus.js` fail 2-3 of 42 bars at random. `all_menus` 42/0 is the guard on that. Proven able to fail: bars 2, 3 and 4 all go red against the pre-fb591 page. |
| `eqz_dots_gate.js` | **tp44 — THE DOT SITS ON ITS OWN BAND. NOTHING ELSE MOVES IT** (page). Max: "whenever I'm controlling a point I don't want it to move the other point... it just can't stay still" and, on the Dynamic type, "when I stop holding the MIDI it shifts somewhere else". fb592 had put every dot on the COMPOSITE line, so a wide band lifted its neighbours' dots and the ride dragged them all with the audio. Now the engine pushes each node's OWN static curve (`bc`, `Viz::band`, ride divided out), the card draws it faintly and pins the dot to it. 7 bars: inventory (8 band paths) · every dot at its own gain, never on a tilted composite · moving one band moves no other dot AND a wild composite/db under a fixed `bc` moves none · two bands at one Hz with opposite gains draw apart and each grabs its own · a drag writes ONE band's Hz+gain · the wheel writes that band's Q only · the Low band's menu adds a Low Cut at its corner (free band, shape 1). [7] tp46 — THE PLOT AUTO-RANGES: +39 dB of Air (Amount 150 %) leaves through no edge and runs flat along none (the axis scales, `st.sc` units/dB, glided; Max: "the paint is rubbing off"). Mutation (the dot back on the composite): bars 1, 2, 3, 6 red. `NODE_PATH=Tests/node_modules node Tests/eqz_dots_gate.js` |
| `_ui_lockin_sim.js` | **tp44 — THE PAINT IS KING: the UI soak** (page, both UAs). Max: "the paint never scratches and the paint never disappears... run simulations... see where the UI will break". Instruments the page BEFORE it boots (every rAF callback, every registered painter — the registry swallows a throw, so dead paint is silent — every timer, window/document listeners, long tasks), then walks: three laps around the panels, one of every rack card + a flow chain, dice ×12, three remove-all/add-all churns (DOM/interval/listener leaks), six Bodes + six Splitters, the Patcher at 100 nodes (painter pass < 50 ms, derive < 30 ms, a 60-step drag with no > 100 ms block, zoom 0.5/2, fit, viz on/off, dice ×8, three syn↔tp laps with heap/DOM/interval/listener deltas), a 300-push 60 Hz soak with notes on (worst painter < 40 ms), every menu opener timed (< 80 ms on the thread), the six `?card=` pages, theme flips. After every step it fingerprints every VISIBLE canvas (24×24 downsample): ink that was there and is gone is a SCRATCH; `.fx-spec` (the audio layer, fb442) is exempt. Found and fixed on first run: the Patcher dice at 100 nodes blocked 1.3 s (`fxRemove` re-rendered the whole rack per card → `__fxrClear`, 39 ms), the noise cloud collapsed to 204×0 on the canvas. `NODE_PATH=Tests/node_modules node Tests/_ui_lockin_sim.js` · `SIM_UA=win` for the Windows arbiter path · `SIM_VERBOSE=1` lists every canvas |
| `_tp_dice_profile.js` | dev tool — a CPU profile (self + inclusive by function@line) of `__tpDice()` ×3 at ~100 Patcher nodes. This is how the O(n²) rack render was found (956 ms self in `render`). `NODE_PATH=Tests/node_modules node Tests/_tp_dice_profile.js` |
| `_tp45_flow_dice_gate.js` | **tp45 — THE FLOW CARDS' FLICKER** (page 5, stubbed backend whose `setSynParam` lands LAT ms late while `getSynParams` answers from what has landed — a dice roll's burst on the message thread). Max: "every time I try to randomize the flow cards they pop up and fade in on the patcher and then fade out, and on my main synth page they start flickering and I can't turn them on or off". Rolls the dice 40× and watches chain / tiles / canvas for 3 s after each; any change after 800 ms is a flicker. ROOT CAUSE: a poll issued BEFORE a write answered AFTER it and both the pool relay and the slider shim overwrote the page's fresh value with the stale one (the chain reverted to the previous roll, the tile left, 700 ms later it came back — and every write the revert provoked raced the same way). FIX: a value the page wrote after the poll was issued, or within 1500 ms, stands. Mutation (guards dropped): 3-4 of 40 rolls flicker at LAT 400. `LAT=400 ROLLS=40 NODE_PATH=Tests/node_modules node Tests/_tp45_flow_dice_gate.js` |
| `_tp45_cable_tip_gate.js` | **tp45 — WHAT IS CONNECTED TO WHAT** (page 5). Max: "hover over our node cables and it shows what's connected to what... a little x"; the Voices module "exactly like the synth page, popped out... click on the name and drag it"; "I don't want the dice to light up on hover". [1] hovering a cable shows the chip naming both ends (a mod cable names the knob) and lights it; [2] the chip's × cuts an editable cable (A's output cable → SYN_OSC_A_OUT 0, the cable leaves); [3] hovering a module lights every cable it owns; [4] the Voices module's Velocity label starts a mod-source drag on the canvas (its grips joined the CONTROL list — the node drag used to eat the press); [5] its pills keep the synth page's border (the column's 32 rules re-scoped from #syn-panel / .ribbon-row.rr-new to #tp-page .tp-voice); [6] the header dice is the gear's --text-secondary and does not change under the pointer. `NODE_PATH=Tests/node_modules node Tests/_tp45_cable_tip_gate.js` |
| `_tp46_mod_marks_gate.js` | **tp46 — A ROUTE THE PAGE JUST MADE KEEPS ITS MARK** (page 1). Max: "try to drag key onto the wavetable — you can't... it's being modulated by envelope 2 but there's no line". Key → WT Pos in the 3D waterfall view writes the route and marks the knob; a stale `getSynthMod` mirror reply (`[]` — a poll that left before the write landed) must not prune it: routes the page made in the last 4 s are kept (`a.t` stamped in `addRoute`); Key → cutoff and Env 2 → resonance the same. `NODE_PATH=Tests/node_modules node Tests/_tp46_mod_marks_gate.js` |
| `_tp47_marks_gate.js` | **tp47 — THE MOD MARK ALWAYS READS · THE FILTER GRID PLAYS ON THE CANVAS**. Max: "you see ENV 2 but I see NO ATTENUATION LINE... we need to ALWAYS see mod sources" / "move the filter with my mouse on the patcher like on the synth page, when I touch the edges I can move it". The underline's rail was 16 % white and its territory 40 % white — an 11 % route on the cutoff emblem was an 8 px grey hair. Now: rail purple 30 %, territory house purple ≥ 6 px. [1] Env 2 → cutoff at 11 %: the underline under the emblem, shown at rest, ≥ 6 px, purple; [2] the same once the filter is on the Patcher; [3] a drag across `.fxy` (the filter's grid, now in CONTROL) plays the grid and leaves the node; a drag from the node's edge moves it. Also in tp47: the eye's cards go back to the centred fit (only env / filter / LFO fill their module), the LFO's SVG viewBox becomes the wave's own box in the eye (non-scaling strokes) so the wave reaches the bottom. `NODE_PATH=Tests/node_modules node Tests/_tp47_marks_gate.js` |
| tp48 — **THE FLOW CARDS ARE OUT OF THE GLOBAL DICE** | Max: "it's still flickering... remove the flow cards out of the global randomization... from now on let me do the flow cards... just keep it oscillators, effects and modulation... lean heavy on the creative uses of each filter". `generate()` forces `B.flow=0` (the chain that is there stays; nothing re-dealt, no per-card dice fired, no LFO aimed at a flow-card knob), the Roll sheet has no Flow cards chip; the filters lean harder (shortlist rarer, filter 2 more often, key/velocity tracking a third of the time, the second LFO on the cutoff 60 %). Each flow card's OWN dice is untouched. Gated by `dice_rules_gate.js` [6] and `_tp39_dicemenu_gate.js` [10]. |
| `au_output_trim.cpp` | **tp51 — THE OUTPUT KNOB IS A REAL MASTER TRIM** (the real installed AU). Max: "the footer Output gain isn't a real MASTER OUTPUT gain, it only outputs a certain part — it's PHONY." MEASURED before the fix: with a Reverb in the rack, moving the knob 0 → -6 dB moved the output **2.43 dB**, with a Delay **2.13 dB** (a bare oscillator moved the honest 6.05). The knob was never bypassed — it sat BEFORE the master limiter, so at any level that reaches the limiter it spent itself buying headroom back instead of turning the instrument down. It is now the LAST stage on every path that reaches the output (the mix, each flow-card capture, each deferred rack capture), and applied nowhere else. After: 6.00 / 5.99 / 6.00 dB. At the default 0 dB it multiplies by exactly 1.0, so the AU null stays bit-identical. Each configuration flushes ~2 s of silence first, or the previous reading's reverb tail inflates the next. `clang++ -O2 -std=c++17 Tests/au_output_trim.cpp -o /tmp/auout -framework AudioToolbox -framework AudioUnit -framework CoreFoundation -framework CoreAudio && /tmp/auout` |
| `_tp54_chop_pass_gate.js` | **tp54 — MAX'S SECOND PASS ON THE CHOP PAGE** (page; the overlay is extracted from PluginEditor.cpp). 🚨 **THIS GATE EXISTS BECAUSE `_tp53_list_gate` DID NOT.** That one asserted the chop panel's STRUCTURE and CSS with the panel forced `.open` and no target — so it never asked the panel to PAINT, its "no page errors" bar was measuring a page where the panel had never run, and it shipped a menu whose every control was dead. tp53's block-replace of the ADSR canvas had swallowed `fmtMs` / `fmtPct` / `fmtPitch` / `fmtStretch`, which sat BETWEEN the two functions it replaced; `ovApplyState` calls `ovRedrawEnvelope` FIRST, so the ReferenceError aborted it before `ovRedrawEmblems` / `ovRedrawFx` / `ovRedrawScan` ever ran. Max: "the right click menu on the chop engine doesn't work, sliders don't work and some of the buttons don't work." **Every bar here drives the thing the way a hand does** — a real sample through `onSampleLoaded`, a real right-click on the waveform, a real pointer drag. 14 bars: [1] every control in the hero strip shares ONE centerline (fb275 — they were `bottom:12px`, BOTTOM-aligned at four different heights, so their middles sat at 295.3 / 296 / 297.5 / 300; equal-height boxes + `align-items:center` now, spread **0**); [2] the Slices pill is the play pills' size and case and 🚨 **CLEARS the library** (MEASURED at **17 px of overlap** before — the library's ‹ was underneath it); [3] the Slices drawer wears `.tpb-panel`'s own glass, a selected pill in it is an outline with nothing filled, its labels lost the colons; [4] nothing blooms on any selected thing; 🚨 **[5] THE PANEL PAINTS** (five ADSR rows each with a value AND a filled bar, three tiles reading) **AND IT DRAGS** (the bar follows the pointer and every step reaches `setSliceAttackMs` — before tp54 the throw aborted `mousedown` before the move listener was attached, so it was click-to-set with no drag); [6] the stretch modes are the WORDS Off/Beats/Tone/Text and the six FX chips are GONE from the DOM; 🚨 **[7] FINE TUNE** — `Slice::pitchOffsetSemis` was always a float and `setSlicePitch` always took -12.0..+12.0; the emblem's `v = Math.round(v)` was throwing the cents away in one line, so this is the half of an existing parameter that was never exposed, not a new one; [8] a SOURCE bar answering Max's question — `writeToStemBuffer` sits in the per-layer render with no `captureEnabled_` above it, the rings arm off a LOADED SAMPLE, and `captureEnabled_` gates a different ring, so **the stem separator does not need DAW capture on** and the panel must not be greyed. ⚠️ tp54 INVERTED four bars in the older gates, each with its reason inline: `_tp53_list_gate` [3]×2 (the FX chips are gone, not live; five ADSR rows, not four) and [6] (the time-mode letter is PURPLE with no box — Max reversed tp53's reading of "transparent white"), and `_tp49_chop_gate` [9] (the library name box 124 → 96 px, because a fixed NAME box never could stop the centred PILL GROUP from growing into it). `NODE_PATH=Tests/node_modules node Tests/_tp54_chop_pass_gate.js` |
| `looptempo_cert.cpp` | **tp57 — WHAT TEMPO IS THIS SAMPLE?** (`Source/LoopTempo.h`, the shipped header — JUCE-free for the Vibrato.h reason). The BPM lock cannot stretch a loop to the host until it knows the loop's OWN tempo, and a .wav does not say. Two readings in order of reliability: the NAME ("Pad 138.wav", "174 dnb break", "Loop 4 Bars 120bpm") and then the LENGTH (a loop is a whole number of bars; the bar count whose implied tempo lands nearest the session wins, compared in OCTAVES so 90 and 180 are one unit either side of 120). 8 bars, including 🚨 [1] **808, 909, 44100, track numbers and key names are NOT tempos** — the bar that stops the feature being a menace in a real sample folder; 🚨 [3] **no reading is 0, and 0 means do not stretch** (a guess dressed as an answer would put every unlabelled one-shot through a time-stretcher); 🚨 [5] the stretch is the right way up — `stretchRatio > 1` means the chop plays LONGER (SignalsmithEngine's own words), so a 138 loop in a 120 session lasts 1.15x; 🚨 [6] **half and double time are FOLDED, not "fixed"** — a 174 break in a 90 session is a double-time break and a musician hears it as 87, so stretching it 1.93x would keep the arithmetic and destroy the part; 🚨 [7] after the stretch, 126 loop/session pairs all sit on the session's grid (or a clean half or double of it) to 1e-12 of an octave. `clang++ -std=c++17 -O2 -I Source Tests/looptempo_cert.cpp -o /tmp/ltcert && /tmp/ltcert` | 8 |
| `json_feed_gate.py` | **tp58 — EVERY FEED THE EDITOR PUSHES INTO THE PAGE MUST BE PARSEABLE.** Max: *"you now broke the distortion paint smh! now there's no white shaper at all, wtf?"* 🚨 **AND A JS GATE COULD NEVER HAVE CAUGHT IT.** `_tp57_curve_gate.js` proves the PAINTER works by assigning `window.__dstVizPush = {…}` as a JavaScript object from the test. The plugin does not do that: PluginEditor writes the literal text `window.__dstVizPush=<getDistortionCurveVizJson()>;` into the page and the webview EVALUATES it — and tp57 appended `,"e":[…]` to that builder **without closing the `"o":[` array it was inside**, so the pushed statement was a SYNTAX ERROR, not bad data. The assignment never ran, `__dstVizPush` stayed undefined, and every distortion card — instance 1 included — fell back to the native poll fb354 had replaced precisely because it dies silently. Seven green JS bars and no curve anywhere, a strictly worse picture than the bug tp57 set out to fix. So this gate reads the C++ that BUILDS the string: it concatenates every literal each builder streams (a `<< expr` carries values, never punctuation; a loop body's literal is a bare comma; a constructor seed opens the object) and balances the brackets. 13 builders across PluginProcessor.cpp and PluginEditor.cpp. ⚠️ It flags only a container that is NEVER CLOSED, never a surplus of closers — a builder with several `return j + "]}"` branches contributes every branch's closers to one flat skeleton, and that says nothing. **CONTROL: re-introduce the missing `]` and it names the function and the line.** `python3 Tests/json_feed_gate.py` | 13 |
| `bpmlock_cert.cpp` | **tp58 — THE BPM LOCK ACTUALLY RETIMES THE AUDIO** (the SHIPPED `WarpProcessor` and the SHIPPED engines, not a model of them). Max: *"the audio in fact does NOT stretch to the bpm whenever I have it on global LOCK. When the drum loop is 117bpm and the global daw bpm is 130, I should press that LOCK, it LOCKS TO THE BPM and plays in THAT TIME."* 🚨 **THE BUG WAS NOT THE STRETCHER — all three warp engines retime correctly.** It was `WarpProcessor::setStretchRatio`, which carries fb204's one-pole glide: right for the SYNTH's Sample engine, which pushes the ratio EVERY BLOCK, and catastrophic for the chop voice, which calls it exactly ONCE at note-on. A lock asking for 0.900 landed on `1.0 + (0.9-1.0)*0.35 = 0.965` and stayed there for the whole note. 6 bars: [0] the note-on setter lands on its ratio first call; [1] the per-block setter STILL glides (fb204 preserved where it belongs); 🚨 [2] a 117 BPM click train locked to 130 comes out at **129.68**; 🚨 **[3] and the glided setter still gets it wrong — 121.13 BPM, the shipped bug kept as a measurement so the fix cannot be quietly reverted into a test that still passes**; [4] a layer already at the session tempo gets ratio 1.0 exactly; [5] the half/double fold keeps all 19881 source/host pairs inside [0.69, 1.45], which is BEATS' good range — the fold is what keeps the engine out of the territory its own header calls "damn near unusable". `./Tests/bpmlock_gate.sh` | 6 |
| `_tp58_center_gate.js` | **tp58 — THE CROSSHAIR TEST: EVERY LETTER SITS ON BOTH OF ITS BOX'S CENTRE LINES.** Max: *"make sure the LETTER IS IN THE MIDDLE OF THE BOX, draw a line thru middle and the top like a horizontal and vertical middle line, and the LETTER NEEDS TO BE IN THE CENTER OF IT, run that test and fix."* Screenshots each letter box at 4x, finds the GLYPH'S INK bounding box, and compares its centre against the box's own centre lines — not the text node's layout rect, because a line box reserves descender room under a capital that has no descender and that gap is exactly the error a layout test cannot see. 🚨 **TWO WAYS THIS MEASUREMENT LIES, AND BOTH BIT THE FIRST CUT: (1) CROPPING** — insetting far enough to miss a border also clips the glyph, and every letter then reports the SAME offset, the crop's centre; four different letters agreeing to three decimals is the tell. The border is excluded by COLOUR (a coloured border has a big channel spread) and the search window is centred on the box, so nothing is cropped. **(2) ASSUMING THE BOX IS IN THE MIDDLE OF THE CLIP** — a screenshot clip takes integer pixels, so the box sits up to half a pixel off-centre inside it, the same size as the error being measured. The threshold is ADAPTIVE (an inactive pad paints at opacity .55; a fixed cut reports "no ink" on the dim ones and the gate then passes or fails on which pad is selected). MEASURED, before: layer A-D dy +0.25..+0.375, stem A-D dy -0.25..-0.375, hero pads dy +0.625..+0.75, all dx -0.125..-0.375. After: every letter within 0.375 px on both axes. ⚠️ **TOLERANCE IS HALF A PIXEL AND THAT IS A MEASUREMENT LIMIT, NOT TASTE** — sub-pixel vertical padding on these boxes is not even monotone (0.15 px of padding-bottom moved a measured ink 1.0 px, and so did 0.6 px); below half a pixel this method fits noise. `node Tests/_tp58_center_gate.js` | 4 |
| `au_mod_pro.cpp` (6) + `mod_source_gate.py` + page gates | **tp96 — THE MOD PAGE IS THE MATRIX (the approved mockup, wired).** Max: *"This is absolutely perfect … wire it in … it needs to look exactly like this … replace the main mod page."* The MOD pill now opens the mockup's matrix sheet (Basic / Pro · Matrix / Routes · search · sort · lock · templates · bake macros), minus the play strip, as a VIEW over the ONE matrix (`assigns` via `window.__mmApi`): a knob drop appears here, a page edit draws its underline, both are the patch. Max's review applied: selected words WHITE, purple with NO glow, INV dead centre (measured: the text range's 0.73 px offset is exactly half the trailing letter-spacing), Source/Destination/Aux headers within 0.06 px of their row words, the routes view draws the PATCHER's own cables (no dotted lines), scrolls by trackpad or drag, and only flips a class when a source goes live. 2 050 destinations in 23 families. **Four new route fields**, absent = the pre-tp96 route bit for bit: `pl` polarity (an OVERRIDE of the source's own — Bi on an envelope turns ownership into a signed swing), `ai` aux invert, `ac` aux bend, `o` output (folded into depth at parse). One definition each in SynthModConfig.h (`modBend`, `auxScale`, `applyPolarity`), used by the global pass, the per-voice pass, the LFO-amount prepasses and the per-sample cutoff path. 🚨 **Found and fixed: the aux ("Scale by") NEVER reached the per-sample cutoff path.** **Measured in the installed AU** (LFO 1 → Osc A Level, held chord): tremolo 13.5 dB vs 3.6 control · Output 25 % → 4.4 dB · Uni lifts the mean level 0.22 → 0.33 · aux velocity 13.1 dB @127 vs 4.5 @25 · INV 3.7 vs 11.0 · aux bend +0.8 at vel 72 → 3.8 dB vs 8.1 straight. ⚠️ The cutoff was the first test target and read NOTHING: filters start OFF and the dry oscillator path remains beside the send — the harness's fault, measured on Osc A Level instead. ⚠️ `_tp42_noise_module_gate.js` [1] is red — and was red on the commit BEFORE this one (checked in a clean worktree). |
| `FlowShaper_test.cpp` [T38] (47) | **tp95 — THE TIME LANE'S "BITCRUSH" ON RAMPS WAS READ-HEAD JITTER (and images on the slow side).** Max: *"time depth at 100% still has that weird bitcrushed sound when there's ramps."* A 5 kHz tone through ramps at 0.25 / 0.5 / 1.5 / 2x, everything NOT at the expected pitch counted as dirt. **Cause 1 — float precision:** the offset is shape × cycle × Range × Depth and the shape came back as a 32-bit float; at a 4-bar cycle its rounding × 384 000 samples ≈ 0.01 sample of RANDOM read-head motion per sample = phase noise: **−47 dB on a 4-bar 2x ramp vs −67 on a 1-beat one** — the cycle length was the tell, and it scales with Depth (his "50 sounds better"). `readShapeD` reads the Time lane's shape in double → −82 dB. **Cause 2 — the slow side was never band-limited:** tp88 guarded rate > 1.02 only; a slowing ramp upsampled through the cubic and its images folded: −49 dB at 0.25x → the same windowed sinc at 1.1 (cutoff ~21.8 kHz) → −107 dB. Every rate at both cycle lengths now ≥ 82 dB (the sinc's own stopband). ⚠️ My own 1-beat numbers first read a flat −66 dB even at 1.0x — an 8192-sample analysis window, not the lane; same 16 384 window → clean. Mutation: float read → T38 62.8 dB (fail); cubic slow side → 49.1 dB (fail). |
| offline DistortionEngine probe (23 types × Drive .25 / .5 / 1) + `dst_morph_depth.cpp` + `dst_curve_gate.js` (7) | **tp94 — THE DRIVE OUTLIERS, FIXED IN THE SHARED ENGINE (the rack Distortion card changes too — Max: "as long as they sound good").** Each fix confined to its own type; the other 18 types measure DIGIT-IDENTICAL before and after. **Transformer** — the output is dB/dt of a saturating core, so a hard-driven core's flips are spikes and nothing bounded them: +14.4 dB, peak 7.3 at full Drive → a soft ceiling in the shaper's OWN units (`4·tanh(y/4)`; a first cut at 1.5 in OUTPUT units clipped from quarter Drive and ran the level BACKWARDS) → +4.7 dB, peak 1.01, still 57 dB of travel. **Rectify** — 48 dB squares any chord, |square| is DC, the blocker erased it: −40.8 dB at full → its drive stops at 12 dB, where the octave lives: −3.6 / −4.8 / −7.3 dB and MORE travel (22.4 dB). **Linear Fold / Sine Fold / West Coast** — ×63 folds into hash the Tone stage eats (−19 / −16 / −25 dB) → ×17: 5–11 dB louder at the top AND as much or more travel (33 / 44 / 48 dB vs 38 / 36 / 44). A ×11 span was louder still but cut the travel — rejected on the Lifeguard law. |
| `ShaperTargets_test.cpp` (32) + `au_shaper_targets.cpp` (the in-plugin sweep) | **tp92 — THE WEAK-TARGET SWEEP ON LANES 0-7, THE TAPE GRID, THE DRIVE OUTLIERS.** Max: *"some of these targets really don't move anything."* Each native target swept 0.03 → 0.97 with a shape that EXERCISES it (a gate for Volume, stairs for Time …), scored on the larger of mid spectrum, side spectrum and 5 ms envelope. The offline file is bit-deterministic (two runs hash identically) — the in-plugin sweep has a 2–17 dB floor from the synth's free-running modulators, so it FINDS candidates and the offline file DECIDES. ⚠️ The in-plugin sweep's first two cuts were wrong: a −60 dBFS envelope floor made gated frames swing tens of dB (Volume Hold "100 dB"), and a window sized in seconds overran the 143 872-sample render so every Goertzel returned 0. **Dead or toothless, fixed:** Volume Attack 0.16 / Release 0.30 dB of envelope (scaled only with Smooth; the top half now opens to +400 ms → 9.4 / 16.6) · Phaser **Drive 0.00 on both built-ins** (never read; now the Filter lane's own tanh drive → 28 / 25) · the Phaser's built-in **Flanger ignored Centre** (0.00 → 23; ×1 at its 0.2 rest) · **Filter Punch** (a BOOL into a follower the lane runs at zero depth; now Volume's 12 ms overshoot on every opening of the cutoff) · 🚨 **Drive Bias was a dead wire on every type** — `DistortionEngine::setBias()` stores a value no family reads since fb319/fb345; the lane now sends Bias/Knee where each FAMILY keeps them (CLIP slot 5 + knee · SHAPER slot 6 + knee · DIODE SIG + slot 5 · ANALOG SIG + Sag · FOLD SIG + Corner): 0.0 → 8–27 dB, and every rest point is what it was. Mutation: two fixes reverted → 2 weak. **Tape grid: CORRECT** — all five machines put the repeats on the tempo grid within ~1 ms at 1 bar … 1/16T once the transport settles (the first reading, −59…+61 ms, was the servo still spinning up: its time constant is ~1 s). **Drive outliers (engine, shared with the rack card, NOT changed here):** Transformer +14.4 dB / 7.3 peak at full drive; Rectify −40.8 dB (a hard-driven chord rectifies to DC and the blocker removes it); Linear/Sine Fold, West Coast −16…−25 dB; Soft Clip ignores Knee once driven. |
| `FlangerSweep_test.cpp` (5) + `au_shaper_fx.cpp` [15] (14) + `au_shaper_level.cpp` (Flanger rows, 0 fail) + `_tp77_gate.js` (35) | **tp91 — THE FLANGER GETS A LANE OF ITS OWN.** Max: *"flanger is a whole different beast than a phaser and it deserves its own dedicated panel … four targets."* Kind 17, a LEND of the rack card's `TerrainFlangerFx` (recycle law — no new flanger), **32 voicings** (Tape Zero · Jet · BBD · Endless × 8 characters, names read from the engine). Like ShaperBox 3's LiquidShaper (*"the LFO-modulated Centre parameter controls the frequency of the first peak"*) **the drawn line IS the sweep**, not a send: `setSweep()` replaces the engine's modulator, glided on its own 15 ms smoother so a drawn gate cannot click. Targets **Feedbk (bipolar, 0.5 = none) · Centre · Range · Stereo** (the right channel narrows, stops, then counter-sweeps). Envelope and Step are NOT offered: their modulator IS the type. **The rack card is untouched: bit-exact null of the engine against its pre-tp91 self, 48 voicings × 102 400 samples.** Measured: Jet · Silver's delay 4.28 → 1.42 → 0.47 ms bottom/middle/top (notches rise 9.1×); Tape Zero |Δ| 4.52 / 0.01 / 4.49 ms — through zero; a 1/16 drawn gate's largest step 1.29× a smooth sweep's; all 32 voicings move ≥ 6 dB; in the AU the comb moves 10.7 dB trough→peak against 0.8 dB of note drift. Mutation: the line ignored → 4 of 5 fail. ⚠️ **Two first cuts of the proof were wrong, not the engine:** a notch-finder skipped Jet's first (ADDITIVE, 1/2Δ = 117 Hz) notch — now it reads the engine's own delay; and a "full null at zero" bar contradicted **fb634 (Max's own law: the crossing is a BREATH, ~8 dB, not a hole)** — now it proves the dip AND that it is not a hole. Level: the 32 voicings measured −5.4 … +2.1 dB, trimmed to 0 fail. Also: the chain loader was bounded by the LANE count while writing the 8-entry `slot[]` — a longer list wrote past it; bounded by `kShaperSlots` now. |
| `au_shaper_level.cpp` (the gate, exit 1 on a fail) + `FlowShaper_test.cpp` [T37] (46) | **tp90 — EVERY TYPE OF EVERY LANE AT UNITY, CALIBRATED ON TWO SOURCES.** The tp89 audit judged one type per lane on one held chord. Per type it found **73 failing lane types**: the Phaser's 28 types from -6.8 to +4.8 dB (the "flanger makes it quiet" complaint was types 1 and 5–17), Tape's five from -4 to -17.6, all eight Chorus types -4 to -10, Crush's roster to -12.5, Bode to -4.5, Reverb Shimmer +4.2, Multiband's types from +0.6 to +10.9. Now **`kShaperTrimDb` in FlowShaper.h**: one trim per type, riding the amount of the effect that is IN (in dB) so a closed shape stays a wire. **0 fail** on the rebuilt AU. ⚠️ **ONE SOURCE LIES:** the same Delay read **-3.07 dB on a sine chord and +2.88 on noise** (a sustained sine and its own echo partly cancel), so every trim is minus the MEAN of the chord and the Noise lane's PINK hiss chained ahead (white put Tape at -24 dB — it is mostly treble). ⚠️ **THE CHAIN IS EIGHT DISTINCT KINDS OR IT IS NOT THE CHAIN:** setting one slot left a repeat, `sanitise()` fell back to kinds 0..7, and the first run read every lane past Crush at exactly +0.00 and the noise at -229 dB. Every take now writes all eight. Also fixed: the mid metric (a constant-power pan read -3.01 dB — now stereo power), and 🚨 **REPEAT LOOPED ONE SLICE FOREVER** when its shape was held up across the loop point — capture was on the rising edge only, so a lane lit in the silence before the first note looped SILENCE (-226 dB); it re-captures on every cycle wrap now. [T37] mutation-checked: the old capture reads -227 dB. NOT JUDGED, by design: Volume, Time, Repeat, Filter (its job is removing energy), Pan's Linear law, Drive (the drive knob). |
| `au_shaper_level.cpp` (the audit) | **tp89 — THE LOUDNESS LAW, AND THE TWO LANES THAT BROKE IT WORST.** Max: *"nothing should be making anything quieter or louder in terms of these effects — unless they're shaped, or the volume knob or the drive knob."* This lights each lane alone, shape pinned OPEN, knobs at rest, and compares rms AND peak against the same render dark. 🚨 **THE PHASER WAS AN UNSCALED DRY+WET SUM**: a phaser is dry PLUS allpass, and summing them at full level approaches 2× where they align — MEASURED **+7.45 dB rms at a 2.47× peak**, which is exactly the clipping Max reported (*"it really distorts and clips"*). The built-in flanger had the same bug (+7.03 dB). Summed at half with a calibrated makeup (`kPhMakeup`, measured — not guessed: unscaled +7.45 → halved +1.43 → ×0.85 lands on 0), the lane now reads **+0.15 dB at a 1.07× peak**. 🚨 **AND THE MULTIBAND LANE WAS BORROWING THE WRONG ENGINE ENTIRELY.** In the rack, "Multiband" IS the OTT (`SYN_OTT`) and "Splitter" (`SYN_SPL`) is the band splitter beside it — two engines, two pools. The lane took the **Splitter**, which has no compressor in it at all: only crossovers and per-band gain/width/pan. That is precisely what Max described — *"a very thinning effect instead of that powerful boost/OTT that a real multiband gives"* — and it measured **−10.43 dB**. It borrows `TerrainOttFx` now, **with the drawn curve driving `amount`, so the shape IS the push**; targets are Speed · Lift · Character · Range. Calibrated in two passes: the shape across the engine's full range read +15.76 dB at a 5.71× peak (unmixable), 0.60× read +9.30 dB at 2.76× (still over full scale), and **0.45× lands at +5.68 dB with a 1.85× peak** — 0.93 absolute, loud and clean. ⚠️ **STILL BREAKING THE LAW, and listed so they are not forgotten:** Tape −11.07 dB · Chorus −6.71 · Bode −3.40 · Delay −3.05 · Filter −2.52 · Widen −1.76 · Drive +1.29, plus Granular lifting the PEAK 1.35× with no rms change (clipping). ⚠️ **TWO CAVEATS ON THIS TABLE.** Pan reads −3.01 dB because a constant-power pan measured as MID is down 3 dB — the metric's fault, not the lane's (and the first cut read it as −89 dB by only looking at the LEFT channel: **a pan is not a mute**). And the source is a held chord from the synth, which [[feedback-a-measurement-is-only-as-repeatable-as-its-source]] says is NOT repeatable — Bode read −0.40 dB on one run and −3.40 on the next. **The large numbers are real; anything under ~3 dB needs the deterministic Noise-lane source before it is acted on.** |
| `FlowShaper_test.cpp` [T36] (45) | **tp88 — DOUBLE SPEED WAS ALIASING, WHICH IS WHY IT SOUNDED BITCRUSHED.** Max: *"double time has a weird bitcrush bug going when things are CHOPPED in the 2x speed … it's bitcrushed and distorted when it shifts up, and it's almost detuned as well."* 🚨 **Reading faster than realtime is a DECIMATION, and a decimation without a band-limit folds** everything above SR/(2·rate) back around Nyquist as inharmonic partials — which is exactly what a bitcrusher is, and why it reads as "detuned". MEASURED BEFORE: a 13 / 15 / 17 kHz tone read at 2× **vanished from 2f and came back at |SR−2f| at FULL amplitude** (0.250 of 0.250), while the same tones at 1× left those bins empty. ⚠️ **He diagnosed it by ear before the maths did** — *"I turned the depth down to 50, it sounds WAY better, 100 sounds off"* — and that is the same fact: **Depth scales the offset, so it scales the RATE**; a full rise is 2× at depth 100 and 1.5× at 50, and half the speed-up is a quarter of the folding. THE CURE HAS TO BE APPLIED WHERE THE RESAMPLING HAPPENS, so `rd` stays the Catmull-Rom (correct at rate ≤ 1) and **`rdBL`** takes over above it: a Blackman-windowed sinc with cutoff 1/ratio, the textbook decimation filter. One table, built once in `prepare()` — the kernel is `K(u) = sinc(u)·w(u)` over a fixed `u`, and a tap at source distance `t` reads `K(t/ratio)`, so widening the kernel and lowering the cutoff are the same act and **no `sin()` is called per sample**. Taps whose own distance-behind would reach the unwritten future, or past what the ring holds, are dropped and the rest renormalised. The rate comes from the read itself — `1 − d(behind)/dsample`, smoothed, with a jump ignored because **a jump is not a rate** (the jump law owns those); for the **Repeat** lane, which pitches its slice by `2^(semis/12)`, the ratio is known outright and needed no estimate. AFTER, at 2×: 15 kHz folds to **0.000043** and 17 kHz to **0.000005** (from 0.250), 11 kHz passes with **0.66 dB** of droop, and the rate itself is untouched — T31's 2.000× and 3.000× still read exact. **CPU 1.98% of one core at 2×, 0.31% at 1×.** Mutation-checked: force the cubic path and T36 goes red at the full-scale fold. |
| `FlowShaper_test.cpp` [T35] (44) | **tp87 — THE DROPOUT WAS THE LANE REPLAYING THE SILENCE BEFORE THE FIRST NOTE.** Max, on his own 16-tread shape at 2 bars: *"it's supposed to either shift pitches or stutter, right now it's just doing a bunch of DROPOUT CLICKS."* Reproduced and measured: with the plugin open and quiet (a ring **full of silence**) and a phrase starting, the lane reached BACK PAST THE FIRST NOTE — **2.5 s of silence, in pieces, inside the first three seconds** — and in the installed plugin, 81 of 4666 windows up to **28 dB down**, against a DRY control of **0**. 🚨 **tp86's live-fallback could not catch it: `ringFilled_` counts samples WRITTEN, and an instrument that has been sitting open has written nothing but silence.** `soundAge_` is the age of the oldest sample belonging to the sound that is playing; the offset is held inside it and falls back to the present past it, so a phrase chops from its first note and fills in as it gains history. ⚠️ A short gap is part of the music — a drum loop is mostly gaps — so only a silence run longer than **a second** counts as "the sound has not started". AFTER, in the plugin: **0 of 4666**, and the level per second is flat at −13 dBFS for twenty seconds, matching dry exactly. Mutation-checked (ignore `soundAge_` and T35 goes red with the 2003 ms hole). 🚨 **AND THE PROCESS BUG THAT MADE IT WORSE: A FILTERED BUILD LOG HID A FAILING BUILD.** `cmake --build … | grep -E "error|Built target"` matches none of make's own words (`No rule to make target …`, `Error 2`), the pipe's exit status comes from `grep`, and the post-install `ls` shows the **copy** time. Deleting the WebUI binarydata dir left cmake with no rule to rebuild it, two "builds" did nothing, and the install copied a **stale artefact** — so tp86 was reported installed and green while Max was auditioning a binary from before the change, hearing the OLD model freeze on a flat line. **Check the ARTEFACT's mtime against the source you just edited, let the build's own tail print, and prove the new code runs with a measurement that could only pass on it — byte-identical output across a real DSP change means nothing was rebuilt.** |
| `FlowShaper_test.cpp` [T23] [T24] [T28–T34] (43) + `_tp77_gate.js` [39] (35) | **tp86 — TIME IS SHAPERBOX'S TIMESHAPER NOW: THE SHAPE IS THE OFFSET, AND NOTHING DROPS OUT.** Max sent the Cableguys TimeShaper tutorial with *"we need our time to act like this"*. The model it states on screen: the vertical is TIME OFFSET, the top line is the present, *"a horizontal line gives 100% playback speed"*, *"when your LFO matches the angle of this gray guideline you get 0% playback speed — the audio is stopped"*, steeper is reverse, a line angled up plays faster. That is **rate = 1 + slope × Range**, and it replaces the absolute-position reading the lane had. 🚨 **THE OLD MODEL RE-ANCHORED THE READ HEAD TO *NOW* AT EVERY CYCLE BOUNDARY** — `behind = nowBeats − y·cyc·Range`, and `nowBeats` is zero there, so `behind` could only come out ≤ 0 and was clamped to the write head. Two capabilities fell out of that one line, both measured: **a constant delay decayed into a freeze** (the best drawable "play normally, one beat late" averaged **0.755×** instead of 1.000×, because the first beat of every cycle was frozen) and **the average speed over a cycle could never exceed 1.000×** however the shape was drawn, so the tutorial's sustained 200% riser was not expressible at all. AFTER: flat at the top **1.000×** with no lag · flat at half **1.000×** held a constant half-cycle behind · a full rise **2.000×** sustained (and **3.000×** at Range ×2) · falling at the guideline **0.000×** · half a bar flat then falling twice as steep **+1.000×** then **−1.000×**. 🚨 **AND THE DROPOUT MAX HATES, FOUND AND FIXED.** *"I press play and it drops out and then it comes in two bars later — I hate that shit, ShaperBox doesn't do that, it's very reactive."* He was right. An offset the ring could not satisfy was clamped to `maxB = ringFilled − 4`, which pins the read to the **oldest sample in the ring — and the oldest sample does not move**, so the read stood still while the write head ran away from it: a whole bar of frozen near-DC. MEASURED: a constant half-bar offset rendered **−24.8 dBFS for exactly one bar** and then snapped to −9.0. It falls back to the **PRESENT** now, so every shape holds full level from the FIRST sixteenth (**T34**, mutation-checked by restoring the edge clamp). ⚠️ **MIGRATION:** the blob carries `tv`; absent or < 2 means the old reading and the Time lane's points are sheared once — `y' = y + 1 − p/Range` — on both sides (the page, and `PluginProcessor.cpp` for the case where no editor is ever opened), then stamped `tv:2`. The old default unity ramp at the default Range lands on a flat line at the top, which is exactly the new neutral. Past 1 the old lane was already pinned at the head, so clipping there reproduces it; a CURVED segment and a Range far from ×1 are approximate. **Every Time preset was redrawn** — Unity is a flat line, Halftime a −½ slope, a stutter a DESCENDING STAIRCASE (flat treads, jumping risers), Reverse half a bar flat then falling twice as steep, Tape stop flat bending down onto the guideline. ⚠️ **AND THE EDITOR DRAWS THE GUIDELINE** (`_tp77_gate.js` [39]): the dashed stop gradient, which falls 1/Range across the cycle and so SHALLOWS as Range doubles, plus the cross-hatch below it for the buffer it cannot reach yet. Time only — on every other lane the vertical is not time. ⚠️ **THE MEASUREMENT LESSON: STEADY STATE IS NOT ONSET.** The first cut of T28 read the lag at six seconds and reported "no lag" on a lane that dropped out for a whole bar every time you pressed play. Max heard it; the bar did not, because it never looked at the first bar. |
| `au_shaper_cert2.cpp` (24) | **tp85 — THE CERTIFICATION BATTERY'S SECOND HALF: TARGETS, TYPES, SEAMS, FEEDBACK.** tp83 certified that each borrowed lane is AUDIBLE and RHYTHMIC. What it could not reach was what the lane is MADE of, because the four target knobs are not parameters — they live in the `shaperJson` blob that only the interface writes. 🔑 **THE UNLOCK IS THE tp83 LESSON A SECOND TIME: THE BLOB WAS ALREADY REACHABLE, AS A PRESET.** `getStateInformation` writes `shaperJson0..3` into the plugin state and a host sets that state through `kAudioUnitProperty_ClassInfo`, so this harness does what a DAW does when it loads a patch — read the state, put a lane in it, hand it back — and every `k[]` became measurable with the REAL engines running, with **no test-only door added to the plugin**. Bars: the four targets of all nine borrowed lanes swept end to end; every entry of every type menu compared with every other; a hard gate proving the send opens and shuts without a seam; Delay and Bode held at maximum feedback for twenty-four seconds. 🚨 **WHAT IT FOUND — A SHIPPED DATA LOSS AND THREE INERT KNOBS.** (1) tp84 inserted the `shpNoiseSel` loop between an `if` and its `else`, so the `else` re-bound to the loop's INNER `if`: any Shaper without a noise sample — the normal case — ran `removeProperty("noiseSampleSel")` and **wiped the SYNTH's noise selection out of the patch being saved.** Bars [2] and [3] are that round trip. (2) A reverb setter only STORES its value; it reaches the DSP when `updateCoefficients()` rebuilds the tank, which the rack's own path calls and this branch did not — so **Size and Decay moved nothing at all** and Diffusion was half-applied (only `tone` is read straight in `processSample`). MEASURED: sweeping Decay end to end changed the tail's length by **0.3 dB**; committed on CHANGE (never per sample — it is a coefficient rebuild inside the sample loop) the same sweep reads **38.7 dB**, and Tone goes 16.1 → 40.7 dB. ⚠️ **A PATCH WITH THE REVERB LANE LIT WILL SOUND DIFFERENT**: Size and Decay were pinned at the engine's construction defaults and now do what the knobs say. ⚠️ **AND IT RE-LEARNED tp83's LAW FOUR MORE TIMES — plus one the project had not written down.** A click is not a slew rate (Noise and Bode failed a max-jump bar while perfectly clean: a WIDER signal jumps more every sample — peak-over-percentile is the dimensionless measure); a reverb's Decay is tail LENGTH and its Diffusion is tail SMOOTHNESS, not a spectrum; Widen's one claim is the IMAGE, which a mono spectrum cannot see; a tape TRANSPORT is a pitch WANDER, which neither harmonics nor spectrum can see (types 0/4 and 1/3 share a machine). 🚨 **AND THE NEW ONE: THE SOURCE WAS THE NOISE IN THE MEASUREMENT.** A held note on the current patch is not repeatable — the synth's modulators free-run — so every small number moved between runs (Reverb Tone read 15.1 dB, then 0.9, then 1.8). Driven by the **NOISE lane** instead, which tp82's null proves deterministic bit-for-bit, Multiband went from 0.2–9.3 dB and unstable to 16.8–63.6 dB and repeatable. **A MEASUREMENT IS ONLY AS REPEATABLE AS ITS SOURCE.** A second harness law alongside it: **ARM, THEN WAIT** — the roster builds a lent engine on demand and passes audio through until it is built, so a measurement taken across that boundary compares no-reverb with reverb. |
| `FlowShaper_test.cpp` [T28–T32] (41) | **tp85 — TIME AGAINST SHAPERBOX 3 / GROSS BEAT, BEYOND THE PITCH LAW** (the last item owed from tp79). T23 proved the one law Max named — the shape's SLOPE is the playback rate — but that is the headline, not the contract. Measured on T23's own instrument (**the input's VALUE IS ITS OWN SAMPLE INDEX**, so the output says which sample was read and no estimator can be fooled): **T28** a FLAT shape FREEZES (rate 0.000) and the top line plays NOW (rate 1.000, lag 0 samples); **T29** a FALLING shape plays BACKWARDS at exactly the slope drawn (−1.000 and −0.500); **T30** RANGE is the interval — the same half-slope at Range 0, ¼ and ½ reads 0.125 / 0.250 / 0.500, the knob's own 4^((k−½)·2) law, an octave a quarter turn; **T31** THE FUTURE CANNOT BE READ, and that is WHY the rise is drawn the way Gross Beat draws it — a ramp through the origin at Range ×2 pins to now (1.000) because it is asking for samples that do not exist, while FREEZE-then-catch-up runs at exactly ×2 and ×4; **T32** FADE closes the seam at the cycle wrap (largest read jump 1001 → 546 → 201 samples as Fade goes 0 → 0.3 → 1). All four mutation-checked (removing the past-only floor turns three red; making Fade inert turns T32 red). ⚠️ **TWO MEASUREMENT TRAPS THIS COST.** (1) Reading the rate too early reads UNITY whatever the shape says — the ring has not filled and every request clamps to the head. (2) **WHERE IN THE CYCLE YOU MEASURE IS PART OF THE MEASUREMENT**: a falling shape starts AT the present, so for the first part of its cycle it is asking for the future and reads +1.000; `1−x` only clears the present at p ≥ ½ and `1−½x` at p ≥ ⅔. Measuring at p = 0.15 "proved" reverse was broken when it was not. 📘 **AND THEN AGAINST THE ACTUAL MANUAL** (`Cableguys-ShaperBox-3-Manual.pdf`, TimeShaper 3, pp. 10–11). Every capability it lists is present and now proven: click-free stutters (T32), half-speed (T30), real-time reverse (T29), tempo-locked scratches (T23), tape stops (T28 — a stopped playhead IS the tape stop). 🚨 **TWO REAL DIFFERENCES, BOTH FOR MAX TO DECIDE, NEITHER A DSP DEFECT.** (1) **THE AXIS CONVENTION IS SHEARED BY ONE DIAGONAL.** ShaperBox's Y is TIME OFFSET (how far back), with a grey "buffer start" reference line running top-left to bottom-right; the manual's rule is *"when the LFO waveform is at the same angle as the reference line, playback will be stopped… steepen the angle and playback goes into reverse."* So in ShaperBox a FLAT line plays NORMALLY and the REFERENCE DIAGONAL is the freeze. In Terrain the Y is the absolute READ POSITION: a RISING UNITY RAMP plays normally and a FLAT line is the freeze. Same capability set, mirrored drawing — **so ShaperBox's factory Time shapes cannot be transcribed one-to-one, and the neutral shape differs.** The cheap fix if he wants it is to DRAW the reference diagonal in the Time lane's editor, not to shear the axis (which would change the meaning of every saved shape). (2) **THERE IS NO FINE (ms) TIME RANGE.** ShaperBox sets Time Range independently of LFO length — Linked, or Beat (1/2/4/8 bars), or **Fine (2.5/10/20 ms)**, the last explicitly *"to create realistic tape/vinyl-style pitch 'wow', vibrato, chorus/flanger"*. Terrain's Range is a MULTIPLIER on the cycle: `mode × 4^((k₂−½)·2)` = ×⅛ … ×8. At a one-bar cycle the smallest reachable offset is **250 ms**, about a hundred times too coarse, and the only way to reach ~15 ms is to also run the shape at a 125 ms cycle. **Four of the eight uses the manual lists for TimeShaper — wow, vibrato, chorus, flanger — are therefore out of reach.** |
| ✅ `_fb606_wtnav_gate.js` (26/26) · `_tp35_wtstep_gate.js` (6) · `_tp36_dicecap_gate.js` (10) · `_tp37_lfoglobal_gate.js` (10) · `_tp40_patcher_parity_gate.js` (5) | **tp85 — THE RED ONE WAS THE GATE, NOT THE PAGE.** The tp84 clean-up listed every gate file against this README, found five no row referenced, and one of them — `_fb606_wtnav_gate.js` — was red 5/26 (identical failures at `775d213`, so pre-existing). Root-caused at tp85 by measuring instead of assuming: a wrapper round the real opener proved `__tpbFolderMenu` **was** reached and **did** build the right menu — `Locate Folder` / `Remove Folder`, 176×76 at the click point, `elementFromPoint` returning the menu at every row. 🚨 **The gate was reading the wrong element.** `__menu()` did `querySelector('.pmenu')`, and the page has since grown FIVE `.pmenu` singletons (`tp-q`, `tp-ctx`, `tp-sug`, `tp-tip`); the folder menu is a SIXTH, created fresh at body level, so the first in DOM order was the CLOSED quick-menu host — an empty `0×0` box that took five bars down with it. Two rots, one refactor: the gate also called the bare `window.__tpbMenuClose`, but the opener registers its closer under the INSTANCE-mangled name (`__tpbMenuClose2`), so every between-bar close was a silent no-op, and `FB606_MUT=synmenu`'s anchor no longer matched the `window[G('__tpbFolderMenu')]` it had become — **a mutation that misses its anchor is a mutation that never ran.** Now: `__menu()` picks the menu that is actually OPEN and carries `openCount` so a second one cannot hide in a green bar, `__closeMenus()` closes every `__tpbMenuClose*` variant, the anchor is repaired, and all three mutations (`synmenu` 6 red · `flat` 7 red · `empty` 1 red) turn it red while HEAD is 26/26. ⚠️ **THE LAW: WHEN A UI GATE GOES RED, PROVE WHICH SIDE ROTTED BEFORE FIXING EITHER** — a singleton selector is only unique until someone adds the second one, and the failure looks exactly like a broken feature. |
| `_tp77_gate.js` [38] (34) | **tp84 — THE NOISE LANE BROWSES THE WHOLE LIBRARY.** Max: *"noise needs to have the browser of all 200+ sounds we have."* 🔑 **Nothing about the library was rebuilt.** Every noise-sample native already carried a trailing INSTANCE — tp43 added it so Noise 2 could share Noise 1's library — so the Shaper's lane reaches the same factory categories, imported files and dropped folders by passing its own instance number. **Instances 3.. are the Shapers** (`kShpNoiseBase`), one `tw::SampleBuffer` per card, and widening `getNoiseSampleBuffer` / `setNoiseSampleSel` / `getNoiseSampleSel` by that range carried the entire load path with it: `loadNoiseFactory`, `loadNoiseByPath`, `clearNoiseSample` and the persisted selection all just work. **When a sample is loaded it plays INSTEAD of the algorithmic colour**, looping, with **Scan as its playback rate** — which is what puts a vinyl crackle or a field recording on the grid. Tone, Width and Drive still shape it, and the drawn curve is still its level. ⚠️ **The shared_ptr is swapped ONCE A BLOCK** (`ShaperRoster::refreshNoise`, called where the slots are pushed), never per sample: the audio thread only ever reads raw pointers it already holds. That is the same discipline the voice's own sample source uses and the reason a `load()` per sample would have been wrong. **The chip:** the lane's type chip is a browser chip now, like the Filter's, with the thirteen colours as its first category and the library after it; it says the loaded sample's NAME when there is one and its colour when there is not, and the card asks `getNoiseSampleSel` on open so the name survives a reopen. The selection persists per card in the plugin state (`shpNoiseSel<n>`). Bar: **[38]** the chip is a browser chip with its native dropdown hidden, it opens the two-pane browser, and the thirteen colours are in it — driven by placing Noise in a chain position first, since it is not one of the default eight. |
| `au_shaper_fx.cpp` (13) + `_tp77_gate.js` [36] (33) | **tp83 — THE CHAIN BECOMES PARAMETERS, AND THE NINE BORROWED EFFECTS ARE SOUND-CERTIFIED.** Max: *"how do we sound certify it?" … "LETS GO THEN."* **The unblock:** which kind occupies each position lived only in the shaperJson blob, so the host could not automate the order and — the reason this mattered — no harness could PLACE a kind, which left every borrowed effect uncertifiable by measurement. Eight choice parameters now (`FLOW_CHOP_SLOT1..8`, each listing all seventeen kinds), pushed to the DSP every block like the lane controls, mirrored into the card's state like every other parameter, and cloned to instances 2..4 automatically by the `FLOW_CHOP_` tap. **What is certified, and why in this form:** each lane's output against the same render with the lane dark, measured as a **magnitude-spectrum change** over a log-spaced Goertzel bank — magnitude, not samples, because a phase-only change gives an enormous sample difference and is inaudible (the fb283 trap: Plate Dispersion measured "102 % divergence" and Max heard nothing). Then the same measurement taken where the shape sits in its **trough** against its **peak**, because an effect that is merely always-on is not a Shaper lane. 🚨 **THE BATTERY FOUND ITS OWN BLIND SPOT, WHICH IS THE INTERESTING PART.** Multiband and Tape both failed the generic bar (0.1 dB and −1.1 dB of shape tracking) — and neither was broken. A band TILT is invisible to a magnitude metric, because tilted one way and tilted the other are both "changed" by about the same amount; and Tape's claim is the harmonics it ADDS, not the level it sits at. Measured by their own signature they track plainly: **Multiband 1.90× high-against-low from trough to peak, Tape 0.034 → 0.235 harmonics-against-fundamental, seven times the colour.** The lesson is the one fb283 already taught in the other direction — **the metric has to be the one the claim is about**, or it will fail working code and pass broken code with equal confidence. **The three with a signature of their own get it named:** Bode moves 19.9 dB of a held C4's energy OFF the note's own frequency · Widen raises side-against-mid from 0.0000 to 0.3984 · **Noise makes sound out of silence** (−240 → −15.3 dBFS with no note held), the only lane that adds signal rather than processing it. ⚠️ **WHAT IT DOES NOT CERTIFY: taste.** It says each lane is audible, rhythmic and doing its own job. Whether a Plate at Size 0.45 sounds GOOD is Max's ear, and this is the point that hands it to him. Still to add: every target swept end to end, every type proven distinct from the others, a click check on a send opening, and Delay / Bode held at full feedback for stability. |
| `TerrainNoise_test.cpp` (15) + `FlowShaper_test.cpp` [T25c] (36) + `_tp77_gate.js` (33) + `au_shaper_lock.cpp` (9) | **tp82 — THE NOISE LANE, AND THE NULL THAT MADE IT SAFE.** Max: *"it should just be like ShaperBox's noise engine, nothing too complicated … we already have a whole bunch of noises … imagine the stuff we can break beat with."* **This one was not a lend.** Every other effect the Shaper borrows is a finished, self-contained rack engine; the noise generator lived INSIDE `SynthVoice`'s render — three functions and eleven lines of state, thirteen colours, sitting in the middle of the instrument. Bolting a second noise engine on beside it was the wrong answer, so it was **lifted into `TerrainNoise.h`, which the synth and the Shaper now share**. 🚨 **THE CODE WAS MOVED, NOT REWRITTEN, AND THAT IS PROVEN RATHER THAN CLAIMED.** The extraction was done by capturing the body and asserting it byte-equal to the lines it came from (the only edit: `juce::jlimit` → `tnClamp`, so the header needs no JUCE). `TerrainNoise_test.cpp` then holds a **FROZEN copy of the generator as it stood before the move** and runs it in lockstep with the shared one for two seconds a colour, demanding they agree **bit for bit** — `memcmp` on the floats, not an epsilon. Thirteen colours, thirteen nulls. It also checks each colour actually makes sound, so a silent one cannot pass a null trivially, and that `reset()` returns a used generator to a fresh one's exact state (a second owner sounds like the first). ⚠️ **If a future edit to `TerrainNoise.h` reds this file, that is the point:** either the change was meant to alter the instrument's noise, and Max has to hear it first, or it was not, and the change is wrong. **The lane:** the shape is the noise's LEVEL — the only kind that ADDS signal instead of processing it, which is exactly what makes a drawn gate a hi-hat, a ramp a riser and a vinyl tick land on the offbeat. Four targets: Tone (one-pole, 200 Hz .. 20 kHz) · Width (mono to twice the sides the generator already decorrelated) · Scan (the generator sampled, held and interpolated, so it turns grainy and pitched-down) · Drive (saturation, so it thickens and cuts instead of just getting louder). Thirteen colours, index-aligned with `SYN_NOISE_TYPE` — the lane hears exactly what the synth's noise module hears. It boots on a GATE, not a sine, because rhythmic noise is what it is for. ⚠️ It needs **no lazy arm and no atomic**: unlike every other lent engine it carries no ring at all, so it simply is. `noiseLpL_` / `noiseLpR_` went with the move — declared in the voice, used nowhere. Bars: **[T25c]** Bode and Noise reach `fx()` with the shape as their value (eight chain positions cannot hold nine lent kinds at once, so those two get their own placement). |
| `FlowShaper_test.cpp` [T27] (35) + `_tp77_gate.js` [4] [9] (33) + `au_shaper_lock.cpp` (9) | **tp81 — THE ROSTER CLOSES BUT ONE, AND EVERY LANE HAS FOUR TARGETS.** Max: *"still owed: Tape, Granular, Bode and Noise … I want everyone to have four targets, it's not three, not two, exactly four … so that means four parameters night and day."* **Three more lends** (Tape · Granular · Bode) through the same `ShaperExt::fx` door — sixteen kinds in eight chain positions. Type lists for Granular and Bode are the rack's own at PluginProcessor.cpp:6596 / :7096 (⚠️ neither engine exposes `typeNames()`, so those two lists are COPIES and must move together with the page's `LNMODE`). 🔑 **The shape and the blend now arrive APART** (`fx(kind, s, k, mode, blend, …)`, was a pre-multiplied `mix`). Pre-multiplying quietly forced every kind to spend its shape on wet amount — right for a reverb or a delay, where the shape IS the send, and wrong for **Bode**, where the shape wants to be the INTERVAL while the wet level stays put. That is the one kind that proves the split was needed. **FOUR TARGETS ON ALL SIXTEEN.** The fourth on each original lane: Volume **Hold** · Time **Tone** · Filter **Punch** · Pan **Tilt** · Repeat **Tone** · Drive **Knee** · Phaser **Centre** · Crush **Stereo**. ⚠️ Filter's Punch and Drive's Knee are controls the rack ENGINES ALWAYS HAD and the lane had simply never reached — `ShaperExt::filter`/`drive` each gained the argument rather than anything being written. The other six are new DSP here. 🚨 **A DEFAULT IS BEHAVIOUR.** Hold booted at the table's existing 0.5, which held the gate open, and **every Volume bar in the offline proof went red at once** — the knob was new but the LANE was not. Every fourth target now boots neutral: Hold 0, Punch 0, Stereo 0, Centre 0.2 (the 200 Hz the phaser always swept from — its mapping was re-fitted so the default lands exactly there), Tone/Tilt 0.5. ⚠️ **Crush had no width at all**: both channels shared one hold counter. Stereo gives the right channel its own, up to twice as long; at 0 the counters are the same number and it is bit-for-bit the old mono grit. Bars: **[T27]** 🚨 the six NEW fourth targets are each AUDIBLE — the lane is lit alone, the knob swept end to end, and the outputs compared (⚠️ **over BOTH channels**: a left-only reading called Crush Stereo, the one knob explicitly about the image, inaudible) · **[T27b]** Filter Punch and Drive Knee ARRIVE at their engine with the value set · page [9] re-aimed from three knobs to four on every lane, [4] to sixteen lanes. ⚠️ **NOISE IS THE ONE STILL OWED, AND IT IS NOT A LEND.** Every effect above is a self-contained engine class; the noise generator lives INSIDE `SynthVoice`'s render (a switch on `noiseType_`, thirteen colours), so a Noise lane needs it lifted into its own header used by both — surgery on the instrument's core, and it wants a bit-identical null on the synth before it ships. |
| `FlowShaper_test.cpp` [T25] [T26] (33) + `_tp77_gate.js` (33) + `au_shaper_lock.cpp` (9) | **tp80 — THE ROSTER: FIVE RACK EFFECTS BECOME SHAPER LANES.** Max: *"we're going to make each of these effects available to shape … reverb, tape, widen, multiband, granular, delay, bode, chorus … and a noise engine too, we already have our noise sample library … I want everyone to have exactly four targets … I want to be able to chain these."* **Kinds and positions stop being the same number:** eight chain POSITIONS (`kShaperSlots`), thirteen KINDS (`kShaperLanes`), so the chain is eight distinct kinds out of the roster rather than a permutation of all of them. Picking a kind already down SWAPS the two positions; picking one that is NOT down REPLACES what is there — which is how a Reverb gets into the chain at all. **One door, not nine virtuals:** `ShaperExt::fx (kind, s, k, mode, mix, l, r)`. The lane hands over the shape's value, its four target knobs, its type and its mix, and the PROCESSOR — where the engines live — decides what to drive. That keeps FlowShaper.h free of every engine's Params struct (the offline proof still builds with no JUCE) and makes a new kind eight lines instead of a block. **Nothing is re-implemented:** Reverb (Room/Plate/Hall/Shimmer), Delay, Chorus, Widen and Multiband are the SHIPPED rack engines, a second private instance each in `ShaperRoster`, armed lazily exactly as Filter and Drive already are — a Delay is ~8.4 MB of ring at 48 k, so a card that never lights one never allocates it. Type lists come from the engines' own `typeNames()`, never retyped, so a menu name cannot drift from the roster (the splitter's three "Reserved" slots are trimmed before they reach a menu). 🚨 **THE BUG THIS BATCH FOUND, AND IT IS THE HEADLINE:** `setLaneCtl` indexed `ctl_[lane & 7]`. The mask was sized to the eight lanes that existed when it was written, so the moment the roster grew, lanes 8..12 wrapped straight onto 0..4 and pushed their own OFF state over Volume, Time, Filter, Pan and Repeat. **The entire Shaper went inert in the real plugin while every parameter still read correctly** — the AU cert dropped to 5/9 with the dry signal passing through untouched. A mask is not a bounds check: it aliases silently instead of rejecting. Same shape as `ShaperRoster::filter`'s `fltLive[which & 3]`, fixed one batch earlier. Bars: **[T25]** all five lent kinds reach `fx()` carrying the shape, the four knobs, the type and the mix, a lit NATIVE lane never uses that door, and an UNARMED engine is a wire rather than a mute · **[T26]** 🚨 pushing all thirteen lanes the way the processor does leaves the one lit lane gating (mutation-checked: restoring `& 7` reds it) · the page gate's [35]/[36] re-aimed to eight positions out of thirteen kinds, and [36]'s out-of-range probe moved from 9 (now Delay) to 99. ⚠️ **NOT YET CERTIFIED:** that each of the five SOUNDS right in the plugin. The AU cert cannot place a kind, because the chain lives in the shaperJson blob rather than in parameters — making the eight positions eight choice parameters is the next step, and it buys host automation of the chain as well. Tape, Granular, Bode and Noise are still owed; their engines all ship, so each is one more lend. |
| `_tp77_gate.js` [35] [36] [37] (33) + `FlowShaper_test.cpp` [T21] [T22] [T23] [T24] (30) + `au_shaper_lock.cpp` (9, re-aimed) | **tp79 — THE SHAPER IS A CHAIN YOU ARRANGE, AND TIME'S PITCH LAW IS BACK.** Max: *"these are in a chain obviously, so these can actually be per-routable … we right click on the buttons … I right click on Time, boom, Multiband … I want to put Bode second, I want to put Pan third … please stop starting off with the volume on … change the shape to a triangle or a sine, that gate looks very ugly … where it says Phaser and Cycle, that's purple on purple, can the letters be white? … our time is broken, I want ours to sound exactly like ShaperBox 3 — every time my grid goes somewhere, the fucking pitch moves."* **THE CHAIN:** the eight lane blocks were a FIXED sequence (Time · Repeat · Drive · Crush · Filter · Phaser · Pan · Volume) that nothing on screen showed. They are lifted into lambdas and dispatched in `slot[]` order, so the tile row left-to-right IS the signal chain; right-click a tile to place a lane there. A kind cannot appear twice, so every pick is a SWAP — which is why the views stay indexed by KIND and the reorder needs **no state surgery at all** (one set of parameters, one set of DSP state, one clock per lane, all where they were). The order rides the `shaperJson` blob with the breakpoints, so **no parameter was added and no saved patch lost one**. `sanitise()` is the wall in front of a switch the audio thread indexes: anything that is not a permutation of 0..7 falls back to the identity. 🚨 **TIME'S GLIDE WAS DESTROYING THE PITCH LAW.** The shape is the read position, so its SLOPE is the playback rate and playback rate is pitch — that is the Gross Beat / TimeShaper law the lane is built on. Glide slew-limited the read position's TOTAL motion, which clamped the pitch itself: **measured, at Glide 0.7 the playback rate sat at 0.818 for every shape** — slopes 0.75, 0.5 and 0.25 all came out identical and the octave never arrived. Now the target's own velocity passes through untouched (the pitch) and only the ERROR a discontinuity leaves behind is slew-limited (the slide). **Measured after: 1.000 / 0.750 / 0.500 / 0.250, with Glide off AND on.** 🚨 **THE RING READ IS CATMULL-ROM, NOT A STRAIGHT LINE.** Every read at a slope other than 1 is a resample, and 2-point linear is a low-pass whose corner moves with the fractional position — a pitched read came back dull and grainy. ⚠️ A cubic reads TWO samples AHEAD, and ahead of the write head the ring holds silence or a 16-second-old sample: a read sitting ON the head (what a unity shape does) interpolated towards stale memory and **unity measured 0.964 instead of 1.000**. Within two samples of the head it falls back to linear, which at the head is the written sample itself — unity stays bit-identical. ⚠️ **A REGRESSION THIS BATCH MADE AND CAUGHT:** lifting the blocks swallowed the ring's write-head advance into the Repeat lambda. It still ran every sample (the dispatch calls every lambda), so nothing went silent — but the head then moved at whatever POSITION Repeat happened to occupy, which is wrong the moment the chain is reorderable. It belongs to the sample, not to a lane, and now sits after the dispatch. **Also:** nothing is lit at boot and the default shape is a sine (the `FLOW_CHOP_*_ON` default and `rebuildShaperState`'s seed both moved; the seed's own "sine" was still tp77's three-point SPIKE and is now the page's `SINE9` verbatim). Box ribbons are white on their purple chip. `ShaperRoster::filter` bounds-checks `which` (it masked `& 3` against a 3-element array — unreachable today, one slot away from not being). Bars: **[35]** a tile right-click swaps two positions, every other holds, and the new order reaches the processor in `setShaperJson`'s `slot` (still a permutation) · **[36]** the chain rides the card's state and a duplicate / out-of-range / empty list all read as the tile order · **[37]** a fresh card lights nothing, opens on a sine, ribbons white · **[T21]** chain order is AUDIBLE (gate→crush against crush→gate differ by 0.12 peak; ⚠️ measured at k0 = 0 they differ by 0.0005, which proves nothing — the bar uses the COARSEST bit depth) · **[T22]** sanitise rejects a duplicate, an out-of-range and a negative · **[T23]** 🚨 the playback rate IS the slope at 1 / 0.75 / 0.5 / 0.25 with Glide off and on, measured by feeding a ramp whose VALUE IS ITS OWN SAMPLE INDEX so the output says exactly which input sample was read (no estimator to fool — an autocorrelation here locks onto the slow read sweep, not the tone) · **[T24]** the ring advances for TIME ALONE. T21/T22/T23/T24 all mutation-checked. The AU cert leaned on the old defaults (Volume lit, a 1/16 gate) without setting either — it lights the lane explicitly now and its four transport bars read the sine. |
| `_tp61_gate.js` [8e] [8f] (16) | **tp78 — THE CHOP MENU'S SELECTOR SAYS THE NUMBER, NOT "ALL".** Max: *"whenever we right click one of our slices … instead of it saying ALL in the selector, can it just be the number of slices? Same number, same style, same everything. No glow in the middle. I just don't like how it says all."* The `.ov-all` pill wears `state.slices.length` — the **TOTAL** chop count, not how many are selected: the header's own two spans already carry the selection (`All Chops` / `Chop 7` / `5 Chops`, and **that phrase is Max's from tp61b, so it is untouched**). `ovPaintAllPill()` paints it from `ovPaintTitle` (a panel open, a selection move) **and from the end of `redrawSliceOverlay`** — the one function that re-runs on a slice-list change, so a chop added or deleted under an open panel cannot leave a stale count. Nothing about the pill moves but the word: same 15 px outline on the Off pill's left rule, same dim-to-white hover, same purple border and white ink when the selection is live, and `background/box-shadow/text-shadow: none` in **both** states so nothing fills or haloes the number (Max: "no glow in the middle"). `font-variant-numeric: tabular-nums` keeps the pill from jittering as digits change. Bars: **[8e]** runtime — the pill reads the total, stays put under a full and a partial selection, and is unfilled and unglowing at rest and lit; **[8f]** source — the repaint call sits **inside `redrawSliceOverlay`'s own body** (brace-matched, not a bare file-wide `indexOf`), because deleting a chop needs the native `deleteSlice` and there is none headless, so that half cannot be driven at runtime here. Both mutation-checked: restoring the word or removing the redraw hook reds them. |
| `_tp77_gate.js` (30, was `_tp76_gate.js`) + `_shot77.js` | **tp77 — ONE MIDLINE, ONE CARD COLOUR, A REAL SINE, THE LFO'S GRID BOTH WAYS.** Max: *"every time I try to click on point or custom, it gets lower … draw a line through the center of that bottom, it needs to go through the middle of them both … the shaper doesn't follow the same grid rules as the LFO, the LFO's grid rules are very smart … the shaper has these purple dots on the outline, I just want it to be pure white … sign is not in the middle … the Ladder LP, that's supposed to have the arrows, make sure they hug the word … our card is a couple of shades lighter, I need ALL the cards to be the exact same colour … any card that still has capital glitch, ARP, Robin needs to get changed down … I don't want that purple button at the top … move Shaper to the very top left, the beginning of S lines up with all this stuff … whenever I select random curve it's not random, it's the same … that's not a fucking sine wave, that's a triangle spike wave … free draw should always be a curve but sometimes there'd be like a ramp."* 🚨 **THE BAR WALKED BECAUSE A FOCUSED `<select>` SCROLLED ITS `overflow:hidden` SCREEN** — the chip's invisible select sat at `inset:-3px -2px`, overhanging the screen's floor, so the browser scrolled the box to reveal it (**measured `.screen.scrollTop` 0 → 2.85 on the first click**) and the whole bar rode up with it. Select flush at `inset:0`, plus a `scroll` belt on the screen. The bar is now a **three-column grid** (`minmax(0,1fr) auto minmax(0,1fr)`), so the middle chip is centred in the bar rather than merely between two words of unequal weight, and every chip owns the bar's full height so all three share one midline. 🚨 **A SINE CANNOT BE DRAWN ACROSS THREE POINTS** — the segment law is the exponential `bias()` (the identical one in the DSP at `PluginProcessor.cpp:1914`), so the old `[[0,0,-.62],[.5,1,.62],[1,0,0]]` could only ever be a spike with soft shoulders. `SINE9` = nine points on the eighths with each segment's curve **fitted min-max to the cosine it covers** (±0.252 / ±0.041): **max error 0.5 % of full height**. The Sine brush stamps it too. 🚨 **THE CLIFF AT A FREE-DRAW SEAM** — a stroke starting inside the field left the old shape's breakpoint an arbitrary hair outside the swept span, so the line fell off a wall to reach it (**measured: a stroke 4 px in over a gate opened `M0.0 10.0 L3.6 98.0`** — full height across 0.8 % of the width). `FREE_EAT = 0.02`: the sweep eats a margin past each of its own ends and owns an edge outright when it comes that close, so the bridge is a ramp, never a wall. **The LFO's smart grid, both axes:** the LFO's `shSnap` lands a drag on the time cells AND the level lines (`index.html:32257/32264`); the Shaper only ever snapped x, so a node slid freely off the grid it was drawn against — `snapY()` now mirrors `snapX()` on every drag, group drag and double-click add. **Random stays random:** a native `<select>` fires NO change when you pick the row already selected, so a second helping of Random never reached `stock()`; the random rows now leave the select deselected (the chip's word comes from the state, so it still reads its name). **One card colour:** `.ti-card` was translucent over a 22 px backdrop blur, so its colour was whatever sat behind it — lighter over the synth panel, true in the popped window, which has always been forced opaque. Every card now paints that opaque pair (`#26223E → #1C1932`) docked and popped. **The header:** the grip glyph and the purple pip are gone (the whole `.h` still drags), `text-transform` off the title so Arp / Glitch / Robin read as built, and `.h` padding 10 → 14 px so the title's first letter stands on the same left rule as the screen and the tile row. **Tiles:** a lit lane is plain white with no border and no ring; the lane on the screen is told apart by its ink (deep purple on white), and an unlit selected lane keeps the purple outline with white ink. Breakpoint dots are pure white in both renderers. **The type chip wears the filter's arrows** (`‹ Ladder LP 24 ›`) above the invisible select, stepping the roster and swallowing their own clicks. ⚠️ **HARNESS TRAP FOUND AND FIXED:** the card measures ~560 × 800 device px (body zoom 1.46 × card zoom 1.2) and opens near x 880, so a **1200 px viewport cut its right third off** — a drawn stroke simply stopped at the edge (**59 of 91 moves reached the field, the rest landed on `<html>`**) and every gesture bar was silently short. The gate now parks the card at 6,6 before any gesture. Bars: [25] one midline + no walk on focus · [26] the middle chip centred to half a pixel with a long word on the right · [27] the arrows hug (≤ 8 device px), sit on the word's line, step the roster and never fall through to the browser · [28] 🚨 the Sine is a sine at 64 probes · [29] 🚨 snap on both axes · [30] 🚨 no wall at either seam of a mid-shape stroke (a Gate base, so its own instant risers are told from the seam) · [31] Random deals a new shape · [32] 🚨 docked colour == popped colour, no alpha, no blur · [33] the header across all four card families · [34] the lit tile and the white dots. Mutation-checked: [28] [29] [30] each fail when their fix is reverted. |
| `_tp77_gate.js` (was `_tp76_gate.js`) + `_shot76.js` | **tp76 — ONE LINE, ONE GRID; THE LFO'S GRAMMAR IN THE SHAPER; THE MENU ON TOP.** Max (with a beat made on it): "we need consistency with the LFOs … three different grid lines … I want the shaper line, that thin white, to be our new LFO … the capabilities and functionality of the LFO on the shaper … the point is glitchy … the right click menu pops behind my shaper … it's a pretty big menu … fill in white like the LFO and the glitch does … a whole list of shapes". 🚨 **One line:** the Shaper's line is 1.7 units at ~0.75 scale ≈ 1.07 px; the LFO pane (~0.98 scale) now draws 1.3 units, the LFO card (non-scaling strokes, fb232) 1.1 px, the expanded pane 1.7 units; the pane's nodes shrink through CSS `r` (SVG2 geometry); every grid is the white .065 / .14 (the pane's stock-shape grid was purple). Bar [17] measures the pane's and the Shaper's stroke in PIXELS and wants them within a quarter pixel. 🚨 **The LFO's grammar** (its `bindShaper`, ported): a click SELECTS a node (never adds), a drag on empty ground rubber-bands, grabbing a selected node drags the whole selection inside its neighbours' bounds, a double-click on empty ground adds (snapped), on a node deletes, on a curve dot straightens; the 3 px threshold, the near-grab, the zero detent — bar [15]. 🚨 **The menu on top:** `#syn-panel` is a stacking context (z 30), so INSIDE it no z-index beats a body-level card; while a card floats, `showMenu` portals `#syn-ctx-menu` to the body as position:fixed (the panel's origin in unzoomed px + the local px — the fb368 one-space law) and `hideMenu` brings it home; the 44 `#syn-panel .syn-ctx-menu` rules became `:is(#syn-panel .syn-ctx-menu, body > .syn-ctx-menu)`. Bar [6] proves `elementFromPoint` over the card lands in the menu. The menu is trimmed (no tool rows, no stock list — the bar's dropdowns own those) and grows Remove / Straighten selected. **The shape library:** the shape dropdown groups Basic · Gates (trance patterns) · Curves (swell, pluck, sidechain, bounce, pump) · Steps (random), and Time's Basic · Stutter · Tape — every entry a generator. A lit lane fills white (`.fxb.on`, the chassis rule) with the purple ring when selected. The bar's chips sit on the floor. ⚠️ CSS `r` on circles needs a current WebKit — the gate skips the node check when the pane draws no nodes. |
| `_tp77_gate.js` [0b] [13] (was, through tp76, `_tp74_gate.js`) | **tp75 — THE SHAPER'S EMBLEM IS A SAWTOOTH.** Max: "I think I want the Shaper emblem to be a sawtooth wave." The tile's one path (`path.shpWave`) is TWO saw periods — ramps that rise to the top and drop straight down at x = 8 + 13·phase and 13 further; at phase 0 the drop sits mid-tile, so the home reads as a saw at rest (one period read as a plain line). ⚠️ A `//` comment appended inside a one-line loop swallowed its closing brace: the whole frame-dispatcher script block failed to parse, `__tiFrameReg` never existed, and the filter roster global never landed — the Shaper gate read 15/17 with NO page error listed. `_tp74_gate.js` [0] now asserts the page BOOTED whole (`__tiFrameReg`, `__tiWind`, `__FLT_ENGINES`). Same law as before: one period per beat of the host BPM, the house wind-up / wind-down, still with Motion off. |
| `_tp77_gate.js` (was, through tp76, `_tp74_gate.js`, 17) + `FlowShaper_test.cpp` (25) + `_shot72.js` | **tp74 — THE SHAPER, COMPACTED.** Max on tp73: "make where it says Free draw / Custom / Formant way smaller … I don't fuck with the emblems … increase the height of the screen … there should not be an on and off button in Target — three buttons … Filt is purple instead of white … a regular Apple default drop-down menu … the browser menu gets cut off when popped out … the draw mode snaps — free-flowing like water … see the shaper lines in the background … stay away from the all capitals … Smooth and Tension overlap … am I really hearing it?". So: the screen is 124 px and its bar 18 px of 7 px chips, each a NATIVE `<select>` (only the Filter's 118 keep the rack's two-pane browser), no glyphs; three knobs a target, no On (a double-tap on the tile lights it) — Volume Attack/Release/**Punch** (a 12 ms overshoot on every opening, edge read against a 5 ms lag because a table step is a ~47-sample ramp), Time Fade/Glide/**Range** (¼…×4), Filter Reso/Drive/**Spread** (`FilterFxEngine::Params.spread`, a continuous L/R split beside the rack's bool), Pan Width/Bass/**Haas** (the side the pan leaves delayed up to 15 ms), Repeat / Drive / Phaser / Crush as they were — `k[6]` per lane, T19 / T20 prove Punch and Haas; Smooth now reaches 250 ms (heard, not implied). The lit tile's ink is white (`.ti-card.shp-ext .fxb.cur` — the chassis rule carries the same three classes and painted it purple). No caps but the tiles. Other lit lanes ghost behind the line (spliced into the field's markup — `insertAdjacentHTML` into an svg births HTML-namespace paths that never paint). Free draw keeps every pointer sample at its own x, replaces only the x range it covers, drops collinear points on release (bar [3] checks the points sit on the drawn curve and off any grid). 🚨 The popped card's browser: `openTwoPaneBrowser` in a card-only page asks `resizeCardWindow` to grow the native window to hold the panel, places it after the view resized, and the card's own size push restores it on close. ⚠️ `getComputedStyle` is live — snapshot before the next paint (twice now). |
| `_tp77_gate.js` (was, through tp76, `_tp73_gate.js`, 16) + `_shot72.js` | **tp73 — THE SHAPER IS THE GLITCH CARD WITH THE LFO IN ITS SCREEN.** Max on tp72 (screenshots): "this does not look anything like the LFO … why are you not taking the code from the LFO and just pasting it here … the shaper is way too big … take the exact same glitch and just replace everything there with the LFO at the screen". So the card is the Glitch chassis again (316 wide, the 96 px screen, the eight text tiles with their dots, Shape / Target / Clock, two boxes a row, the chain) and the screen calls THE LFO PANE'S OWN RENDERER: `window.__lfoField` (lent from the LFO IIFE — its `shX / shY / shPathD / shEvalPts`, the node / curve-dot / hit markup of `shaperSVG`'s custom branch, its icons, chevron and dropdown). 🚨 Bar [1] proves it byte for byte: the field's stroke `d` equals `__lfoField.pathD(pts, 456, GEO)`. The field's grid ink is the LFO's (white .065 / .14 — tp72 had copied the wrong rule, the purple `mv-grid`). The screen's bar is the LFO's (`.mv-ov.bot` / `.mv-c`): the brush chip, the shape chip (the LFO's glyphs and its dropdown; Time lists its presets), the type chip on the roster lanes opening the RACK'S two-pane browser (the filter's categories, the distortion's families, the phaser / crusher groups) — normal case, no more all-caps list. The lane's Phase / Swing / Tension / Floor / Duck move the BREAKPOINTS the LFO draws (nodes stay ON the line — bar [8] checks every node against the stroke), the display smoothing and the dashed raw line are gone. Target = the Glitch's two boxes: knobs + On left, the steps (dropdowns) right (Poles / Char, Range, Law, Mode) — a step drives a k through an `sk_` proxy. Clock = the Glitch's OUT box: Rate · Grid · Trigger dropdowns, Sense beside. ⚠️ `CHEV` / `ICON_CUSTOM` are `var`s declared AFTER the lend point in the LFO IIFE — lend them through getters or the chips read "undefined". ⚠️ A phase-shifted lane's display cuts the cycle at its seam: both screen edges read the value at `phase`, the seam is a step at swing⁻¹(1 − phase); the first cut dropped the endpoints and drew a flat line. |
| `FlowShaper_test.cpp` (23) + `au_shaper_lock.cpp` (9) + `_tp77_gate.js` (was, through tp76, `_tp72_gate.js`, 16) + `_shot72.js` | **tp72 — THE SHAPER, TO THE BRIEF.** Max (screenshots of tp71): "this looks nothing like the mock-up … the grid to literally be the same thing as the LFO … the follower is trash, it bounces back and forth … the target is fucked up … too lengthy, needs to be wider … I need to be able to right click … refollow the instructions". **The card is the mockup's** (462 wide, the eight targets, Shape / Target / Clock, pill rows in titled boxes — Brush · Shape · Rate · Grid · Trigger). **The screen is the LFO editor's field**: its grid classes and numbers (`sh-grid` .14/.22 purple, the white glowing `mv-stroke`, `mv-fill`, `sh-nd` nodes with `sh-hit` targets, `sh-cd` curve dots for the segment tension, the `mv-play` / `mv-foll` follower that fades in silence). 🚨 **The line the eye sees is the line the DSP reads**: Tension / Phase / Floor / Smooth / Swing move it (the raw breakpoints stay, dashed behind, when the knobs have moved the line off them) — bar [8]. **The rosters**: the Filter lane's type is the rack's 118-engine roster through the rack's own two-pane browser (`FLOW_CHOP_FILT_MODE` is the engine index; the processor lends a `FilterFxEngine` per lane, armed lazily), the Drive lane's the 23 distortions (`DistortionEngine`, the engine mixes its own aligned dry), the Phaser lane picks from the roster's phasers / flangers / combs, the Crush lane adds the roster's crushers and the digital family — `wc::ShaperExt`, the JUCE-free engine's hook (T17 counts the calls per sample). **The triggers**: Sync · Free · Audio · MIDI per lane (`FLOW_CHOP_<LN>_TRIG`; T12–T14, AU bar [8]: the gate runs from the note-on with the transport STOPPED). **The Target tab is filled** for every lane (Volume Attack/Release + Gain/Duck · Time Fade/Glide + Range · Filter Reso/Drive + Poles + Character · Pan Width/Bass mono + Law · Repeat Seam/Decay/Pitch + Slice/Reverse · Drive Tone/Makeup/Character/Bias · Phaser Feedback/Stereo/Drive · Crush Bits/Rate/Tone), each in `FlowShaper.h` (T15 Duck, T16 reverse/decay/pitch, T18 Glide = a pitch-bent slide instead of a cut). **The follower** advances on the tempo between pushes, soft-corrects on each push, snaps on a jump, never runs backwards (bar [11]). **Right-click** opens on the right pointer-DOWN (macOS opens menus there — the EQ's law; the gesture's contextmenu is swallowed for 150 ms) with the LFO's rows: Grid + Level sliders, Snap, flips, Random ×3, the eight tools, Wavetable → Shape (Osc A–D via `getOscLfoWave` + RDP), the stock list, Clear — no Extend. ⚠️ `.chop-ext .pane .kin` forces four columns on every box of the chassis — the pill rows were a quarter wide until `.ti-card.shp-ext .gbox .kin` outranked it (found by `_shot72.js`, the card's own screenshot). ⚠️ `getComputedStyle()` is LIVE: snapshot a pill's colours before the next click unlights it (bar [10]'s first cut read the base colour). ⚠️ The smoother bleeds across the cycle wrap on a saw — a real deviation, so bar [8] measures on the Pan lane's sine. |
| `FlowShaper_test.cpp` (14) + `au_shaper_lock.cpp` (6) + `_tp71_gate.js` (11 — DELETED in the tp76 cleanup: it aimed at the tp71 DOM and threw at HEAD; `_tp76_gate.js` is the Shaper's page gate) | **tp71 — THE TERRAIN SHAPER: THE CHOP CARD IS A SHAPERBOX / GROSS BEAT-STYLE MULTI-LANE SHAPER, DAW-LOCKED.** Max: "as soon as you press play it instantly shapes … no offset time … the screen being replaced as the shaper and the buttons being the effects … everything you make with the mock up has to actually be in the DSP". `Source/FlowShaper.h` (JUCE-free): eight lanes (Volume / Time / Filter / Pan / Repeat / Drive / Phaser / Crush) each with its own 2048-cell baked shape read at `phase = ppq / cycleBeats mod 1` EVERY SAMPLE — nothing can leave the grid; the Time lane is Gross Beat's read-position law on a 16 s ring (jump law: look 1.5 cells ahead, land on the settled target, ≥ 48-sample fade — the first cut scratched through the ramp cells); Repeat is a grid-captured slice loop. `FlowShaper_test.cpp`: T1 play from 0 / mid-cycle / loop wrap land on the same sample, T3 halftime halves the clicks and a 1/16 stutter repeats the first sixteenth, T4 no clicks, T9 the phaser's notches, T10 the crusher's residual, T11 a stopped transport holds phase 0. `au_shaper_lock.cpp` drives the INSTALLED AU through `kAudioUnitProperty_HostCallbacks`: 🚨 **[1]** play from bar 1: sixteenth 0 ON, 1 OFF (-17 / -44 dBFS) from the first samples; 🚨 **[2]** play pressed a sixteenth in: the first sixteenth heard is OFF — the shape is read at the DAW's position, not from the press; [3] stopped = phase 0 held; [4] Volume On = 0 → no gate; [5] the Time lane's unity ramp is a wire (per-sixteenth level within 0.01 dB; ⚠️ the voice is NOT sample-repeatable take to take since tp70's Random phase, so a sample diff is the wrong tool). `_tp71_gate.js`: the card shell (Glitch chassis, title Shaper, mix bar, pop-out, presets), the tile's lone sine emblem, breakpoints + ghosts + selector strip + brushes + free draw, the JSON push to `setShaperJson`, the params, the dice leaves it alone, the right-click menu, preset recall, no page errors. **tp71b — THE TILE'S SINE RIDES THE MOTION CLOCK.** Max (screenshot): "i wouldn't have the shaper cut off like this … it needs to wind up / wind down just like every other animation … make the rate the same as our daw … good on CPU … follow the motion setting". The clipped SMIL scroll is gone: one FULL period between fixed ends (x 8..34), its phase rewritten by the `flowTiles` painter through `__tiWind('tile-chop', P=1, home 0, rate = __hostBpm/60)` — one period per BEAT of the host BPM (a new one-statement stamp in the frame, shipped only when it changes), the 250 ms wind-up, the Hermite wind-down onto phase 0 (the same picture every time), still with Motion off (tile keys are not WIND_ESSENTIAL), and a 330-byte path string only on frames where the clock moved. Bar [9]: 120 vs 240 BPM travel ≈ 1.29 vs 4.0 periods over 0.72 / 0.96 s, lands on phase 0 with m = 0, Motion off = home picture through 45 live frames. ⚠️ `K[key]` is LIVE — snapshot it before the page's own lane calls again. ⚠️ Page gates resolve paths from the plugin root (`node Tests/_tp71_gate.js` from `plugins/Terrain`), and `_tp57_patcher_gate.js` takes the page path as argv[2]. |
| `au_chord_cert.cpp` (+ `au_chord_beating*.cpp`, the investigation) | **tp69 — WHY A SINE CHORD CLIPPED AND BEAT IN TERRAIN AND NOT IN SERUM 2.** Max: "play a C minor 11th, even the D next to the D sharp … Serum 2 doesn't clip." Both installed AUs, Serum 2 on its SUB oscillator (its sine; the init patch is a saw), the same close Cm11. **FOUND:** the per-note level is IDENTICAL (-15.14 dBFS both); Terrain's Sine is a pure sine; the beating is the same physics in both; the difference was the master stage — the limiter's 0.8 ms attack cannot hold a sine's peak, so the soft clip squared every beat peak: a 7-note Cm11 carried distortion **46 dB** under the notes, 9 notes **29 dB** under, while Serum's output on the same chords was +0.74 / +1.65 dBFS with **nothing** added (-285 dB). Serum has NO limiter (fb264's premise was wrong); the RMS was identical either way — the stage never made the chord quieter, only dirtier. **FIX:** a linear master in a host (`masterGuard_` = standalone only); measured after: 81 / 79 dB under, peaks +1.38 / +1.75 like Serum's. ⚠️ A **-80 dB second-order residual** (difference tones, 20-50 Hz) remains: it vanishes when OSC Level is halved and stays when Output Gain is, so it sits between the voice sum and the output trim — inaudible, not yet located. ⚠️ Phase Mode "Manual" starts every voice together (attack peak -1.8 vs -4.0 dBFS with Random; Serum ships Rand Phase 100 %) — left as is. |
| `FlowGlitch_test.cpp` T35 | **tp68 — THE ANCHOR IS ON THE GRID.** Max: "as soon as you press play you get it right there … sometimes it waits a bar and a half." **FOUND:** two anchors, two faults — the first clock latched a boundary already behind the play position (a play pressed mid-step fired at once, off the grid), and every re-anchor (loop wrap, relocate, grid change) latched the NEXT step, so a DAW loop wrapping exactly onto a boundary missed that downbeat and fired a whole step late, every pass. One rule now for the main tracker and every fire group: land ON a boundary → fire there; otherwise the next one. T35a a play from a bar line fires in block 0; T35b a quarter-step-in play waits for the next boundary; T35c a wrap onto beat 8 fires in the wrap's own block. Plus: the card's dice keeps off Swing, Chaos, the Gate's Nudge, the Repeat's and Scatter's Vary (`DICE_TIME_RX`). |
| `_tp67_gate.js` | **tp67 — THE GLITCH IS ALWAYS IN TIME.** Max: "always be on sync … Free only selectable … do not randomize the sync ever to free … do not randomize the time of each of the modules." **FOUND:** the global dice had left the Flow cards alone since tp48; it was the CARD's own dice — `syncf` (the Clock) and every module's `_otrg` (Trig) are binaries, rolled as coin flips, and `diceGrids` re-dealt every `FLOW_GLI_*_GRID`. Now `DICE_TIME_RX` keeps the clock, the fire grid, quant, every Trig and out grid out of the roll, `DICE_SYNC` snaps the Clock and every Trig to Sync on every roll, and the grid re-deal is gone. 🚨 **[1]** twelve rolls from a hand-set Free: Sync after every one, no time key moves, the rest of the card still rolls. **[2]** Free by hand still reads Free. |
| `_tp66_gate.js` / `_shot66.js` | **tp66 — ONE WAVEFORM, AND THE AUDIO PICTURES KEEP MOVING WITH MOTION OFF.** `window.__tiWave` (index.html, beside `__tiMotionOn`) is the one sample picture: a mirrored min/max envelope on a hairline baseline, one column per CSS pixel folding every bin that lands in it (🚨 **[2]** a spike in every other bin is a full-height column everywhere — nearest-bin sampling, the OSC slot's old way, would show all or nothing depending on phase), the outer envelope held over ±1 column and 5-tap smoothed (two rippling hairlines at the hero's zoom become one edge), the ink at 16 % for the body and 92 % for two 1.25 px contours, no glow. 🚨 **[1]** the same peaks through two boxes = the same pixels. **[3]** a chop tile scales by the whole sample's peak (`peakRef`). **[4]** the hero and an OSC slot paint through it. **[5]** the scope is not parked by the editor with motion off and the noise wave view follows `act` unscaled; **[5b]** with the motion envelope at zero the noise wave shows its sample and its follower head (the view reads its ON and its viz MODE once at boot — the gate answers `getNoiseVizMode` before load). `_shot66.js <dir>` renders w66-hero/osc/noise.png with one synthetic pluck: **look at them**. |
| `au_tape_types.cpp` | **tp64 — THE FIVE TAPE TYPES REACH THE ENGINE (installed AU).** The card powered, in the chain, fed by OSC A (`Tape SRC_A` — without the pill the engine is never built and the slot passes through, which is what the first run measured: five identical spectra), Mix 1.0; each type rendered 1.5 s past its re-seat and measured for 1 s on 32 log bands. **FOUND:** `applyTpe` clamped the choice to `ty <= 1` from the day the card was born, so tp43's Reel and tp60's Porta / Wire ran as Studio in every host — Max's "the DSP is the same" was literally true. Now: closest pair Studio/Wire 2.83 dB, Reel/Porta 14.3 dB; the Reel's Tilt moves the Sculptor by 9 dB. `TERRAIN_DETERMINISTIC` is set. |
| `_tp64_gate.js` | **tp64 — THE EVENING LIST, PAGE HALF.** 🚨 **[0]** the Tape shelf is the routed card's five types + the Deck, no global machine; **[1]** two Reels at once, each a card node, Sculpt / Drive / Timbre on its own `SYN_TPE2_` set, no flip, both TYPE params written; **[2]** the Deck lands from the shelf, Remove clears its In-Chain claim, 🚨 presence follows `SYN_DCK_ACTIVE` (the sampler law) — written through `__tpSetP`, the canvas's own door, because `pv()` reads the shim, not `window.__params`; **[3]** MOD and CHOP pills never toggle off; **[4]** the bend "2" and "st" are white in the label's font; 🚨 **[5]** a preset recall re-hydrates the Chop page (natives answered by the gate through a `window.Juce` wrapper — the sim freezes `getNativeFunction`, so the property is redefined, not assigned): waveform up, chops drawn, the library strip named; a recall into an empty layer clears the picture (`getLayerHasSample` must be faked too or the 10 Hz poll strips `empty-state`); **[6]** the C++ half by text. |
| `mac_dice_memory.mm` | **tp63 — DOES THE MEMORY STACK UP ON EVERY RANDOMIZE?** Max: *"every time I randomize my memory goes up ... it just won't go down."* One installed AU with its real editor, chord held; the fb504 hook rolls the Patcher dice N times and the host + page footprints are printed per roll. **MEASURED before tp63:** 812 → 827 → 2073 → 3866 MB on rolls 0–3 (+3.05 GB in three rolls), then ~+16 MB per roll. `/tmp/tpdice <rolls> <gapMs>`. |
| `au_roll_memory.cpp` | **tp63 — WHICH PARAMETER FAMILY GROWS THE FOOTPRINT PER ROLL?** Rolls each family the dice touches (wavetable presets, filter types, every rack device in/out of the chain, device types, engines, flow slots) to random values R times on the installed AU and weighs each round. **FOUND:** wavetable presets +57/+64/+32/+33/+40/+8 per round and +234 MB net after defaults — every table ever visited stayed built; every other family +0. The wavetable bar requires the net back under 60 MB after the idle release. Sample LOADS are not reachable here (`mac_dice_memory.mm`). |
| `au_lazy_memory.cpp` | **tp64 —** two bars added: BANK B goes back in the same idle window (read on **malloc `size_in_use`**, printed beside the footprint now — a release of many small blocks stays resident in malloc's pools and the footprint does not move, which is why the first run read "+288 held" with the bank already gone), and OSC E alone sounds again after a re-arm (+313 MB of live bytes, −15 dBFS). **tp63 — WHAT EACH LAZY ENGINE COSTS, AND WHETHER IT COMES BACK.** Weighs each arm on the installed AU with no editor: every OSC A engine, the second bank, every OSC E engine, then everything back to default. **FOUND:** engine 6 (MODAL) = **+1,213 MB per bank**, engine 5 (HARM) = +55 MB, bank B = +298 MB, and before tp63 nothing returned (+2,834 MB held on a default patch). The last bars wait out the idle timer and require the MODAL + HARM memory back, then re-arm MODAL. |
| `_tp63_gate.js` | **tp63 — THE AFTERNOON LIST, PAGE HALF.** 🚨 **[0]** the Tape shelf lists the three MACHINE modules (Reel = the Harmonic Sculptor, Porta, Wire — tp43a unlisted them, tp60 rebuilt them as card types on the wrong engine) plus the card's Studio / Cassette, no "Tape ·"; **[0b]** picking one lands the module and sets `TAPE_MACHINE`; 🚨 **[1]** the master filter always wins (a cable into the filter turns raw taps post-filter; a card cabled while the osc is in the filter takes it filtered); **[2]** the Slices pill holds its position across 8/16/32/64; **[3]** no caption under the stems; **[4]** the pitch-bend number wears the theme ink; **[5]** the distortion push is ONE counter (tp62 nested two — they de-phased after any quiet spell and the shaper stopped following its type with motion off); **[6]** the capture strip's OFF word is white on dark. |
| `_tp62_motion_gate.js` | **tp62 — MOTION OFF: NOTHING MOVES ON ITS OWN, AND THE ESSENTIALS STILL DO.** Drives the page the way the plugin does (a 60 Hz fake lane stamping the note flag and calling `__tiFrame()`). 🚨 **[2]** with MIDI live and motion off, the decorative envelope (`__tiMot.e`) stays at ZERO and the breathers stay still; **[3]** a Flow-tile wind key still advances, a decorative key does not (⚠️ on FRESH keys — calling the painters' own keys with a different period re-homes the live tile, which the first run of this file did); **[4]** the frame still dispatches every registered painter at the lane rate — the essential painters are ordinary registrants and never read the clock; **[6]** the C++ half by source: decorative feeds on `decoTick`, filter every 4th tick always, scope parked, spectrum halved, lane at 30 Hz, 28 audio-driven numbers through `vz()`, reverb wash = mix × decay, marker read in the ctor. |
| `au_motion_null.cpp` | **tp62 — THE MOTION SETTING NEVER TOUCHES THE AUDIO.** Two fresh instances of the installed AU, the same chord and blocks, `motion-off` marker present vs absent: **bit-identical**, 0 of 184,320 samples differ. The user's preference file is restored on every exit. |
| `mac_motion_cpu.mm` | **tp62 — MOTION ON vs OFF, WEIGHED ON REAL EDITORS.** N instances of the installed AU, each with its real editor in a real window (rAF needs one), chord held on a real-time render thread per instance; the plugin's whole cost on the machine = this process (push lanes + DSP, `getrusage`) + every WebContent process that APPEARED for the scenario (`proc_pid_rusage`, per-scenario pid-set diff because a closed page is kept alive, fb521). ⚠️ `ri_user_time` is MACH TICKS (~41.7 ns here), not ns — the first cut read the pages at 2 % of a core. **MEASURED 2026-09-20:** 1 editor 41.2 → 27.3 % of a core (−13.9, 34 % of the UI); 4 editors 160.8 → 91.3 % (−69.5, 43 % of the UI). The DSP is inside the host column and does not move (`au_motion_null`). Memory: pages ~80 MB per editor less; the HOST column's difference is first-instantiation allocation (ON runs first), not the setting — do not quote it. `/tmp/tpmot <instances> <seconds>`. |
| `_tp61_gate.js` | **tp61 — THE 2026-09-20 LIST, THROUGH THE SHIPPED DOORS.** 🚨 **[0]** Sampler A → Glitch → Delay is ONE chain — tp41's `inlineFlowInto` asked the OSCILLATOR mask only, so a card fed by a sampler answered *"Cable an oscillator into Glitch first"* (Max hit exactly that toast). It drives `window.__tpConnect`, the canvas's real cable gate, not a re-implementation. **[1]** the Sampler has an out and no in (tp61 reversed tp58, on Max's word). **[2]** a CABLED sampler is re-adopted with `layout.samp` deleted — its presence is derived from parameters now, not a localStorage flag shared by every instance on the machine. 🚨 **[3]** E F G H open the oscillator menu: the wiring loop always reached the pool clones, a `/SYN_OSC_([A-D])_/` written when there were four oscillators dropped them. **[4]** exclusive solo spans eight. **[5]** the library cluster clears the Slices pill WITH the count badge showing (the tp54 trap). 🚨 **[6]** no purple-FILLED box and no purple glow in any selected / playing / open state, measured on live elements in forced states. **[7]** the Slices count stays white. **[8]** the ALL pill is clear of the X, **[8b]** its left edge lands on the Off pill's, **[8c]** header and pill are proper case / thin / white, **[8d]** the header reads *All Chops* · *Chop 7* · *5 Chops*. **[9]** capture off greys the stem buttons and says why. |
| `au_stem_memory.cpp` | **tp61 — THE STEM RINGS, WEIGHED.** Max: *"Why can't you run it?"* — and the answer was that I had not looked: a V2 state blob's `<layers>` tree names each layer's `sourcePath` and `restoreSamples()` decodes it, which is exactly a hidden instance restoring a DAW project. Opens the installed AU twice with the same four-sample state — capture-off marker present, then absent — and weighs the process (`task_info` phys_footprint). **MEASURED at 48 kHz: on = +1155 MB, off = +8 MB, five rings = 1152.** The user's real preference file is restored by an RAII guard on every exit path. Not reached, said plainly: the live ON→OFF toggle (an editor native) — `stem_memory_gate.py` pins that release. |
| `stem_memory_gate.py` | **tp61 — CAPTURE OFF MEANS NO RINGS, AND THE MEMORY COMES BACK.** tp43's switch released the ~202 MB export ring and never touched the FIVE STEM rings (600 s × SR × 2ch × f32 × 5 = **~1,058 MB**), which armed off a loaded sample whatever the setting said. Reads every arm site and its guard, the release order (un-publish → sleep → free), and that the preference is read in the CONSTRUCTOR — before the timer that arms can tick, which is what a hidden instance needed. Structural half; the WEIGHED half is `au_stem_memory.cpp` (it turned out a state blob CAN load a sample into a layer — my "no headless way" was wrong). `--controls`: four mutations, all red. |
| `tapemodes_cert.cpp` | **tp60 — FIVE TAPE MODES, AND NO TWO OF THEM SOUND ALIKE** (the SHIPPED `TapeFxEngine`). Max: *"where are the rest of my TAPE MODES??? we literally had 5 total and now it's three."* He counted right: before tp43a the Patcher's Tape shelf listed the three GLOBAL machines (Reel · Porta · Wire) plus the routed card's two types (Studio · Cassette). tp43a did what he had asked — *"these tape modes need to stop being global … actually follow the patcher's cables"* — by deleting the three global entries and moving Reel onto the card; PORTA and WIRE existed only as global machine names and went with them. 🚨 **THEY CANNOT COME BACK AS ALIASES** — Porta WAS the CassetteMachine under another name and Wire WAS the WireMachine under another name, so re-listing them would put two pairs of identical entries in the browser, which is the other half of the same complaint (*"why is REEl the same visualizer as the STUDIO?"*). Each gets its own TRANSPORT, which is where fb368 established the machines actually separate (*"the RAW machines differ by 12.18 dB … only 7.66 survives the transport — a ripple, not a character"*). 5 bars: [0] all five render finite audio; 🚨 [1] all TEN pairs clear the ripple bar (worst 6.37 dB); 🚨 **[2] the bar is SELF-CALIBRATING — whatever the closest pair among the three SHIPPED modes is, the two new ones must be at least that far from everything** (Porta/Cassette 7.86 and Wire/Studio 6.69 against a shipped worst of 6.37; a mode harder to tell apart than Cassette and Reel already are is not a fifth machine); [3] Studio / Cassette / Reel resolve to exactly the voice they always did, so no saved patch changes; [4] the machine map is what the comment says. ⚠️ **SEED IT DETERMINISTICALLY OR IT MEASURES NOISE** — every machine seeds its wow/flutter/drift from its own ADDRESS (`tw::seedAddr (this, 0x5EEDu)`), and the first cut of this cert reported Wire/Studio at 8.94 dB and then 5.59 after a change to PORTA, a mode neither shares anything with. fb636's `TERRAIN_DETERMINISTIC` is the switch. `./Tests/tapemodes_gate.sh` | 5 |
| `_tp60_tape_gate.js` | **tp60 — FIVE TAPE MODES IN THE BROWSER, AND FIVE DIFFERENT PICTURES.** [0] the Tape shelf lists five again; 🚨 **[1] IS THE ONE THAT MATTERS** — `TDRAW` held TWO entries and the lookup ends `|| drawWire`, so Reel (and every other name not in the table) drew the Studio card. **A bar that only checked "the table has five keys" would pass on five keys pointing at one function**, so this one RENDERS each mode to a 300×150 canvas with one identical feed and requires five different pictures by pixel signature; [2] the routed card offers all five, appended so slots 0..2 never renumber a saved patch. The three new drawings are built from the shipped drawers' own helpers (`bounds` / `reel` / `vu` / `tA` / `tP`) so they read as the same instrument: Reel is two BIG reels with a straight path and one wide VU (it is the stable machine), Porta is a small shell over FOUR channel faders, Wire is two spools with a strand that visibly TWISTS and a magic eye. ⚠️ Checked by eye as well as by hash — the first cut put Reel's VU underneath its own head block and drew the magic eye as a filled blob. `node Tests/_tp60_tape_gate.js <index.html>` | 4 |
| `_tp59_gate.js` | **tp59 — MAX'S LIST, AND THE TWO GATES THAT LIED.** 🚨 **[0] AND [1] ARE CORRECTIONS TO EARLIER GREEN BARS.** tp57 shipped ⌘A and its gate dispatched a synthetic KeyboardEvent straight into the page — proving the HANDLER and never the ROUTE, when fb135 had already recorded that the page does not get keys in a host ("fb134's make-the-WKWebView-first-responder did not survive the host — FL kept the keys"). Max: *"I cannot control A shit, you fucking liar."* He was right; the shortcut never fired once in a DAW. [0] drives the chord through `window.__tiHostChord`, the function C++ evaluates from `keyPressed`, so the bar goes red if that route is ever removed. And tp54's bar [2] measures the Slices pill's clearance from the sample library **with no slices loaded** — the one state where the count badge is `display:none` and the pill is a dozen px narrower; it read a comfortable 13.1 through two separate collisions. [1] loads sixteen chops first (2.7 px measured before the fix), which is the state Max screenshots. Also: [2] the Slices pill is the play pills' 17.5 px again (the 11 px count badge made it 19, so the word sat low against its row); 🚨 [3] the stem pane's status chip is invisible at rest — **tp58 gave it a background and left it at the default opacity 1, so a dark 358×19 bar painted straight across Export / Reveal from page load** (Max: "it has that purple whatever"); 🚨 [4] `#controls` stays hidden even when something else writes its inline style back — it was hidden by ONE click handler and four other places re-show it, so arriving at Chop from SYN left the Grain Engine / Effects strip on; 🚨 [5] chop 1's number wears the house chip — it is appended as a bare div outside any marker, so `.ti-slice-marker .ti-slice-label` matched **nothing** and it rendered as unstyled default text: Max's "black one" at the top left. `node Tests/_tp59_gate.js` | 7 |
| `_tp58_chop_gate.js` | **tp58 — MAX'S LIST, MEASURED.** [0] the empty hero has NO white grid and wears the sample oscillator's own prompt, not a shouted one (⚠️ the first cut scoped the rule to `#hero.empty-state` and the hero does not always carry that class — measured, it can be on screen with no class at all and the wireframe came straight back; "remove the white grid lines" has no state in it); 🚨 [1] the stem menu's A/B/C/D are the layer menu's own size — they were 83.5x38.75 against 83.5x48.5, the same width and TEN PIXELS shorter; [2] the stem pane's bottom space matches its top (12 / 11 px; it was 12 / 31, because an empty `.stem-status` held 11 px of height plus its 9 px gap whether or not an export had ever run — it keeps its job OUT OF FLOW, as a chip that fades on its own timer, because the house law is that nothing moves when content changes); 🚨 [3] the lock shows the loop's own tempo → the session's and typing one reaches `setLayerSourceBpm`; 🚨 [4] **switching pages leaves no chop residue at 60 ms**; [5] a covered subtree has its transitions OFF. `node Tests/_tp58_chop_gate.js` | 7 |
| `_tp58_samp_gate.js` | **tp58 — THE SAMPLER HAS AN INPUT, AND THE FLOW HALF THAT WAS ALREADY BUILT IS REACHABLE.** Max: *"why doesn't the sampler have an INPUT? I want to patch FX THRU IT BRUH CMONNN even FLOW CARDS n SUCH PLEASE ... I need my inputs bro."* Every bar drives the real door (`__tpAdd` / `__tpConnect`) and reads the PARAMETER that came out, never a class name. [0] the node has an audio in AND an audio out (tp56 gave it out only, on the reasoning that a sampler is a source — true of the signal, false of the PATCH: a cable is a relationship and which end you drag from is not part of it); 🚨 [1] an effect cabled INTO the sampler lights the same `_CHOPS` mask as the sampler cabled into it; 🚨 **[2] A CHOP LAYER FEEDS A FLOW CARD — `FLOW_CHOP_CHOPS` is a mask tp56 laid down and wired into `poolChopRef_`, and the canvas has refused the cable ever since** (`a.kind==='samp'` fell through to "cable it into an effect, or to Out"), so this is not new DSP, it is a door onto DSP that shipped a version ago; 🚨 [3] the eye hides a card's chrome AT ONCE (the LFO numbers used to outlive their own transition and paint over the emblem that replaced them); [4] and the VISUALIZER is exempt, because the emblems still have to move. `node Tests/_tp58_samp_gate.js <index.html>` | 6 |
| `_tp57_chop_gate.js` | **tp57 — MAX'S CHOP PASS, MEASURED.** 21 bars, every one driving the real control and reading what it SENT or what the box measures. The BPM is big / white / ultra-thin (was 10 px, weight 700, PURPLE); 🚨 the LOCK is visible again and a click sets `TI_BPM_LOCK` (tp51 had hidden it because it did nothing); ARM keeps its word and the dot carries the state; the slice count is white and ultra-thin; ONE font family across 187 visible elements; 🚨 **no label touches its box** — every pill measured on all four sides AND for top/bottom skew, because "the slices boxes be pissing me off" is a 1 px asymmetry; RR is Robin; 🚨 the trigger pane's top matches its bottom (measured 11 / 13 px, was 100 px of dead air); the stem pane's boxes share the trigger pane's left edge and width; 🚨 **the morph handle is where the pointer is at both ends and the middle** (0.0 px offset — it was up to an eighth of the track away and could never reach either end, because the pointer was mapped over the full track while the handle was painted at 12.5..87.5 %); Export / Reveal are emblems that still say what they are; the stem line goes away; 🚨 the strip knobs are the synth page's SVG arc carrying their value, with no conic gradient or radial mask left underneath (that banding IS the "tearing"); 🚨 **the slice drawer's crossfade moves and reaches `setChopFadeMs`** — it was already correct, and the bar says so with a number; the ADSR reads seconds over a second; 🚨 **⌘A selects every chop and ONE drag in the menu reaches all eight**, with the header saying ALL 8; Escape drops it; 🚨 **Delete removes every selected chop HIGHEST INDEX FIRST** (deleteSlice re-indexes what is left, so walking upward would delete the wrong chops from the second one on). ⚠️ **THE SIM HAS NO SLICER** — the grid pill asks C++ through `gridSliceSlices` and the stub answers `[]` to every get, so the stub is taught to answer `{slices:[…]}` (the shape `applySlicesJson` actually parses) or the count bar measures an empty string and says nothing. ⚠️ **The ADSR rows listen on `.ov-ad-track`, not the row** — aiming at the row dispatches into a parent with no handler and reads as "nothing happened". `node Tests/_tp57_chop_gate.js` | 21 |
| `_tp57_curve_gate.js` | **tp57 — EVERY CARD WITH A CURVE DRAWS IT, HOWEVER MANY YOU SPAWN.** Max: *"every time I just loaded up a second distortion the shaper is gone ... the distortion curve HAS to be there ... and make sure that doesn't happen for anything else that has a shape or curve on it."* Root cause, and fb355 had already written it down without anyone acting on it: *"the feed only ever describes instance 1"* — and the page did `rack.querySelector('.fxr-core[data-core="saturate"]')`, FIRST MATCH, so distortion 2 was left with `<path d="">` forever. (fb350 fixed this exact class of bug for the DELAY and the lesson did not travel.) 6 bars: three distortions spawn; 🚨 [1] all three have a curve; 🚨 **[2] EACH ONE IS ITS OWN CURVE** — a fix that painted instance 1 onto every card would pass [1] and be a worse bug, so the feed pushes a DIFFERENT shape per instance and the bar requires three distinct paths; [3] moving only instance 2's payload moves only instance 2's card; [4] a SWEEP over every shape-bearing card in the rack (11 lines) finds none empty. 🚨 **[5] AND ON THE PATCHER, WHERE THE CARDS DO NOT LIVE IN THE RACK** — the canvas MOVES a card out of `#fxr-rack` into its node (`adoptFx` → `n.body.appendChild`), so a rack-scoped query finds nothing there and a distortion SPAWNED while the canvas is open comes up blank. This never showed up by eye because a card painted BEFORE it was adopted carries its `d` attribute over; the bar wipes both curves from inside the canvas and re-pushes, so a stale attribute cannot carry it. Every other driver on the page already walks `__fxrCardsAll` for exactly this reason (the reverb's, the delay's, the tape's). **CONTROLS: 4 of 7 bars go red on the pre-tp57 code, and [5] alone goes red on tp57a.** `node Tests/_tp57_curve_gate.js [index.html]` | 7 |
| `_tp57_patcher_gate.js` | **tp57 — THE PATCHER'S THREE.** [0/1] 🚨 **the toolbar lives UNDER the modules** — Max: *"these emblem buttons should go under the modules at all times, we should only be able to see them on the dotted background ... it does it with the header and the footer, but it doesn't do it in the middle"*; it was `z-index:12` against a `.tp-world` of AUTO, so it out-ranked every card. The bar parks a module over the eye and asks `elementFromPoint` who owns the pixel. [2/3] 🚨 **the mini-map drags** — it listened for `mousedown` and nothing else, so holding and moving did nothing until you released and clicked again (that is what "laggy" feels like), and `drawMini` threw away the whole SVG subtree on every `applyView` to move one rectangle; the static ink is now drawn in WORLD units and *transformed*, so a pan writes one attribute. ⚠️ **the first cut cached on a signature that included the MAPPING, which moves with the viewport — so every pan frame missed the cache and rebuilt anyway.** [4/5] 🚨 **the hover chip's two names are doors** — clicking one glides that module to the middle (measured 1534 px → 0). ⚠️ **THE WHOLE PLUGIN HAS TO FIT IN THE TEST WINDOW**: the page scales to the window WIDTH (a 1200 px window zooms an 820 px client box by 1.4634), so an 820-tall window cuts 140 px off the bottom — and the MINI-MAP lives there; a real pointer aimed at it lands outside the document and the map reads dead. The first cut of [2] failed for exactly that reason with nothing wrong in the code. **CONTROL: 5 of 7 bars go red on the shipped code.** `node Tests/_tp57_patcher_gate.js <abs index.html>` | 7 |
| `au_chopsend.cpp` | **tp56 — THE CHOP SAMPLERS FEED THE RACK** (the real installed AU). Max: *"these aren't the sample oscillator engine, these are sample CHOPPED engines"* — Sampler A/B/C/D as real modules on the Patcher, routable into effects / filter / EQ / tape / granular. ONE PARAMETER PER DEVICE, not one per (layer, device): `<device>_CHOPS` is a 4-bit mask, bit L = chop layer L feeds that device's send bus. 4 bars: [0] all 96 rack devices + the Deck carry one and every one defaults to **0**, which is the Chop page's own law; [1] the reference note sounds; 🚨 [2] **every chop mask slammed to all four layers and the synth is BIT-IDENTICAL** (97 masks set, null **−240 dB**) — a routing change that reaches into the block where every oscillator's send is resolved is exactly the kind that quietly moves the synth, and a "does the parameter exist" test would never see it; [3] the processor reads the mask in BOTH places — a routed layer LEAVES the dry mix and is ADDED to every bus that claimed it, because one without the other is either a double or a silence. ⚠️ **WHAT THIS CANNOT REACH, SAID PLAINLY**: the layers themselves. A chop layer only sounds with a sample loaded into it and nothing in the AU's parameter surface loads one, so the audible half is not measured here — the cable that writes the mask is driven for real in `_tp56_chopsamp_gate.js`. ⚠️ **JUCE's AU WRAPPER REPORTS EVERY PARAMETER IN 0..1**, not its declared 0..15; the first cut of [0] asserted the declared range and went red on a parameter that was perfectly correct. `clang++ -O2 -std=c++17 Tests/au_chopsend.cpp -o /tmp/auchop -framework AudioToolbox -framework AudioUnit -framework CoreFoundation -framework CoreAudio && /tmp/auchop` | 4 |
| `_tp56_chopsamp_gate.js` | **tp56 — THE SAMPLER MODULES AND THEIR CABLES** (the page half). Every bar drives the real door (`__tpAdd` / `__tpConnect` / `__tpCut` are the functions the mouse calls) and then reads the PARAMETER the engine acts on. 11 bars: the browser's **Chop** shelf offers Sampler A/B/C/D; the node has ONE audio out and no MIDI in (its notes come from the Chop page's own keys); uncabled it draws to Out; 🚨 **the cable writes `SYN_RVB_CHOPS` and it is BIT 1 for layer B** — a fix that lit bit 0 would still "make a cable"; two samplers on one device is 0b1010, not a replacement; cutting one leaves the other; cabling to Out drops every claim; **removing the module drops its claims too** (a routed layer that lost its module would be routed into nothing and go silent); the patch remembers which samplers are on the canvas. ⚠️ **READ THE PARAMETER THE WAY THE CANVAS WRITES IT**: the Patcher's params go through `window.__synSliderShim`, NOT `window.Juce.getSliderState` — reading the wrong one showed 0 for a mask that was correctly set. `node Tests/_tp56_chopsamp_gate.js <abs index.html>` | 11 |
| `vibrato_cert.cpp` | **tp55 — JITTER BECAME A REAL VIBRATO** (`Source/Vibrato.h`, the header the plugin runs — the math was lifted out of `SamplerVoice.h` for exactly this reason; tp54's lesson is that a gate driving a HELPER proves nothing). Max: *"jitter isn't broken, it's the wrong feature."* The old `pitchJitterCents` sampled ONE random detune at note-on and held it for the voice's life — a de-phaser for stacked layers, silent on a lone one-shot. 16 bars: [0] at depth 0 the block is the EXACT identity (`active=false`, mul 1.0, step 0) so every existing null stays green; [1] it swings the depth on the knob (±49.99 of ±50 cents); 🚨 [2] **it is PERIODIC at the rate** — 96 unity crossings in 8 s = 6.000 Hz, and a one-shot detune would never cross unity at all; 🚨 [3] **the SEGMENT GRID** — the first cut ramped once across the whole block and this bar caught it at **7.97 cents of error on a 512 block and 30.62 on a 1024** (a chord across an arc of A radians misses the sine by ~A²/8, so my comment's "0.7 %" was wrong by 10×). A 64-sample grid divides that by 64: **0.1263 cents at every block size from 64 to 4096**, at ceil(N/64) transcendental pairs per block instead of N; [4] every sample-to-sample step is inside the slope's own ceiling, so there is no seam at a segment boundary OR a block boundary; [5] `reset()` lands on the zero crossing so note-on never steps; [6] DETERMINISTIC bit for bit (the old jitter pulled `juce::Random` and could not null); [7] a knob slam slides in; [8] clamps; 🚨 [9] **`SamplerVoice` uses it on BOTH paths** — the NONE path's per-sample playback rate and the warp path's per-block pitch (a stretched voice cannot varispeed, the stretcher owns the read rate) — and the old jitter is gone; [10] the warp offset is the same wobble. ⚠️ **A UNITY-CROSSING COUNTER MUST TRACK THE SIGN, NOT THE PRODUCT**: the phase grid divides the period exactly (4000 samples at 12 Hz / 48 k, 64 per segment), so every other period lands a boundary on phase 0 where the multiplier is EXACTLY 1.0 — the `(prev-1)*(cur-1) < 0` test lost a quarter of them and read **9.00 Hz for a 12 Hz vibrato**, a bar failing for a reason with nothing to do with the code under test. `clang++ -std=c++17 -O2 -I Source Tests/vibrato_cert.cpp -o /tmp/vibcert && /tmp/vibcert` | 16 |
| `_tp55_vib_gate.js` | **tp55 — VIBRATO ON THE CHOP MIXER STRIP** (the page half; the engine half is `vibrato_cert.cpp`). Every bar drives the control with a REAL pointer drag and reads what the knob SENT to the engine, not how it looks. 9 bars: JIT is out of the DOM and all four strips carry VIB + RATE; an 80 px pull sends cents for the right layer and the ring follows; the RATE knob sends Hz inside 0.05..12 and its sweep is LOG (a linear 12 Hz dial buries the musical 4-7 Hz band in the first half-inch — the 5 Hz default sits at 0.84 of the ring); double-click resets to 0 cents / 5 Hz; the knobs are tp54's 24 px house ring; the pair shares one centerline; the labels are sentence case; the page throws nothing. `node Tests/_tp55_vib_gate.js` | 9 |
| `_tp55_eye_gate.js` | **tp55 — THE EXTENDED CARD'S EMBLEM ANIMATES, AND THE SAMPLER'S PICTURE GOES EDGE TO EDGE.** tp54b gave the extended flow card its emblem back as a CLONE made once at adopt time — it can never move, because the frame dispatcher paints the LIVE tile's own children. 10 bars, including 🚨 [2b] the extended card's emblem actually MOVES and 🚨 [4] the sampler's picture fills its module (**measured before: a 235 × 51 strip in a 258 × 127 box, 25 px of dead ground over it and 52 under**; a waveform has no aspect ratio to protect, so the box is SIZED and the canvas redraws through its own ResizeObserver). ⚠️ **tp54b's probe COULD NOT have caught this**: it dispatched a `click` on the eye tool and THE TOOLBAR LISTENS ON `mousedown`, so the eye was never open and it shipped on a class name; bar [0] now asserts `#tp-page.viz` before anything else is measured. ⚠️ **AND THE TILES ONLY MOVE WHILE MIDI IS SOUNDING** (fb636 h1) — a motion bar that does not hold `window.__tiLive` open measures a resting page and reads FALSE for working code. ⚠️ **THE FIRST CUT REGRESSED tp33**: `.tp-body` is a flex COLUMN and the extended CARD is the tile's sibling, so a tile claiming 100 % × 100 % shoved the card **348 px** out of its node — the exact spill tp33 exists to forbid. Absolute + inset:0, the way `.tp-vglyph` has always covered the body. `node Tests/_tp55_eye_gate.js <abs index.html>` | 10 |
| `au_crossblend.cpp` | **tp53 — EFGH CROSS-BLEND WITH ABCD** (the real installed AU). Max: "EFGH cannot cross-blend with ABCD." He was right and the reason was ARCHITECTURAL, not a missing menu row: oscillators E–H are a SECOND VOICE BANK (`ensureBankB` builds its own UnisonSynth with its own sixteen `tw::SynthVoice`s), and a blend slot's modulator is `modPrev_[src]` — the previous sample of an oscillator rendered INSIDE THE SAME VOICE. There was no A in osc E's voice object to reach for. `Source/CrossBlendBus.h` is a shared board: every voice publishes the four taps it already computes, keyed by the NOTE it is playing and indexed by the ABSOLUTE block position, and reads the other bank's row. Nothing is re-rendered and no engine is duplicated — the modulator IS the real oscillator, whatever engine it runs. 7 bars: both banks offer 21 sources (E's WSLOT params are CLONES of B's, so the appended four reach them free); E FM'd by A moves the spectrum **8.41 dB**; 🚨 **[2] it is really the OTHER bank** — measured against a BANK-MATE source at the same depth on the same carrier (**3.74 dB** apart), because a fix that aliased 17 back to a bank-mate would still make a sound and still pass a bare does-it-change test; 🚨 **[2b] THE DECISIVE ONE** — with E's source at 17, retuning OSC A +12 st moves E's spectrum **12.01 dB** and retuning OSC F by the same +12 moves it **0.00 dB**, so the modulator is A and not F; 🚨 **[3] fb523's law across the wall** — osc A's LEVEL at 0 (inaudible) and the FM on E still moves **9.85 dB**, which only holds because the cross-FORCE travels (a source cannot know anyone is listening from another voice object); [4] it goes both ways (**7.07 dB** on A ← E); [5] the board is INERT unarmed (two renders null at **−240 dB**, and a voice only claims a row when `anyWanted` says someone is listening, so an unused patch pays one comparison). ⚠️ **[2b]'s FIRST CUT WAS A CONFOUND AND READ RED**: both candidates sat at Level 0.7, so retuning F moved the SUM (3.48 dB) whether or not it modulated anything. Both are at Level 0 now and only the carrier is audible — which is safe for exactly the reason [3] proves. ⚠️ **THE TWO DIRECTIONS ARE NOT THE SAME**: `processBlock` renders bank 0 then bank 1, so E–H ← A–D reads THIS block (exact, one sample delayed like every in-bank source) while A–D ← E–H reads the row before bank 1 overwrites it — a clean ONE BLOCK delay line (10.7 ms at 512/48k), contiguous across the boundary, no dropout. That asymmetry is what the single buffer buys, and it is why nothing reorders the banks: the order is what keeps a patch with no cross route bit-identical. `clang++ -O2 -std=c++17 Tests/au_crossblend.cpp -o /tmp/auxb -framework AudioToolbox -framework AudioUnit -framework CoreFoundation -framework CoreAudio && /tmp/auxb` |
| `_tp53_crossblend_gate.js` | **tp53 — A THROUGH H IN THE BLEND MENU** (page; the DSP half is `au_crossblend.cpp`). Max: "wanted: the LFO blend menu's scroll list, A through H." The Source section was `__oscBankLetters(o)` — the carrier's own four — so from E you could only reach F, G and H. It is all eight now, in the SAME contained scroll window the LFO shapes have used since fb226 (three rows visible, wheel for the rest, canonical `syn-ctx-item` rows). 10 bars: 🚨 **[0] the C++ `srcs` StringArray and the page's `NSRC` are both 21, read OFF DISK** — fb373's law, and it bites twice here because NSRC both WRITES (`v / (NSRC-1)`) and READS BACK (`× (NSRC-1)`), so a mismatch silently renumbers every saved blend source; [0] also pins the four names to the PAIR form (`Osc A/E` …), which is forced: E–H's WSLOT parameters are clones of B's and share ONE list, so "Osc E" would be a lie on half of them. [1] from OSC E the rows are A,B,C,D,F,G,H (itself skipped — that is Self) in a 68 px `overflow-y:auto` window; [2] picking "Osc A" from E writes **17/20** and picking the bank-mate "Osc F" writes **1/20**, not 18; 🚨 [3] **the index is BANK-RELATIVE** — "Osc E" from A writes the SAME 17, one index space for both banks; [4] the pill's face reads `FM(E)` on A, so it names the letter it is actually reading. `NODE_PATH=Tests/node_modules node Tests/_tp53_crossblend_gate.js` |
| `_tp53_list_gate.js` | **tp53 — MAX'S 2026-09-19 LIST** (page; extracts the C++ overlay from PluginEditor.cpp the way tp49's gate does). [1] the Chop footer sits where the PATCHER's sits — measured on both pages in one run: the Patcher pins `#footer` absolutely to the bottom of the 656 box (`body.tp-open #footer{position:absolute;bottom:0}`), so its output slider rides 624.5..640.5, while the Chop page left the footer in flow behind a 272 px panel at 592..640 with 16 px of `#plugin` ground showing UNDER it. The panel takes those 16 px back (272 -> 288). [2] the sample library IS the house two-pane browser (`openTwoPaneBrowser`, 384 px) built from the same two sources in the same order as `openSampleBrowser` — only the door differs (`loadSampleFromPath`, per layer). It also catches a live bug: `libIdx` became an ARRAY in tp50 and the ‹ › handler still did `libIdx + d`, which stringified the whole array and handed `libLoad` a NaN — **the arrows have been dead since tp50** and the tp49 gate could not see it because `__tiLibStep`, the hand it uses, already stepped the per-layer index. [3] the chop's right-click panel wears `.tpb-panel`'s OWN glass (rgba(22,20,34,0.72), blur 20 saturate 1.4, radius 12) — the index-card hole-punch and ruled paper lines are gone; the INDEPENDENT row is GONE FROM THE DOM (tp52 only hid it, and the FX grid was still greyed by the switch nobody could see — a chip arms independence itself now); and the ADSR graph is four labelled house-notch sliders with numbers (2 px rail at .16 white, white fill, 2x9 white bar). [4] 🚨 A TALL MENU ON THE PATCHER STOPS AT THE FOOTER'S TOP. Max, with a screenshot of the LFO card's menu: "look at the word EXTEND" — its last rows sat ON the output slider. `showMenu` clamped to `#syn-panel`'s own bottom (656), but on the Patcher the output strip is pinned OVER the panel's last 48 px at z 31, and `.syn-ctx-menu` at z 2147483646 paints straight over it. The floor is the footer's TOP. Asserted with a 41-row menu: it ends at 608 and SCROLLS (`maxHeight` derives from the same figure), which is the fb275 law applied to the one edge that was lying about where it was. [6] a stretched chop's time-mode letter is transparent white, not the purple sticker. `NODE_PATH=Tests/node_modules node Tests/_tp53_list_gate.js` |
| `_tp49_chop_gate.js` | **tp49 — THE CHOP PAGE WEARS THE HOUSE** (extracts the C++ overlay from PluginEditor.cpp itself). Max: "CHOP mode, rename MIX to CHOP... header, middle and footer match the patcher and synth background... buttons the same code as the 4 MODES, purple outline / white inside... One-Shot... key box and BPM transparent, BPM from the DAW... remove SEQ, the plus button, Sync, the XY pad tracker and the dice... arm/unarm the CHOP MODE so it stops any synth playing... SEQ → the sample library with the arrows... getting out of the mix menu is BROKEN, it leaves traces". [1] the header says CHOP and body.chop-open; [2] Arm + sample library present, SEQ / SYNC / play / dice / BPM lock gone; [3] house case (One-Shot, Pitch, Jitter, Layer, RR, Random, Keytrack, Velocity, Stems, Export All 4…); [4] a selected pill = purple outline, nothing filled, transparent key box, panel #1A1A2E; [5] SYN from the page closes it (a `tipanelchange` listener — the old wiring only knew EQ/DLY/MOD); [6] the Arm lights and writes `setTiArmed` (C++: `TI_ARMED` bool param; while on, the synth's MIDI loses every note-on — `armedMidi_` — so the sampler owns the keys; unarmed = untouched, AU null bit-identical); [7] the layer A-D status row is outlined. Also: `getHostBpm` polled each second while open (the DAW's tempo), the chop's right-click panel wears the house glass, the Noise browser entry is ONE `Noise` (the second add becomes Noise 2), the capture strip reads `CAPTURE - OFF` when capture is off. [9] tp50 — the sample library is PER LAYER (Max: "add a one-shot to A, a different one to B, another to C and D, and it doesn't affect the other ABCD"): each layer keeps its own place in the library and its own name, and every load lands on the selected layer (three into B, two into C, none into D); [10] coming back to a layer shows ITS sample again — no name wiped by the switch (a name is only dropped on `getLayerHasSample`'s word, never on the hero's transient `has-sample` class, and never within 3 s of a load). tp50 also gates the V1 singleton `loadedSamplePath` to layer A — writing it while editing B/C/D made a V1-state project reopen with D's one-shot sitting in A. tp52 adds: [4] header, footer, waveform and panel all wear ONE ground (`#plugin` #1A1A2E, nothing painted over it — the Chop page never claimed the synth page's ground, so its header and footer wore #232340 over a #1A1A2E panel); [9] no glass boxes (the strips and the two right tiles are the page, told apart by one hairline each), the sliders are the house notch (2 px, WHITE, the Layer morph included), A-D wear the synth page's 16 px chip, M/S are small, ARM sits beside D, the STEMS word is gone, and the library name is a fixed 124 px box so a long name can never reach SLICES or the BPM. `NODE_PATH=Tests/node_modules node Tests/_tp49_chop_gate.js` |
| `lane_pills_gate.js` | **fb593 — A LANE CARD'S PILLS OWN THE WHOLE COLUMN** (page layout). Max, on a Widen inside the Splitter: "these two pills leave deadspace at the bottom, we dont need this deadspace ... retrig and mono should be stacked and the pills should extend to fill up that space." THE CAUSE: the right column has two forms — an ordinary card is [pills row][route row], a LANE card (a device inside a Splitter band) is pills ONLY, because its band does the routing. Nothing replaced the route row, so exactly HALF the column was empty: measured column 56.2 px, pills 28.1, **28.1 dead below**. Not even a new design — the lane-card comment in the builder already said "its pills fill the column"; it was never expressed in CSS. 5 bars: the ordinary card is UNTOUCHED (route row still there, pills still side by side — the bar that matters most, since 12 devices are not in a lane); a lane card still has its pills AT A REAL SIZE; zero dead space above and below; stacked, each the full column width (143.6 vs 67.9 before); and a ONE-pill lane card (eqz / cmp / ott) fills too. ⚠️ BAR 1 EXISTS BECAUSE AN EARLY ATTEMPT COLLAPSED THE PILLS TO 2 px TALL — which measures as "no dead space" and is worthless. Any bar that only checks for absence needs a companion that checks for presence. ⚠️ AND THE FILL IS ABSOLUTE, NOT flex-grow / 1fr / height:100%: the column takes its height from `align-self:stretch`, resolved AFTER its children are sized, so it is not definite from the inside. All three of those were measured leaving the row at 23.55 in a 41.55 column before `position:absolute; inset:0` settled it. Stacking is safe for every device that can be a lane card: the chassis row is `d.pills.slice(coreN)` = at most 2 (Utility's five keep three lamps in its core; the Splitter, the only 3, is excluded from lane cards by `d.core!=='spl'`). Proven able to fail: bars 2, 3 and 4 go red on the pre-fb593 page. |
| `back_spectral_gate.js` | **fb594 — THE SPECTRAL WINDOW SHOWS ONLY WHERE IT IS ALIVE** (page layout). Max: "if sample doesn't have low and a high ... then the low and the high button needs to get taken away. Like for granular, if there's no spectral for granular, then the low and the high button needs to get taken away ... FM, we can keep that because we have spectral for FM." THE TRACE: Lo/Hi reach the audio through exactly ONE line — `PluginProcessor.cpp:1615`, `buildFromSpec (SpectralMorph::apply (*srcSpec, mode, amount, lo, hi))` inside rebuildMorphIfNeeded — and `grep -rn "SpectralMorph::apply" Source/` returns that one call site. Its output is the per-osc MorphSlot, reaching a voice via wavetableForOsc → `currentWavetable_` → `blendA_` (SynthVoice.h:3742), which is read by exactly TWO engine cases: `Engine::WT` (SynthVoice.h:4200) and `Engine::FM` (SynthVoice.h:4289). The other five are a hard `sAu = 0.0f` at SynthVoice.h:4308-4312. So 5 of 7 engines carried two inert knobs. 🚨 HARMONIC IS DEAD **INCLUDING ITS TABLE FAMILY**, which is the one that looks live: fb588 gave HARM a 7th family whose additive recipe IS a wavetable, but it bakes through `oscSourceSpec` → `specForPreset` (PluginProcessor.cpp:1516) — no morph, no cut. Two independent traces agreed before anything was hidden. ⚠️ THE PARAMS ARE NOT DELETED, ONLY THE CONTROL IS HIDDEN — they are identity at their defaults (Lo=0.0/Hi=1.0), live on WT/FM, and mod destinations 1846..1853 pinned by a static_assert in SynthModConfig.h:265-266; deleting them would renumber the matrix and break every saved patch, and `spec_cert.cpp` would go red. ⚠️ AND THE GRID IS RE-DECLARED, NOT JUST THE KNOBS HIDDEN (the fb586 law): bar 3 measures column spacing and row midpoint, because "the knob is gone" and "the row is right" are different claims. Proven able to fail BOTH ways: on the shipped page bar 2 goes red (all five dead engines show 7); on a naive fix that hides the knobs without re-declaring the grid, bar 3 goes red with the row pulled **43.72 px off-centre** — the two dead columns hanging off the right edge. ⚠️ `.back-only` is `display:none` unless `.swapped`, which no other UI gate ever adds — bar 0 refuses to assert on an invisible row. Regression: `osc_view_gate` 9/9, `all_menus` 42/0, `harm_wtmenu_gate` 5/5. |
| `harm_header_gate.js` | **fb595 — THE HARMONICS HEADER NEVER COLLIDES, THE ARROWS ARE CENTRED, NOTHING MOVES** (page layout). Max, with a screenshot reading "OSC TaHarmonicKeel ‹ PLUTO 2 - JNO … ›": "it still collides ... because our table names are longer ... the arrows are way taller than the names ... make sure the names don't overlap no matter what ... it gets cut off really early ... dot, dot, dot ... Nothing moves. Everything has to stay fixed ... run a bunch of tests on it." TWO INDEPENDENT FAULTS. (1) THE COLLISION: `.device-label-row` (left:17) and `.device-corner-preset` (right:14) are BOTH `position:absolute` in one 16 px band — neither is in the other's flow, so when the corner outgrows the room beside "OSC · Harmonic" it grows LEFT straight through it: no reflow, no clip, superimposed glyphs. On Table the corner carries THREE name-chips (Table + sculpt + the wavetable name) where every other engine carries one, because fb588's comment "the family/sculpt wraps stay hidden" was never true — the `:not()` chain limits what that rule un-hides and hides nothing. Measured on the shipped page: clearance **−63.45 px**. (2) THE ARROWS: `.harm-table .wt-nav { display:inline-block }` on a FLEX child is BLOCKIFIED to `block`, discarding the align-items/justify-content centring of the canonical rule — glyph inset 0.00/6.03 (should be 3.01/3.02), riding 3.5 px high. THE FIX (CSS only, scoped to `.engine-harm.harm-table`): `inline-flex` for the arrows; the corner gets a FIXED width (`calc(100% − 124px)`, 6 px of clearance reserved because WebKit and Chromium disagree on sub-pixel advance), every child pinned `flex:0 0 auto`, and the NAME wrap alone `flex:1 1 auto; min-width:0` — it absorbs all the slack, so Table · sculpt · ‹ and › · A · + sit at the same pixels for every table in the bank and only the name's text changes; the name fills its slot centred and truncates through the ellipsis `.preset-display` already had. ⚠️ THE COST: with Terrace the slot is ~20 px, with Keel ~40 — the header cannot hold three chips + a name in 299 px and the name is the only thing that can give without hiding a LIVE control (Sculpt is audible on Table, `applySculpt` has no mainMode guard). Folding Table into the wavetable menu (no seventh category ⇒ no third chip) gives the name its 108 px back; this makes the header legal until then. ⚠️ THE 1 px NUDGE IS MAX'S: `.preset-display { left:1px }` — "nudge the name text RIGHT 1px (between the ‹ › selectors)" — is deliberate and shared with every engine, so bar 4 measures the SLOT (4/4) and requires Table's label offset to EQUAL the wavetable engine's, not to be zero. 7 bars over **126 states** (7 families × 6 sculpts × 3 name lengths) plus the wavetable engine's arrows measured in the same run as the reference: no overlap + ≥4 px clearance · the name gives, the chips don't · arrows centred to the reference's exact numbers · symmetric · NOTHING MOVES (per sculpt, every rect but the name's text identical across names; label row and A identical across all 126) · other engines untouched. WHY fb588's OWN GATE PASSED WHILE FOUR WORDS PAINTED ON TOP OF EACH OTHER: `harm_wtmenu_gate` asserts `display !== 'none'` and never measures a rectangle. Proven able to fail: the shipped page goes red on bars 1, 3 and 5. Regression: `fb560_wtarrow` all bars, `harm_wtmenu` 5/5, `fm_wtpage` 6/6, `osc_view` 9/9, `all_menus` 42/0, `fx4_ui` 131/0. |
| `harm_cluster_cert.cpp` | **fb599 — A CHORD MUST NOT MUTE ITSELF** (DSP, Pattern A). Max: "a silent spike every now and then whenever I press more than four or five notes ... it may spike out the audio for a second and then come back ... almost like glitch mode is on." THE CAUSE: the 640-partial pool shared by every additive/Resynth/Modal voice was handed out FIRST-COME-FIRST-SERVED (`room = max(24, cap - used)` asks "what is left", not "what is my share") and `renderBankAdd` then RAMPED THE WHOLE BANK TO SILENCE once the counter hit the cap. Measured on a C3 cluster at Partials 1.00: voices 1-4 take 173/163/154/145, voice 5 is clamped to the 24-partial floor (a hard timbre step = "chopped"), voice 6 renders at **−209.8 dB — the note is gone**. Worse, JUCE gives a new note the lowest free slot and voices render in slot order, so a NEW note lands AHEAD of sounding ones and takes their budget: **four held notes at 0.113 RMS drop to 0.00000 for 61/61 blocks** the instant two lower notes arrive, and return when a key is lifted. THE FIX: a per-block CENSUS — each anchor bumps a counter in prepareBank; last block's total is this block's divisor, so every bank takes an equal share decided BEFORE anyone renders. The render-time mute is demoted to an emergency valve at 4× the cap (a leaked counter is what it was really for). Thin floor 24 → 6 with the 2 never-thinned root slots subtracted from the share, so the pool is honoured instead of overshot (2024 partials against a 704 ceiling at 16 notes × unison 4 → within bar). 25 bars: G1 no voice of a 16-note cluster more than 12 dB below its solo self (3 registers × 4 Partials) · G2 pool ceiling ≤ 1.10× cap · G3 solo-note identity bit-identical · G4 a Resynth oscillator takes 400 of the 640 first and HARM still survives. RED on shipped via `-DHM_SHIPPED_GOVERNOR`: −206.8 to −213.4 dB across every register and Partials setting. ⚠️ **THE A-vs-B ASYMMETRY WAS NOT A CODE PATH** — every A/B identifier in SynthVoice.h was rewritten to a common name and diffed: only names and render order differ, and the order serves A FIRST. It is a per-oscillator PARAMETER difference: the mute threshold is a steep function of Partials (C3: 0.35 → never, 0.50 → note 11, 0.70 → note 6, 1.00 → note 3) and `SYN_OSC_x_HARM_COUNT` is per-oscillator and is NOT carried by the wavetable preset menu, which writes only `WT_PRESET`. ⚠️ **THE CENSUS DETECTOR MUST REPORT** — this cert reaches the census through a `std::void_t` detector so one file builds against both headers; the engine's method was briefly named differently and the detector silently skipped it, so every ceiling number measured a fix that was not running. It now prints `census wired: YES/NO` and says so. |
| `harm_churn_cert.cpp` | **fb599 — CHURN DOES SOMETHING** (DSP, Pattern A). Max: "the churn knob on the harmonics engine don't do anything ... please make the churn do something." CONFIRMED DEAD, not merely quiet: `churnMul` had FIVE readers (Tide, Terrace, Fan's orbit, Shine's ghost detune, Braid's wobble) and every one sits behind a knob whose factory default is 0, while KEEL — the sculpt Max keeps — never reads it at any Carve. On Table + Keel at the registered defaults, churn 0 vs 1 measured **0.000 dB with a BIT-IDENTICAL render fingerprint** on three real tables. And pinning Keel (see `harm_tables_cert`) removes Tide and Terrace, the only two sculpt readers — Churn's reach SHRINKS. THE FIX: on the Table family CHURN is what its name says — each note scans the wavetable FRAME on its own, from wherever Hue put it. Per-VOICE and note-relative, which is the one axis the mod matrix cannot express: an LFO on HarmHue resolves once per BLOCK per OSCILLATOR and moves the whole chord in one phase. Measured swing 12.0-42.7 dB at 25 %, flux(100 %) = 3.93× flux(50 %), flux(15 %) = 4 % of full (no dirt at the bottom), 0.0000 cents of detune, no click on a mid-note step (1.07× steady), and prepareBank is 12.7 ns/block FASTER. CHURN = 0 is bit-identical on all eight fingerprints. ⚠️ Its factory default drops 0.5 → 0: the knob used to be inert at any value so 0.5 was free, and leaving it there would put every existing Harmonic patch in motion unasked. ⚠️ **THE HEADER IS DETECTED, NOT DECLARED** — the banner was an `#ifdef` the builder had to pass by hand; forget it and the cert prints SHIPPED, measures 0.000 and reports CHURN dead on a header where it works. It now asks the type whether `HarmParams::tableGridAmp` exists. ⚠️ The voice dereferences the Grid inside prepareBank, so `rebuildHarmTableIfNeeded`'s retireCooldown now guards AUDIO, not just a picture. Mutation `-DHARM_CHURN_DEAD` → 7 of 11 red. |
| `harm_tables_cert.cpp` | **fb599 — TABLES ONLY** (DSP + UI). Max: "I only want to use tables for this ... I don't want the families anymore ... only tables, so I need the header cleared ... make it look like wavetable." The gather pins `mainMode = 6` and `sculptMode = 0` (Keel — "if we have to use one then we have to use keel", and it is the registered default); both choice params stay REGISTERED with all their entries, so nothing renumbers and no saved state or automation lane moves. 🚨 **THE LOAD-BEARING EDIT IS THE BAKE GATE**, and it is not obvious: `rebuildHarmTableIfNeeded` refused to bake unless the STORED mode was 6. Pin the gather and leave that gate and a patch saved on Blade never gets a grid — the not-yet-baked fallback fires every block and HARM plays Blade for ever, measured **19.25 dB** from the table it should be playing. Mutation `-DHM_MUT=1` keeps the old gate and reds bars 2 and 3. 5 bars: every stored family index resolves to the table (worst 0.000 dB) · a Blade-saved preset plays its table · the sculpt pin is a no-op at the registered Carve (0.0000 dB — applySculpt returns before it reads sculptMode) · and it is 18.13 dB with Carve up, which is this item's ONE audible regression surface. The header: corner children 7 → 5, width 206 px frozen → 112.98/186 px, name slot 20.42 px (Terrace) → the wavetable engine's full 34.98-108.00 px, worst clearance 6.67 → 26.67 px — fb595's fixed-width clamp is DELETED because the two chips it was containing are gone. ⚠️ A third defect neither the brief nor I had measured: `.harm-table` was written only when the stored family was 6, so a HARM oscillator on any other family had **no name and no arrows at all** at boot. `harm_wtmenu_gate` bar [3] and `harm_waterfall_gate` bars [3]/[4]/[5] are INVERTED by this change — they asserted the retired Table-only surface and now assert its opposite, with the reason in each. **fb601 — THE CHURN DIAGNOSTIC NO LONGER READS AS "CHURN DOES NOTHING".** This cert printed `Churn 0.00 vs 1.00 -> 0.0000` on the registered defaults, which is the first line a future session would find and exactly the wrong conclusion. That zero is STRUCTURAL: since fb600 the knob's entire effect is the per-voice frame drift at `HarmonicEngine.h:308`, gated on `tableGridAmp != nullptr && tableFrames > 1 && tableStride > 0`, and this cert's synthetic table is a SINGLE RESOLVED FRAME with no grid — `driftOn_` is false and the drift cannot run. The line now says so, and a second diagnostic underneath it measures the real path against a 16-frame grid (with `tableAmp` set to the frame the processor would resolve at this Hue, so the two sides differ by the DRIFT and nothing else) sampled at 8 instants over ~1 s, because the drift is a sine and a single snapshot is 0 by construction at some phases: **churn 0.00 vs 1.00 → peak 26.7724, mean 5.5677; vs 0.25 → peak 16.3090.** A zero THERE would be a verdict. |
| `dly_spread_cert.cpp` | **fb601 — THE DELAY SPREAD DEFAULT, MEASURED THROUGH THE REAL `DelayEngine`** (offline). fb600 moved the registered default 0.60 → 0.05 and proved it in a `/private/tmp` cert that evaporated; nothing in `Tests/` referenced `DLY_SPREAD` at all. The tap position is MEASURED, not transcribed: an impulse through the shipped `processSample`, with the in-loop band edges and the output tilt made EXACTLY identity (`onePole(0)=0`, `onePole(≥0.49·fs)=1`, `toneTilt(0.5)=x`) so the response IS the two-sample linear interpolator and its amplitude centroid is the fractional tap. The registered default is READ OUT OF `PluginProcessor.cpp` at run time — move it back to 0.60 and bar [2] reds without anyone editing this file. 6 bars at 120 BPM / 1/4 (base 500.000 ms): the R tap moves monotonically (0.000 → 5.235 → 8.735 → 104.982 → 174.982 ms at 0.00/0.03/0.05/0.60/1.00); the default is ON THE GRID (+8.735 ms, inside the 10 ms Haas fusion window, +34.94 ms by repeat 4 vs a 125 ms 1/16 — the 0.60 it replaced is 12× outside both); 🚨 the RANGE never quietly shrank (1.00 is still +35.00 %); the measurement agrees with `DelayEngine.h:107` to under ONE SAMPLE; and 0.00 is exactly mono. ⚠️ TWO THINGS THE HARNESS HAD TO GET RIGHT OR IT WOULD HAVE LIED: `prepare()` writes `spreadCur = spreadTgt` **after** its own `updateCoefficients()`, and `updateCoefficients()` reads `spreadCur` (the SMOOTHED value), never `spreadTgt` — driving it either wrong way reads **R−L = 0.000 ms at every spread**, i.e. "Spread is dead", convincingly. ⚠️ A REAL RESIDUAL, DOCUMENTED NOT CHASED: the R tap lands a fixed fraction of a sample short, because the float32 glide stalls once `smCoef·(delTgt−delCur)` drops below half an ulp of `delCur`. Predicted `(ulp/2)/smCoef`, measured over four delay times: 125 ms → 0.178 (pred 0.176) · 250 → 0.356 (0.352) · 500 → 0.713 (0.704) · 1000 → 1.426 (1.407). At a 500 ms beat that is 0.0149 ms, three orders below the bar it sits under. Mutations: `-DDLY_SPREAD_MUT=1` (pretend the processor still registers 0.60) → [2] red; `=2` (taper trimmed to +10 %) → [3][4] red. |
| `dly_spread_state_au.cpp` | **fb601 — THE DELAY-SPREAD MIGRATION, THROUGH THE REAL `setStateInformation`** (installed AU). The half that touches Max's ALREADY-SAVED projects, and the only half that can silently eat a setting he chose on purpose. Transcribing the predicate into a cert proves the arithmetic and nothing about whether the branch is REACHED — the fb373/fb469 failure class — so this drives the shipping plugin: `kAudioUnitProperty_ClassInfo`'s `jucePluginState` CFData **is** the `getStateInformation` blob (JUCE `copyXmlToBinary`: magic `'VC2!'` + uint32 length + plain XML + NUL — **not** compressed on this JUCE, verified), so a synthetic PRE-fb600 project is built by removing the `dlySpreadRebased` marker from the root and storing 0.60, pushed back, and the parameter read out with `AudioUnitGetParameter`. The migration's own constants (0.60f · 1e-4f · 0.05f) are parsed out of `PluginProcessor.cpp`, so a retune moves this line. 7 bars: the constants read; a fresh save carries the marker; a pre-fb600 project migrates (0.600 → 0.050); the 5 POOLED instances migrate too; 🚨 it is NOT re-migrated (a deliberate post-fb600 0.600 survives the next load); 🚨 the knife edge — 0.59900 KEPT · 0.60000 SNAP · 0.60005 SNAP · 0.60100 KEPT, every one through the real branch; and nothing else moved (`SYN_DLY_FEEDBACK` parked at the same 0.600 in the same blob comes back untouched). ⚠️ Bar [6] checks the BLOB, not the AU parameter: the AU exposes every float on a 0..1 scale and only Spread's own range happens to BE 0..1 — Feedback stored 0.600 reads back 0.409091 on the AU scale, which is a units mismatch and not a migration. Mutation: `DLY_MIG_MUT=1` keeps the marker on the synthetic blob → [2][3] red. |
| `harm_tbl_state_au.cpp` | **fb601 — THE HARM_TABLE MIGRATION, THROUGH THE REAL `setStateInformation`** (installed AU). fb601 gave Harmonics its own table param, default **Prophet Saw (4)**, where the old shared `WT_PRESET` defaults to **Sine (0)** — so without a migration every already-saved Harmonic patch comes back on a DIFFERENT TABLE. 🚨 `PluginProcessor.cpp:15149` said *"fb601-MIGRATION-BEGIN — the cert slices these lines verbatim; keep the markers"* and **nothing sliced them**: `grep -rn "HARM_TABLE\|harmTableSplit" Tests/` returned nothing. A comment promising a cert is a promissory note that reads green to the next session; bar [0] cashes it by slicing and PRINTING the shipped 29 lines between their own markers. 7 bars: the migration sliced (never transcribed); a fresh save carries `harmTableSplit="1"` **and** the four `HARM_TABLE` children; the registered default is Prophet Saw on all four; 🚨 a pre-fb601 blob (marker removed, children dropped) comes back with `HARM_TABLE == its own WT_PRESET` on all four — **7/16/2/31**, four DIFFERENT indices so one stuck value cannot fake it; `WT_PRESET` itself is untouched; the state that comes back OUT carries the marker again so a reload cannot re-migrate; and 🚨 a DELIBERATE post-fb601 choice is never re-seeded — **including Sine (0)**, which is also `WT_PRESET`'s default and the obvious wrong thing to treat as a "pre-fb601 tell". 🔒 The instance is reused across bars, so before every pre-fb601 push the four params are parked on a **SENTINEL (1/3/5/9)** through a marker-carrying blob and the sentinel readback is printed: a parameter that merely STOOD STILL cannot pass. Mutation: `HARM_MIG_MUT=1` keeps the marker on the synthetic blob → the branch never fires and **[3] goes red (4/4/4/4 — the registered default — instead of 7/16/2/31)**, while [1][2][4][5][6] stay green. |
| `au_state_blob.h` | **fb601 — the shared ClassInfo / state-blob plumbing** (a header, not a gate). Not a new abstraction: it is `dly_spread_state_au.cpp`'s plumbing lifted **verbatim** when `harm_tbl_state_au.cpp` turned out to need the identical ~200 lines — RECYCLE says reuse it, not fork it. Gives both migration certs `chk()`, `struct AU` (open the installed `Tern`/`Wvcr` AU, read/set params by name, and read **and write** the state blob via `kAudioUnitProperty_ClassInfo` → `jucePluginState`, which IS the `getStateInformation` blob: JUCE `copyXmlToBinary` = magic `'VC2!'` + uint32 length + plain XML + NUL, **not** compressed on this JUCE), the XML surgery (`setParam`/`getParam`/`dropParam`/`hasParam`/`hasProperty`/`dropProperty`), and `sliceMarked()` — print the shipped migration's own lines between its own markers instead of transcribing them. Neither half knows about Spread or about HARM_TABLE. **fb602 — EXTENDED, still not a gate.** Three additions, all lifted rather than invented: `midi`/`render`/`note` + `rmsDb` come **verbatim** from `harm_table_au.cpp:114-136`, because a state cert that only reads parameters back cannot tell a restored SAMPLE SLOT from an empty one — the slot is a buffer, not a parameter, which is exactly how bug (c) stayed invisible; and `setRootAttr`/`getRootAttr` (+ `xmlEsc`/`xmlUnesc`), because `hasProperty`/`dropProperty` could only TEST and DELETE a root attribute, and an fb602 restore cert has to PUT one there — an idle AU's blob carries no `oscSamplePath0`, no `cardStates` and no `convIRRaw1`, so the cert must synthesise the blob a real session would have saved. `chk()` also records every failed label now and `summary()` prints a **FAILED BARS** list: 🚨 a bar in `headless_restore_au` flaked exactly once in ~25 runs during this commit and could NOT be identified afterwards, because the only thing kept was `13 passed, 1 FAILED`. It did not reproduce in 20 further back-to-back runs or under 8 saturating `yes` processes, so it is recorded as unexplained rather than fixed — and the next occurrence will name itself. |
| `headless_restore_au.cpp` | **fb602 — A PROJECT THAT NEVER OPENS TERRAIN'S WINDOW MUST STILL SOUND** (installed AU). Bug (c): until fb602 the ONLY thing that filled a sample slot after a project load was `TerrainUiCore`'s constructor `MessageManager::callAsync` loop, and a miss inside it was a bare `continue` — open a project, hit render, never open the plugin window, and every Sample/Granular/Resynth/Modal oscillator played SILENCE. The cert is a host that never asks the AU for a view: 🚨 bar **[0b] greps its own comment- and string-stripped source PLUS `au_state_blob.h`** for `kAudioUnitProperty_Cocoa*` / `GetUIComponentList` and finds none, because "no editor" in a comment is not a control. Measured on the installed AU at note A3, **fb601 → fb602**: SAMP restored **−200.00 → −10.45 dB**, GRAN **−200.00 → −29.41 dB**, layer-only `<layer sourcePath>` **−200.00 → −11.48 dB**, against a harness-alive WT bar that reads −12.09 dB on both (so a silent measurement can never be blamed on a dead render loop) and a −200.00 dB floor that is `rmsDb`'s 1e-20 clamp, i.e. literal digital silence. 🚨 **[4] IS GREEN ON THE BROKEN BUILD ON PURPOSE** — `oscSamplePath0` re-emits byte-identical on fb601 too, so a cert that checked the PATH would have certified the bug; the path always survived, the SLOT never did. 🚨 **[3b] proves PROVENANCE, not just loudness**: swap the named wav 220 Hz → 880 Hz and the zero-crossing rate moves 371 → 1482/s, ratio **3.99** — audio that is not sourced from the named file cannot track that. And the ledger is READ, not assumed: an AUv2 loads in-process, so the cert captures fd 2 around `writeXml` and reads the shipped `TERRAIN_RESTORE_PROBE` line — `filled=1 misses=0` on a good path, `misses=1 … "why":"dropped-bytes-not-embedded"` for a `mem:` ref (bug (b)'s marker) and `"why":"file-missing"` for a dead path. 14 bars. Mutation `TI_RESTORE_MUT=1` saves the patch WITHOUT the path attributes (the pre-fb602 hole, from the input side) → **[3][3b][5][7][9][9b][9c] red, 7 pass / 6 FAIL**. |
| `state_holes_au.cpp` | **fb602 — THE TWO THINGS `getStateInformation` NEVER WROTE DOWN** (installed AU). HOLE 1 `cardStates_` — what `index.html:33710` calls "ONE truth" for the FLOW cards' multi-slot chain; `grep cardStates_ PluginProcessor.cpp` returned NOTHING before fb602, so it survived pop-out, dock-back and an editor reopen and was destroyed by every DAW project reload. HOLE 2 the user convolution IR AUDIO — `loadConvIRFromFile` keeps only `f.getFileName()`, so there is no path to reload from and an un-embedded IR is a LOST IR. 🚨 **THE TRAP THIS FILE ALMOST SHIPPED, WRITTEN INTO ITS HEADER**: a plain put-X-in / read-X-out round trip **PASSES ON THE BROKEN BUILD** — measured, the first draft was green against the fb601 binary whose `strings` contains neither `cardStates` nor `convIRRaw1`, because `setStateInformation` ends in `apvts.replaceState` and `getStateInformation` begins with `apvts.copyState()`, so a root attribute NOBODY READS is echoed through verbatim for ever. Every bar now carries a DISCRIMINATOR: `cardStates` is sent with its keys REVERSED and a stray space, and `cardStates_` is a `std::map` re-serialised by `juce::JSON`, so only a real round trip comes back sorted and unspaced; `convIRRaw1` is sent with a LIE in `n` (1, not 2048) and NO `R` at all, and only the real `setConvIRRawFromJson` → `convUserIrL_/R_` → `getConvIRRawJson` path recomputes `n` and duplicates L into R. 🚨 **[2] is the worse-than-the-bug bar**: a blob with NO `cardStates` (every pre-fb602 project) must be a NO-OP and never a clear — a restore that wiped the FLOW chains would be strictly worse than never having saved them; [2b] does the same for an explicitly empty string. [4] pushes an IR at Reverb **4**, a POOLED slot whose `convEngineFor` is still `nullptr` inside `setStateInformation`, because the old `if (eng == nullptr) return;` was a silent total loss there. [0] proves the empty guards: a virgin 179 405-byte blob carries none of `cardStates`/`convIRRaw1..6`. The JUCE base64 is reimplemented exactly (`"<count>." + 6-bit groups read **LSB-first**` against `.A-Za-z0-9+` — *not* standard base64, and a standard encoder would still have passed because `fromBase64Encoding` only needs the dot) and is certified by the plugin re-emitting it byte-identical. Cost measured, not quoted: 1.34 base64 chars per raw byte ⇒ **2.94 MB** per loaded slot at the 6.14 s cap (the source comment says 3.07 MB). 7 bars. Mutation `TI_HOLES_MUT=1` → **[1][3][4] red**. |
| `cert_native_dupes.py` | **fb602 — TWO NATIVES CANNOT SHARE ONE NAME** (source gate). `juce::WebBrowserComponent::Options::withNativeFunction` does `copy.nativeFunctions[name] = std::move(callback)` behind `jassert (copy.nativeFunctions.count(name) == 0)` (`juce_WebBrowserComponent.h:323-324`) — the assert is compiled out in Release and **the LAST registration silently wins**. `deletePreset` was registered TWICE in the MAIN chain, so in the DOCKED editor every card/FX preset ✕ ran the PATCH deleter with a card id: `static_cast<int>("gli") == 0`, `deletePreset(0)` early-returns under `numFactoryPresets`, silent no-op. It only ever worked in popped-out card windows. **Section A** parses every `withNativeFunction("…")`, groups by Options chain (string/char/comment-aware paren balancing) and fails on a duplicate within a chain. **Section B** is the other half: removing a collision does not make the NAME mean one thing, so it groups by name ACROSS chains and compares how each handler reads `args[0]` — int / string / num / bool / **dispatch**, resolving a bare identifier callback to its `static` function body. 🚨 **BOTH SECTIONS GO RED WITH NO MUTATION AT ALL** on `git show 5a2ba1c:…/PluginEditor.cpp`: A reports `DUPLICATE "deletePreset" x2: withNativeFunction@1097, withNativeFunction@1670` in a 260-registration chain, B reports `"deletePreset" reads args[0] as int@1670 · string@1097`. On the fb602 tree: 260 + 41 registrations, 0 collisions, 0 splits. 🚨 **`KNOWN_SPLITS` IS EMPTY, AND THE EMPTY IS THE STORY.** This gate's first draft carried a `deletePreset` entry declaring the docked ✕ still broken with a future commit named as its owner — a red bar with a note on it. It did not have to be: the two calls are unambiguous **by SHAPE** (card = `(String, String)`, patch = `(Number)`), so `PluginEditor.cpp:1766` dispatches on `args[0].isString()` and `index.html` needs no edit at all. `arg0_kind()` therefore reports `dispatch` FIRST and treats it as compatible with every other kind: a body that ASKS what shape `args[0]` is cannot be the failure, which was a body reading one shape while the page sent the other. NOT a duplicate of `card_natives_gate.py`, which asks the opposite question (is a native the card window CALLS registered there at all); neither can see the other's failure. Mutations: `TI_CERT_MUTATE=1` injects a synthetic duplicate into the parsed token stream → Section A finds 2; `TI_SPLIT_STRICT=1` drops the allowlist and additionally REQUIRES a dispatcher to exist, so a tree that reverts the shape dispatch reds. |
| `user_data_guard.py` | **fb602 — THE OWNER'S SAVED WORK, GUARDED BY A GATE INSTEAD OF A PROMISE** (real disk, reads only). fb602 moved `tiPresetDir` off a hand-built `userHomeDirectory/"Library/WavesCrate/…"` onto `terrainDataDir()` (`userApplicationDataDirectory`). On macOS `userApplicationDataDirectory` IS `~/Library` (`juce_Files_mac.mm:209`), which is why nobody noticed that the old spelling produced `C:\Users\<u>\Library\WavesCrate\…` on Windows instead of `%APPDATA%` (`juce_Files_windows.cpp:721`) — and why the macOS job is the boring one that matters: byte-identical, or 29 card presets are stranded. `tiCardSlug` was deliberately NOT widened (a draft did; it fixes 0 collisions and moves `cmp_fet 76`), and this gate PRICES that reverted trade on every run instead of trusting the comment. The card-id set is READ OUT OF `index.html`, not typed here: 5 extension-card ids (`presets({id:'…'`) × 16 `FX_PRESET_NS` prefixes × 115 type slugs — ⚠️ a deliberate SUPERSET (1845) containing ids the page cannot build; `preset_path_cert.cpp` enumerates the 122 real ones, and a superset finding nothing stranded is the stronger statement. 7 bars: the backup at `USER-PRESET-BACKUP-fb602` covers everything live **SHA-256 for SHA-256** (29/29, zero drift, `UserPresets.xml` identical) — verified, not trusted; 29 card presets and the 5 named patches all present; no folder stranded (0 ids disagree; the reverted widening would move 48 superset slugs and strand none); 🚨 **[2a] THE PYTHON MODEL HERE STILL MATCHES THE SHIPPING C++** — it reads the `tiCardSlug`/`terrainDataDir`/`tiPresetDir` bodies out of `PluginEditor.cpp` and reds if they stop matching what this file models, because a Python transcription of C++ is a second version of something that already exists and `preset_path_cert.cpp` is the authority. **That bar caught real drift within minutes of being written** — the slug had been reverted under it and this gate was still modelling the widened rule. Then: every populated folder still reached by the IDENTICAL string (`rvb_convolution` → `…/presets/rvbconvolution`); all 29 presets parse as JSON off the real disk; all 5 patches load with 79-80 attributes each. [6] states the Windows half symbolically and says plainly that nothing here proves it. ⚠️ **The first mutation control was BROKEN and the header records why**: it reverted the NEW slug to the old one, which made the gate agree with itself and go GREENER. It now re-roots `terrainDataDir()` at `~/Library/Application Support` — the most plausible way to mis-read `userApplicationDataDirectory`, the same CLASS of mistake as the bug — and `TI_DATA_MUT=1` reds [2] and [3] with all five folders named as stranded. |
| `noise_restore_au.cpp` | **fb602 — THE NOISE MODULE'S SAMPLE COMES BACK WITHOUT THE WINDOW** (installed AU). The third slot in bug (c) and the sharpest: the noise selection was restored by NO native code at all. `noiseSampleSelJson_` was read into the member by `setStateInformation` and then sat there until `index.html`'s `restoreNoiseSel()` ran on GUI OPEN (`index.html:37333`, which re-issues `loadNoiseFactory`/`loadNoiseSample` from JavaScript). No window, no JavaScript, no noise sample — the module fell back to the algorithmic type and the patch played a different sound. Both descriptor kinds are certified because they fail differently: `{"kind":"factory",cat,file}` resolves under `<userAppData>/WavesCrate/TerrainInstrument/Noise` (**fb605: that folder name is an ADDRESS on Max’s disk, not the product name — the product is `Terrain` now and the folder deliberately did not move**), `{"kind":"user",name,b64}` carries the audio in the patch. The factory file is FOUND on the real disk (`RoomTone/RoomTone_alarm-0_397787.ogg`), never invented. Measured: algorithmic **−13.82 dB** vs restored sample **−22.23 dB** — the bar is that they DIFFER, because "same sound" IS the fallback. The `TERRAIN_RESTORE_PROBE` ledger is parsed, not assumed (`filled=1 misses=0`), and [4] asserts that a patch with no selection reports nothing lost — a miss ledger that cries wolf is as useless as one that stays silent. 5 bars. Mutation `TI_NOISE_MUT=1` points the descriptor at a file that is not there → [2] red and the ledger names it: `{"slot":"noise","why":"factory-file-missing"}`. |
| `preset_path_cert.cpp` | **fb602 — THE OWNER'S 29 CARD PRESETS STILL RESOLVE, BYTE FOR BYTE** (real filesystem, real juce_core). 🚨 **IT COMPILES THE SHIPPING CODE, NOT A COPY**: `Tests/extract_helpers.py` slices `terrainDataDir` / `terrainSettingsFile` / `terrainWavetablesDir` / `tiCardSlug` / `tiPresetDir` / `tiSafePresetName` **verbatim** out of `Source/PluginEditor.cpp` at build time (with `#line` back to the original), so a change to those bodies is a change to what this cert measures — this is the authority that `user_data_guard.py`'s Python model defers to. 10 bars: the fb132 and fb602 root strings are byte-identical by `memcmp` over the raw UTF-8 (62 chars) and `memcmp` agrees with `operator==`; the presets root exists; all 5 on-disk card folders are slug FIXED POINTS; all 29 preset files resolve through `tiPresetDir + tiSafePresetName`; the shipping slug relocates 0 folders; and the courier is exercised END TO END on a throwaway folder — it reproduces HEAD's docked no-op (`static_cast<int>("certonlyzz") == 0`), then deletes for real, then cleans up after itself. The Windows half is printed from JUCE's own source (`CSIDL_APPDATA` :721 vs `CSIDL_PROFILE` :715) and labelled not-runnable-here. ⚠️ **Does NOT build on the standard cert line** — `Tests/shim/juce_core/juce_core.h` is a six-line stub with no `juce::File`, so it links the real `juce_core` module; `Tests/fb602_gates.sh` carries that line. Mutations: `TI_CERT_MUTATE=root` (old root — the mac text still matches, which is the POINT, while the Windows column goes wrong) · `=slug` (the widened slug → `cmp_fet 76` MOVES) · `=miss` (wrong root → the 29 files go to 0). |
| `delete_dispatch_cert.cpp` | **fb602 bug (a) — ONE `deletePreset`, TWO MEANINGS, ROUTED BY SHAPE** (offline, real juce_core). The page asks for the same native name for two unrelated jobs: `delFn(pid, name)` at `index.html:12207` and `:33601` deletes a card/FX preset FILE, `deletePresetFn(idx)` at `:17000` deletes a patch by INDEX. The cert feeds the shipped dispatcher **the real thing** — `JSON::parse` of the exact payloads those three JS lines produce, because the args arrive as a JSON array parsed to `juce::var` (`juce_WebBrowserComponent.cpp:299-320`) — and it greps `PluginEditor.cpp` for the predicate so it cannot drift off the code it is certifying. 9 bars: the predicate is verbatim in the source; the card branch calls the ONE courier body (no second copy); `["gli","Broke"]`, `["rvb_convolution","Pocket D"]` and `["cmp_fet 76","Bus Glue"]` route to the card FILE; `[3]`, `[0]` and `[]` route to the patch INDEX. It also prints what HEAD did with each card id (`deletePreset(0)` → out of range → early return → file untouched), so the bug it fixes is on the page instead of in a commit message. Mutation `TI_CERT_MUTATE=collide` restores HEAD's behaviour (patch body wins every call) → the card rows flip to CARD-DELETE-LOST. |
| `fb602_gates.sh` | **fb602 — all six gates above, each run TWICE** (runner, not a gate). Refuses to call a gate OK unless the MUTATED run of that same gate FAILED, because a green bar that cannot go red is not a gate; prints the pair for every one (8 pairs — `preset_path_cert` gets two, `slug` and `miss`). Carries the non-standard build line the two editor certs need (real `juce_core`, flags spelled out — ⚠️ an unquoted `$VAR` of flags is ONE argument in **zsh**, which silently drops every `-D` and makes `juce_core` fail with "No global header file was included!"; that cost a debug round here). Finally runs `cert_native_dupes.py` against `5a2ba1c` and checks BOTH sections still find the shipped `deletePreset` bug — the red path that needs no mutation at all. |
| `hue_track_gate.js` | **fb599 — AN LFO ON HUE MOVES THE PURPLE LINE** (page + C++). Max: "whenever I put an LFO on Hue, aka my wavetable position, why isn't it moving? ... my purple line bro." MY HYPOTHESIS WAS WRONG and the agent refuted it with the code: `harmDisplayParams_` is snapshotted AFTER `ownM` applies the mod matrix, byte-identical to what the voices hear. The break was one step later, in the page: `wtWaterfall.wtpos()` returned early for HARM/Table on the RAW APVTS slider, which by construction cannot carry matrix output — while the wavetable engine's identical line four lines below already read the post-matrix `window.__wtFrameEff` (fb457). There was simply no `__harmHueEff`. Now `harmHueVis()` publishes it on the same 60 Hz lane (−1 = not on Table → the page falls back to the knob, so an idle panel and a hand-turned knob are unchanged). Measured by scanning the canvas for purple ink with the knob HELD at 0.50 and an LFO driven for 48 frames: shipped centroid spread **0.00 px**, fixed **30.59 px**, and the feed lands exactly where the knob lands (max |Δ| 0.00 px). RED on the shipped page at bars 1 and 2. Refuted by number on the way: "there is no indicator" (1779 purple pixels, and the knob moves it 152.91 → 179.31 px) and "the painter never redraws at rest" (37 draws in 600 ms with zero input). ⚠️ Bar [4] used to select family 1 to get "off Table"; tables-only made every HARM oscillator a Table oscillator, so that left `isHarmTable()` true and the bar tested nothing — it now takes the oscillator off `engine-harm` outright, which is the case that actually matters: the new lane must never hijack the wavetable engine's own line. |
| `geode_shape_cert.cpp` | **fb596 — EVERY RESYNTH SHAPE IS ITS OWN IDEAL, ON EVERY SOURCE** (DSP, Pattern A: `c++ -std=c++17 -O2 -ISource Tests/geode_shape_cert.cpp -o /tmp/gsc && /tmp/gsc`). Max: "the saw wave doesn't sound like a saw wave and then some of them just really sound the same ... I only want the shapes there that actually do something and move the sound like the shape." THE CAUSE (H1, confirmed by measurement, not by reading): SHAPE only ever re-TUNED and re-WEIGHTED partials the sample already had — `applySculpt` elected one existing partial per rounded harmonic and never CREATED one — so a realistic 10-partial pluck became 'a saw' with **22 of its first 32 harmonics missing** (24.9 dB from an ideal saw; harmonics 7, 10, 12, 13, 15 … absent at −90 dB where the ideal wants −18 … −30), and on the default no-sample store the Saw target was a literal **0.0 dB no-op** (the store already IS 1/n). The recipes were right to <1 dB wherever a harmonic existed; the HOLES were the whole error. THE FIX (all inside `applySculpt`, mutation seams as `-D`s): (1) SPAWN the target's missing harmonics into free slots — 'the sample BECOMES the wave' — each at ratio n exactly with amp ref·W(n)·shape, in a HOME slot (kMaxPartials−kMaxH−1+n) so harmonic n keeps its phase_/ampZ_ block after block while the source churns (births ramp from ampZ_=0 → click-free by construction); (2) QUOTA separated from THIN in prepareBank so SHAPE may spawn up to what QUALITY → unison → the shared budget allows, and a POST-THIN on the SHAPED loudness so it can never cost more than that; (3) equal-RMS LEVEL match blended by `shape` — SHAPE changes timbre, not loudness (the shipped spread across targets was **17 dB**, Metal +9.6, Half −7.4); Metal gets a ×0.7 peak trim AFTER the match (its zero-phase crest is 4.7 vs a saw's 2.0 — before the trim its peak sat +7.5 dB over the source's); (4) Pulse re-derived d=0.28 → 0.25 (was 3.7 dB from Saw = the same sound; now 11.9). THE REMOVALS, measured through the real engine at SHAPE=1: **Bright == Saw + TILT 0.5625 to 0.00 dB, Hollow == Square + TILT 0.4219 to 0.00 dB** — each is an existing knob wearing a shape's name (Hollow was also 3.1 dB from Triangle, under the 3 dB floor). They are HIDDEN in the menu (`GEODE_SHAPES_HIDDEN`), not deleted: the 11-entry choice param and the shapeWeight cases stay so a patch saved on them keeps its sound and nothing renumbers. NINE remain: Sine · Square · Saw · Triangle · Pulse(25%) · Organ · Half · Vowel · Metal — closest pair 11.1 dB. RESULT: all 9 × 3 sources at q=1.0 within **0.3 dB** of their ideal, 0 holes (was up to 52.4 dB, Metal on the pluck). CPU: quota-bound, not source-bound — a sparse sample at SHAPE>0 costs what a 52-partial sample costs (0.64 % of a core per voice at default QUALITY, 2.74 ns/partial-sample); the 4×16 worst case is unchanged at the 640 budget; ⚠️ CORRECTED IN fb597: fb596's commit claimed "cpu-ceiling A/B 28.3 vs 28.3" — both builds were the patched header, because `GeodeEngine_test.cpp` lives in `Source/` and a QUOTED `#include` beats `-I`. Measured with the header BESIDE the test file: the synthetic cpu-ceiling gate (256 banks × unison 16, every knob, budget 640) reads **10.7 % (922aec6) → 28.3 % (fb596) → 23.2 % (fb597)** — the spawn's 96-slot banks feed every O(nP)/O(nP²) sculpt loop (FORMANT is O(n²)); under the 40 % bar, and the REAL 10-note path was already 61.6 % on 922aec6 from that pre-existing FORMANT cliff (62.6 % on fb596) — fb598 (FORMANT on a grid) is the fix for both. Also fixed: QUALITY thinned on SOURCE loudness before the sculpt, so an odd-only target wasted half its quota on evens the sculpt then killed. SEVEN BARS after the measurer's gate, each with the mutation that reds it: G1 fidelity ≤1 dB / 0 holes (shipped, `-DGEODE_NO_SPAWN`) · G2 every kept pair ≥6 dB (shipped: Saw~Pulse 2.6) · G3 quota holds under thin, unison 16 (52/4=13, all odd) and budget room 6 (`-DGEODE_NO_POSTTHIN`) · G4 level ±1 dB, Metal −3.1±1 (`-DGEODE_NO_LEVELMATCH`: 17 dB spread) · G5 no click stepping SHAPE 0→1→0 mid-note (1.00×/0.82× steady) · G6 no live slot changes frequency under a moving head over a churning store (`-DGEODE_SPAWN_APPEND_ONLY`: 43 jumps) · G7 SHAPE=0 bit-identical to the shipped engine, three fingerprints from the 922aec6 header (`-DGEODE_MUT_SHAPE0`). **fb597 added two bars the reviewers proved fb596 itself fails:** G4b mid-knob (RMS at SHAPE=0.5 within ±1 dB — fb596 blended the equal-RMS gain by `shape` a second time, leaving a bulge: Half −1.3 / Metal +2.0) and G7b SEVEN pressure fingerprints (silent slots · Bloom drive children · a saturated shared budget · Sieve · low Quality · Cut) — fb596 subtracted `alive` instead of `max(active, alive)` from the children's room and gave Bloom **16 → 20 partials at SHAPE=0 under a cap**: not bit-identical. fb597 = three one-liners (`max(active, alive)` · no second blend · `if (quota > 0)` around spawn/level/post-thin) + Metal's trim fades in with the knob. The fb596 header is RED on G4b and G7b; 922aec6 is green on G7 and G7b only. The shipped engine fails G1-G4 and G6 and passes only G7 — the proof that nothing at SHAPE=0 changed. ⚠️ THE CERT'S SOURCES ARE SYNTHETIC (there is no test audio in the repo): a default 48×1/n store, a sparse inharmonic pluck (ratios 1, 2.01, 3.04, 4.1, 5.2, 6.35, 7.6, 9.1, 11.3, 13.8), a dull 12×1/n². Regression: `GeodeEngine_test.cpp` 39/39 (nothing detunes), `all_menus` 42/0, `fb563_menu` 54/0. |
| `geode_follower_gate.js` | **fb598 — THE FOLLOWER IS A MIDI VISUAL** (page + C++). Max: "a static white line always stuck in the middle ... I'm not playing anything ... the follower is simply just a MIDI response visual." TWO HALVES, both proven. C++: a voice that fades out on the slot-release path (SynthVoice.h ~6304) never `reset()` its envelopes (the STEAL path does), so it stays `Stage::Release`, `isAmpEnvActive()` is TRUE forever, and the processor keeps publishing its FROZEN Resynth read head every tick — `Tests/env_park_cert.cpp` on the real TerrainEnvelope.h: 8/12 release cases park (a sustain-0 pluck parks EVERY note 8 ms after key-up), 0/12 with the reset. The same stuck voice pinned `__notesActive`, the scope sum and the noise-carrier pick. Page: `updateSampleFollower` had NO freshness rule (the LFO head has one, fb567); the frame is idle-skipped when byte-identical, so a constant list means nothing arrives and the last line stays through every repaint. Now a 700 ms freshness watchdog clears it and repaints DIRECTLY (fb577's law). 8 bars, 4 and 4b RED on the shipped page (head 156 px at x 0.499 persisting through a stalled feed; 234 px with rAF dead); mutations GEODE_FOLLOWER_MUTATE=1/2/3. The picture itself stays painted at rest (fb577 guard, bar 0). ⚠️ No harness drives the WebView bridge — the SynthVoice half is proven by the envelope cert + parity with the steal path, not on the installed AU. |
| `knob_pager_gate.js` | **fb598 — ‹ › ON EVERY KNOB ROW, NOTHING MOVES** (page layout; needs `png.js` beside it). Max: "I need a set of arrows, not just one arrow to the right ... every engine ... both centered ... no changes on the button size or the button spacing." The pager was already universal (fb538 `initSetArrows`: an explicit per-device index, `+1` with modulo wrap, unison always last) — it just had no `−1` path and one › per row. Now every wrap gets a ‹ cloned from its own › at init (same box, chevron, opacity, y), pulled OUT OF FLOW into the row's 14 px padding with a negative margin so the knob grid is untouched: knob rects byte-identical on all 23 rows of 8 engine cases; box pair centred on the grid to 0.00 px; ink inset 3.00/3.00; ‹ from set 1 lands on the LAST set (2/2 · 3/3 · 4/4); the cycle is unchanged. fb539's back-pointing › on the unison set is retired — › points forward everywhere now. 7 bars, [1] RED on the shipped page (23/23 rows have 0 ‹); mutations m3 (+1 only) → [5], m4 (‹ in flow) → [4], m5 (no mirror) → [3]. |
| `dropdown_dot_gate.js` | **fb598 — NO DROPDOWN DOT** (page layout). Max: "there's a dot right there that collides with this arrow ... get rid of all dots next to drop down menus ... people aren't stupid." The "dot" was `.samp-sel::after { content:'▾' }` — a 6.13 px triangle after the loop-mode word, the LAST thing in the absolute label row, and the first thing the JS-inserted ‹ hits when the corner grows left (fb595's mechanism). Deleted, not hidden — nothing sits to its right, so the label row is byte-identical. 45 states (3 sample engines × 5 loop modes × 3 names): ▾ gone on all, ›/A/+ unmoved. ⚠️ [4] is a WARN, not a bar: 8/45 states still sit under 4 px (worst Granular/Ping-Pong/long −5.14) — that is the fb595 corner mechanism, pre-existing; the measured fix shrinks the name slot to 74 px and the DEFAULT "Load Sample" to "Load Sa…", which is Max's trade to make. |
| `geode_drive_cert.cpp` | **fb598 — EVERY DRIVE MODE DOES SOMETHING** (DSP, Pattern A). Max: "only Saturate, Fold-back and the one under fold back work ... there used to be a cool thing with Glint." THE CAUSE: the three SPECTRAL modes (Bloom/Glint/Moire) append children at slot nP upward and `applyDriveChildren` REFUSED when `total >= kMaxPartials` — and the analyser's tracker keeps all 96 slots busy on any dense real sample, so nP=96 and ZERO children every block (measured: +0/+0/+0 on an analysed chord, dF 0.0). Since fb596 the SHAPE spawn's home slots 32..95 starved them on EVERY source whenever SHAPE>0 (+0 at SHAPE=0.5). Secondary: Moire needed ≥2 parents over 0.30·ref and landed its sum/difference tones ON existing harmonics (DEAD 2.5-3.8 dB); Glint keyed on absolute ratio ≥3 (a volume knob on a low-f0 analysis); Bloom took parents in SLOT order. Now: free-slot placement with a STICKY child key (own slot from last block → silent free slot → any), loudest-N parents, Bloom 2r/3r, Glint 4r/6r above the amp-weighted centroid, Moire ±35..135-cent ghosts on the overtones. Before → after dF(drive=1 vs 0) on the dense sample: Bloom 0.0 → 44.7, Glint 0.1 → 37.4, Moire 0.0 → 37.9; +16 children each, also at SHAPE=0.5. ⚠️ EMBER IS UNTOUCHED — Max says it works; it measures 1.8-2.4 dB from Saturate (same softClip, two gains) and D2 documents that pair as the one exception (the post-clip warmth that would separate them is behind `-DGEODE_EMBER_WARMTH`). 8 bars, D1/D2/D3/D8 RED on shipped; `-DGEODE_DRIVE_MUT_APPEND` → D1 D2 D3 D8, `-DGEODE_DRIVE_MUT_NOKEY` → D7 (2787 births). drive=0 bit-identical (D5, five fingerprints). |
| `geode_formant_cert.cpp` | **fb598 — FORMANT ACTUALLY SHIFTS** (DSP, Pattern A). Max: "the formant shifter doesn't do anything ... not one audible change." THE CAUSE: the envelope was a NORMALISED Gaussian mean in log-frequency, bw 0.5 ln-units (±0.7 oct ≈ 3 harmonics at F1) — it returns the bank's smooth TREND, never its humps, so the gain ratio was a constant on any power-law bank = a VOLUME knob (measured: formant=0 on the dull source −11.1 dB RMS with 0.9 dB of timbre change) and a broad tilt on a vowel (humps at harmonics 3 and 8 STAYED at 3 and 8 at full shift); the normalised mean extrapolates FLAT past the last partial (P(k)=A(k·f)/A(k) → 1); the ±12 dB clamp could not lift a valley onto a 24 dB hump; and the governor thinned BEFORE the shift, so a shifted hump had no carrier at its destination. Now: a true-envelope PEAK INTERPOLANT in (ln r, ln a) with −12 dB/oct skirts, ±30 dB clamp, equal-RMS match, and the thin deferred to AFTER the shift (O(n log n), 41 → 2.2 µs per bank-block). Vowel humps at 3/8/16 → 4/11/23 at +0.5 oct and 6/16/32 at +1 oct (displacement −1.00/−0.51/+0.47/+1.00 oct vs shipped −1.21/0.00/+0.33/+0.81); ΔRMS 0.00; Δratio 0 (nothing detunes). 8 bars, F2/F4/F6/F8 RED on shipped; mutations `-DGEODE_FORMANT_OLD_ENV` → F2, `-DGEODE_MUT_FORMANT_PITCH` → F3, `-DGEODE_NO_FORMANT_LEVEL` → F4, `-DGEODE_FORMANT_CLAMP12` → F7, `-DGEODE_FORMANT_PRETHIN` → F8, `-DGEODE_MUT_FORMANT0` → F5. ⚠️ With no sample loaded (the 1/n default store) the knob is audible only above 0.5 — a saw has no humps to slide down. |
| `geode_cut_cert.cpp` | **fb598 — LOW + HIGH ON THE BACK, COEXISTING, SAME DSP** (DSP, Pattern A; UI in `geode_backrow_gate.js`). Max: "take away the LP/HP and put it in the back ... the same DSP ... these two need to coexist and sculpt the sound." The Resynth had ONE cut stage, LP or HP by a mode switch; the closest a single Cut gets to a Low 0.3 + High 0.7 band is LP 0.60 with 22.8 dB worst error. Now the back row's Low/High (SYN_OSC_x_SPECTRAL_LO/HI — fb594 had hidden them on Resynth because nothing read them) drive TWO stages on the Cut's own ~24 dB/oct curve: HP by Low, LP by High, in that order; their modulated edges were already published per block (dests 1846..1853), so the mod matrix reaches them exactly as on the wavetable engine, and the waterfall shows the cut. Front page 1 is FIVE (Scan · Stretch · Sieve · Shape · Drive, grid re-declared, gap spread 0.01 px); GEODE_CUT / CUT_MODE stay registered, no longer read (no synth presets exist today). C1 neutral bit-identical (G7's three fingerprints); C5 SAME DSP bit-for-bit: High .7 == the shipped LP .7, Low .3 == the shipped HP .7, Low .5 == HP .5 (the one Bloom-carrying case re-baked to fb598); C2/C3/C4 RED on shipped; `-DGEODE_MUT_EXCLUSIVE` → C4, `-DGEODE_MUT_LO_CURVE` → C5. `back_spectral_gate` amended: Resynth joins the LIVE set (3 live / 4 dead). |
| `wt_feedback_cert.cpp` | **fb585 — ONE KNOB, BOTH FEEDBACKS, AND BOTH HALVES OF THE LIFEGUARD LAW** (offline, the real factory bank). The oscillator modulates its own read through the DX7 mean filter our FM operator already uses (`y = 0.5(x[n]+x[n-1])`), bending BOTH the phase it reads at and the frame it reads from. The two paths are tapered differently on purpose, and that taper IS the law: measured, they fail in opposite directions — FRAME feedback is clean and progressive but alone reaches only 0.45x the WT Pos axis, while PHASE feedback reaches 1.6x but *its character is its alias* (filtering it clean drops it from 26.5 dB to 12.5). So frame rides a LINEAR taper and owns the bottom; phase rides a FOURTH-POWER taper and owns the top. 5 bars: the bottom is clean on every table at every octave (worst -26.4 dB at 10-25%); the top is violent (**+27.1 dB** inharmonic — the partials have left the grid); not imitable by WT Pos at full (mean **1.80x** the table's own WT Pos span, worst 0.87x); no dead zone (quietest of 20 fine steps 0.41 dB, 0 below 0.2); level swing 4.85 dB. ⚠️ fb585 raised the ceiling by steepening the TAPERS as well as the maxima, so the whole bottom half is unchanged (-37.5 dB at 10% either way) while the top gained 12.5 dB and the distinctiveness went 1.07x -> 1.80x. Two wrong answers are recorded in the header: a 4.0*s^3 frame taper read a lovely -52.6 dB at 10% because that end was DEAD, and opening it back up dirtied 25% to -15.5. The way out was that the top does not come from the frame path at all. ⚠️ 4x oversampling was built, measured and REJECTED — it costs 4x the oscillator, drops the level 10 dB, and cuts distinctiveness from 1.38 to 0.73, because here the dirty version IS the product. ⚠️ Proven able to fail: zeroing both depths reds bars 2, 3 and 4. |
| `wt_feedback_au.cpp` | **fb585 — FEEDBACK ON THE INSTALLED PLUGIN** (hosted AU). The offline cert proves the shape; this proves the wiring, which is the fb469 failure class (built clean, looked wired). Inharmonic energy measured against a Blackman-Harris window (-92 dB sidelobes), harmonics taken as ±4 bins around each k·f0. 8 bars: exposed as 'Feedback' and 'Spread' is gone; **works at ONE voice** (13.8x the noise floor — the flaw that killed fb582's Spread); the bottom is clean (-82.1 dB at 10%, -81.1 at 25%); the top is violent (**+7.3 dB**, from -82.8 at rest — 90 dB of change); the top dominates everything below it (17.9x); **no clicks** (sweeping is 0.89x the step of holding at the destination); level holds within 0.1 dB. ⚠️ Two of this file's own bars had to be re-scoped after they went red on noise rather than on a defect, and both say so in the source. The click bar's reference is the sweep's DESTINATION, not its origin — comparing a sweep that ends in genuine chaos against a clean FB=0 tone measures the feature working and calls it a click (that mistake read 7.54x). ⚠️ Proven able to fail: a build with the feature disabled reds bars 1, 3 and 4 (bar 3 reads -80.0 dB instead of -18.4). |
| `lane_clock_gate.js` | **fb581 — THE PUSH LANE IS THE CLOCK; A QUIET LANE MEANS REST.** Max: "could you at least put the LFO to rest instead of it free running again... it always has to react and respond and be linked to the midi signal." The page's pushless fallback (fb511b) exists for a page with NO lane — a browser preview, this suite, a card on a dead channel — and it inferred "no lane" from SILENCE, which is exactly what a resting plugin sounds like. Measured on the shipped page by driving the lane as the plugin does: lane alive at 500 ms → 10.3 fps, lane quiet → 48.2 fps, keepalive every 2 s (the 15 Hz idle path, or a slowed message thread) → 48.2 fps, all of which should be 0. Two doors: `lastPush` started at 0 (a fresh instance free-ran until the C++ caught up) and the keepalive is sent every 30 SKIPPED frames (500 ms at 60 Hz, 2 s at 15 Hz — past the 700 ms threshold). One clock drives the LFO, the filter and the terrain randomiser, which is why all three misbehaved together. Now: once the page has heard the lane even once, it never drives itself again. Bars: a seen lane gone quiet is rest; the 2 s cadence is rest; a healthy 500 ms rest is still; once heard never unheard; a push still paints; and a page that never hears the lane still drives itself (so the preview and this suite are untouched). `LANE_CLOCK_MUTATE=1..2`. |
| `canvas_alive_gate.js` | **fb577 — NO CANVAS ANYWHERE IS BLANK WHEN THE FRAME CLOCK IS STOPPED.** Max, after fb576: "make sure that this doesn't happen with anything else... we don't wanna take any risk chances especially with this happening on an open release." fb576 wired the two tape faces by hand; the hole was generic. Measured on the installed AU (c27581a, no note, frames 0): after leaving the front page and coming back the HERO `#terrain-canvas` 820x276 was BLANK, with `spaceVisCanvas`, the four Space knob icons, `reso-cv`, `noise-viz-cv`, the filter device's five canvases and a new card's `.fx-spec` — boot paints once (restore pushes still ship frames), everything shown or resized afterwards did not. Cured in the frame dispatcher: a gesture wakes one burst of frames, a canvas that changes size wakes one, and a 1 Hz watchdog offers ONE repaint per (canvas, size, visibility) — a canvas still blank after its offer is a signal display and is never chased, so idle stays at zero frames. A wake clears both rest latches (`__fxRestDrawn`, `__heroDrawn`). Bars: 26 places / 446 visible-canvas samples with nothing blank; the hero and a card canvas heal within 1.4 s of being wiped; a gesture wakes 1..6 frames then silence; a canvas nobody paints is not chased; zero frames at idle before and after. `CANVAS_ALIVE_MUTATE=1..4`. |
| `tape_alive_gate.js` | **fb576 — A TAPE MACHINE NEVER DISAPPEARS** (the card and the front, with the frame clock at rest). Max: "cassette has disappeared, and on the front it's also disappeared... make sure that these never disappear and self-heal." Measured on the installed AU (a fresh open, no note): frames 0, the rack's tape canvas 300×150 with 0 ink, the front's 360×139 with 0 ink — the machines are canvases painted only by frames, and the clock stops at rest (fb566/fb567). The gate simulates the host at rest (the keepalive stamped every 50 ms, so the pushless fallback never runs) and holds ten bars at frames 0: the front machine drawn at boot, after a resize, after a machine change; a wiped canvas healed within 1.4 s by the 1 Hz watchdog (front and card); the card drawn when added; the Studio card's ink centred (was 12 px high); no idle cost (0 frames, ≤ 1 tape tick in 2 s). `TAPE_ALIVE_MUTATE=1..4`. |
| `macro_face_gate.js` | **fb575 — THE MACRO FACE RIDES THE FRAME CLOCK.** Max: "I move my mod wheel on the Macros, it's still choppy." Measured: 8 face repaints in a 1 s sweep fed at 60 fps (a 150 ms poll). Bars: 60 shipped frames carrying the knob 0 → 1 repaint the face >= 45 times with no gap over 40 ms; a drag keeps the feed off the face. `MACRO_FACE_MUTATE=1`. |
| `macro_wheel_cert.cpp` | **fb575 — A MACRO UNDER THE WHEEL IS AS SMOOTH AS UNDER THE MOUSE** (installed AU). CC 1 → Macro 1 through `midiCcMap`, Macro 1 → Level A through `synModJson`, Level A's knob at 0. A held note while the wheel sweeps 0 → 127 one CC per block with the run loop pumped (the timer fires as in a host); the envelope's biggest 1.3 ms jump and longest plateau, wheel vs the same sweep through AudioUnitSetParameter. Before: 1.60 % / 45 ms against 0.67 % / 9 ms. Bars: the wheel no worse than the mouse; neither path steps (<= 1.5 %, <= 24 ms); the parameter follows the wheel; a mouse move after the wheel wins. |
| `macro_rename_gate.js` | **fb574 — A MACRO CAN BE RENAMED (MENU + DOUBLE-CLICK) IN THE HOST.** Max: "I'm trying to right-click, press rename, and it's not working... make it so that we can double-click on the name... the letters have to be the same size." Why: arming the fb135 bridge makes the C++ (editArm) grabKeyboardFocus(), WebKit blurs the field, and its blur→commit closed it before a key landed (fb136's preset rename already had no blur handler: "survive the arming blur"); measured on the installed AU, a late names seed (paintNames) also rewrote the word over the open field at +400 ms. Bars: the real menu path opens the field on the bridge; it survives a blur; B·a·b·y·Enter through `__tiHostKey`; a real double-click renames; a click elsewhere commits, Esc reverts; the field's computed type equals the label's (8 px); a repaint of the names leaves an open field alone. `MACRO_RENAME_MUTATE=1..4`. |
| `identity_lock_gate.py` | **fb605 — THE IDENTITY IS PINNED, PERMANENTLY.** The rename (`Terrain Instrument` -> **Terrain**, old `Terrain` -> **Terrain FX**) moved directories, targets, bundle ids and every user-visible string. It moved NOTHING a DAW uses to find a plugin in a saved project, and this gate is what keeps that true. 8 bars: both CMakeLists still declare the frozen `Wvcr`/`Tern` and `Wvcr`/`Trrn`; the built VST3s carry the frozen class UIDs byte for byte; **the MECHANISM — each UID's low 8 bytes are decoded and must spell `WvcrTern` / `WvcrTrrn`**, which is why a name cannot move a UID and a code cannot fail to; both built AUs report `aumu Tern Wvcr` / `aufx Trrn Wvcr` **and agree with their own CMakeLists** (source/artefact drift is its own way of lying); and the INSTALLED bundles report the same quad — a bundle that is not installed says so LOUDLY and counts as a skip, never a pass. **Bar [8] is the one the rename itself created**: it scans the whole Components folder and reds if MORE THAN ONE bundle claims a frozen quad, because a stale bundle left under the old name still answers to it and the host picks one without telling you — a "fixed" plugin that keeps loading last month's binary, with nobody lying anywhere. It found exactly that on the first run (`Terrain FX.component` and a stale `Terrain.component` both claiming `aufx/Trrn/Wvcr`). Failure here has no error message of its own: the build goes green, the plugin installs, and every project Max ever saved opens with a silent channel. Four mutations, each an edit a person could plausibly type: `TI_ID_MUT=cmake` -> [0][4], `=cid` -> [2][3], `=au` -> [4], `=fx` -> [1][5][6]. | 9 |
| `extract_imports_scan.py` | **fb606 — SLICES THE SHIPPING IMPORTS-REGISTRY SCAN.** Cuts the whole `// IMPORTS REGISTRY` section out of `Source/PluginProcessor.cpp` (banner → the closing brace of `loadImportsRegistry`) and rewrites exactly one token, `TerrainAudioProcessor::` → `ImportsReg::`, so `wt_folder_scan_cert` compiles the SHIPPING BYTES rather than a copy that drifts (the failure `extract_halfband.py`'s header documents). `builtinWtCatItems` is EXCISED and stubbed — it reads the APVTS, which would drag `juce_audio_processors` and the whole plugin into a `juce_core`-only cert. `--mutate` turns the recursion off in the generated header only: `searchRecursively` → `false` on a build that uses JUCE's flag, or the depth cap → `0` on a build whose walk is hand-rolled (fb606's is). It refuses to run if there is nothing to turn off — a mutation that cannot fire is not a control. | — (slicer) |
| `wt_folder_scan_cert.cpp` | **fb606 — POINT TERRAIN AT A MASTER FOLDER AND THE TABLES IN ITS SUBFOLDERS MUST APPEAR.** Max: "it's hard to open them up because it's subfolders in the MASTER folder, and for some reason Terrain can't open them unless it's DIRECTLY INSIDE OF THE SUB FOLDER." The defect was one boolean — `findChildFiles (findFiles, false, kImportWild)` — and flipping it alone makes it WORSE, because the payload is a flat `{name,path}` list per registered folder and 120 names land in one undifferentiated blob. Eleven bars against a real temp tree: found at depth 0/1/2 exactly once · the relative subfolder travels (an item property OR nested named groups — either shape passes, no shape fails) · an EMPTY subfolder is not a category · a leading **0x7F** in a folder name (the owner's PLUTO 2 pack really is named that) survives `JSON::toString`+`parse` byte-exact · the wildcard did not widen · the depth/count caps REPORT when they bind instead of truncating in silence · `folder.count` matches the items · a symlink LOOP terminates (re-exec'd child, 15 s budget — a bare `fork()` of a `juce_core.mm` process aborts in libmalloc, which is a fact about macOS and not a finding about the scan) · the SECOND non-recursive scan (the managed Wavetables folder) recurses too · the scan cache says whether it fired. `--emit <dir>` writes the two payloads the WebView really receives, for `wt_folder_menu_gate.js`. Controls: the extractor's `--mutate` (compile with `-DTI_SLICE_HEADER`, **never** `-I` — a quoted `#include` searches this file's own directory first and the mutant silently compiles the healthy slice) and `TI_RECURSE_MUT=flat`. | 11 |
| `wt_folder_menu_gate.js` | **fb606 — THE FOLDER MENU COMES OUT FROM BEHIND THE BROWSER.** Max: "delete + locate folder is behind the menu AKA impossible to get to lol." Measured: `.tpb-panel` and `#syn-panel .syn-ctx-menu.act` BOTH carried `z-index: 2147483646`, and at equal z the later DOM node wins — the panel is `body.appendChild`ed at open time, so it always won. 🚨 **A z-index comparison is not sufficient and this gate proves it**: bar [3] hit-tests with `document.elementFromPoint` at the menu's own centre and at every row's centre, and bar [4] prints what a z-only gate would have concluded beside it — under the `synmenu` mutation the two z values are IDENTICAL, a z-only gate says PASS, and every point lands on `.tpb-pane`. Eight bars: the panel laid out · **the seam** (the REAL C++ payloads from `wt_folder_scan_cert --emit` fed to the shipping JS — a C++ shape the JS reader rejects is green on both sides and broken in the plugin) · the menu opens with Locate/Remove · hit-testable · escaped the stacking context · never cuts off (corner right-click, the `.pmenu` clamp) · a row reaches its handler through `elementFromPoint().click()` · Remove is never offered on factory content or a subfolder. Controls: `TPBMENU_MUTATE=synmenu` (the EXACT pre-fb606 `__synShowMenu` path → 4 red) and `=noclamp` (→ 3 red). | 8 |
| `fb606_gates.sh` | **fb606 — both gates, each run TWICE**: normal, then with its mutation control, and it refuses to call a gate OK unless the mutant went red. Also slices the scan, builds both binaries and emits the payloads the JS gate reads. `bash Tests/fb606_gates.sh` from `plugins/Terrain`. | 4 pairs |

The three fx3 engine certs live beside their engines, not here — they need the roster and the
bibles next to them:

```sh
for d in chorus flanger phaser; do
  clang++ -O2 -std=c++17 -I Tests/shim -I Source -I Design/fx3/$d \
          Design/fx3/$d/${d}_cert.cpp -o /tmp/${d}_cert && /tmp/${d}_cert
done            # chorus 87 · flanger 85 · phaser 67
```

⚠️ **Run those three ONE AT A TIME.** In parallel the flanger's BBD CPU gate reads 25.20 µs
against its own 25 µs bar; alone it is 23.07. That is contention, not a regression, and it has
cost a diagnosis once already.
| `tpb_head.js` | **fb608/fb609 — THE TWO-PANE BROWSER HEADER.** fb394 wrote this file to prove the OPPOSITE arrangement ("search never shares a row with the Import label"); fb608 repealed that clause and this is the rewrite. The browser stacked THREE ruled bands over the list; search and the folder path both fold into the header, 384×342 → 384×294, and the two panes keep the exact 262 px they always had. 17 bars: one 31 px band in all six browsers · search FIRST and on the categories' 14 px rail (putting the words first threw the magnifier **85.5 px** off it, which is why the import label became a ＋) · nothing overlaps · the field is still a field (≥ 74 px) · the ＋ keeps the label's words and still fires `onImport` · **fb609: the path has 42 px of air before the ‹ › — bar [6] used to ASSERT the collision Max rejected on sight** · a long path ellipses inside a capped box **with the "…" on the LEFT, proven from GLYPH POSITIONS** (end-truncation shows where you STARTED) · **[8b] a Hebrew-named folder must not reverse the path** · typing a long query must not push the path · the trash glyph joins the row and the field holds its floor. ⚠️ Its predecessor computed `[railMag, railImp, railCat].filter(v => v != null)`, so deleting the row it measured DROPPED the null and the bar went green having stopped testing the law it exists for. Controls: `HDR_MUT=band\|clip\|noellip\|ltr\|nobidi\|nocap\|notitle\|pad\|tight\|nofloor` all RED, and **`push` must stay GREEN** — `min-width:0` is NOT what makes an ellipsis work (Flexbox §4.5 already clamps it on an `overflow:hidden` box), kept as a standing disproof. | 17 |
| `wt_bake_bench.cpp` | **fb610 — HOW LONG DOES THE BAKE ACTUALLY TAKE?** Compiles the SHIPPING `Wavetable.h` against a REAL factory file, because every instinct said the wavetable stall was the transform: 61 mip levels × 128 frames = 7,936 real FFTs and a 61 MB buffer. It is **30.7 ms**. Measuring first is the only reason that run did not disappear into optimising an FFT that was already fast. **Reports, does not gate.** | — |
| `wt_stamp_gate.py` | **fb610 — THE STALENESS SIGNATURE DESCRIBES THE SAME THING ON BOTH SIDES.** `__wtDisp` (pushed at 60 Hz from `PluginEditor.cpp`) and `cachedSig` (index.html) are two lists in two languages 400,000 lines apart and correctness depends on their ORDER matching. Bars: same length · every field on BOTH payload branches (fb589's law) · **every POSITION pushes the value that position is read as** (a length check passes on any permutation of the middle) · **[4] the signature carries the table's own identity — the ONLY bar that reds on the pre-fb610 code, because [1]-[3] were all green while the bug shipped.** Controls: `WTSTAMP_MUT=drop\|reorder\|oneside`. | 4 |
| `wt_stale_gate.js` | **fb610/fb611 — DOES A NEWLY PICKED TABLE ACTUALLY REDRAW?** Drives the SHIPPING `maybeRebake`: a payload identical in every way EXCEPT which table it is must re-bake at once. Before fb610 it did not — `loadWavetableByPath` queues the build and returns, the JS fetched the picture while the build was still running, and the signature carried nothing identifying the table. Plus fb611's wait states: a cloud-evicted file names itself at once and shows it is waiting, the wait clears when the table lands, and a LOCAL file never dims. Controls: `STALE_MUT=nostamp` (strips BOTH sides — stripping one tests the opposite failure), `=notick`, `=noclear`. | 8 |
| `wt_locality_gate.py` | **fb611 — A WAVETABLE FILE MAY NOT BE ON THIS DISK.** The SAME 120 files byte for byte and the same native call — but the Desktop copy was iCloud-evicted: **922.92 ms/file against 3.81 ms local, 105 of 120 flagged `SF_DATALESS`**. No plugin makes an absent file arrive faster; what was ours is that the read ran on the MESSAGE THREAD, so the wait was a frozen UI indistinguishable from a hang. Bars: neither loader reads on the message thread · the read is on its own worker and hands the PCM back via `callAsync` · the cloud case is read from the file's own flag and announced BEFORE the read · **[4] the shipped bank is local, and the bar prints a LOCALITY REPORT for every registered root — that report is what found this.** Controls: `WTLOC_MUT=inline\|nopool\|nodataless`. | 4 |
| `wt_factory_gate.py` | **fb612 → fb638 THE FACTORY WAVETABLE LIBRARY: 454 FLAC + 46 BUILT-INS = 500, NO "TERRA"** (`python3 Tests/wt_factory_gate.py`). [1] the planned 454 in thirteen categories · [2] no name starts "Terra", no ALL CAPS (and [2b] no built-in either) · [3] every name unique among the FLACs and against the built-ins · [4] every file 128×2048 · [5] the bank is found from the plugin binary, not the host · [6] the built bundles carry exactly the library, no stale "Terra - " file · [7] THE RENAME IS PRESET-SAFE: renames_fb638.csv (120 rows) = WtFactoryAliases.h, md5s identical, the restore consults the map · [8] every factory ref in Max's real presets resolves (read-only; skipped on CI) · [9] 454 + 46 = 500. Controls `WTFAC_MUT=terra/allcaps/dupe/builtin/short/hostapp/noalias/md5` each go red on their bar | 10 |
| `browser_never_waits_gate.py` | **fb639 THE BROWSER NEVER WAITS FOR THE DISK** · **fb640 adds [7]–[9]**: all four list natives complete through `tiListPayload` ("b64:" + base64), the page decodes exactly those four in `Juce.getNativeFunction` (round-tripped under node), and `listImports` walks on `wtIoPool_` (kind `kManagedWtKind`). `--controls` runs all nine mutations. (`python3 Tests/browser_never_waits_gate.py`). Max: opening the wavetable browser froze FL Studio for about a second — the list natives ran `getImportsJson` (a folder walk + a ~100 KB payload) on the host's UI thread. [1] listNoiseImports / listWtImports / listSampleImports answer via `requestImportsJson` · [2] the only `getImportsJson` call is the `wtIoPool_` job · [3] editor code edits the registry only through the Async wrappers · [4] `editImportsRegistry` only try_locks · [5] `requestImportsJson` does no I/O or serialisation · [6] `createEditor` prefetches. Controls `NEVERWAIT_MUT=sync/block/direct/noprefetch/io` each go red on their bar | 6 |
| `browser_open_timing.cpp` + `.sh` | **fb639 · THE SAME, MEASURED** (`Tests/browser_open_timing.sh`, macOS, after a build; links the SharedCode inside a fake bundle so the real 454-table library resolves). Pumps the real message loop. [1] a cold open returns at once (0.03 ms; the walk cost 40 ms on this Mac) and the answer lands · [2] 50 warm opens, payload identical to a direct walk · [3] past the 1.5 s window: instant + background refresh · [4] fb606: a folder added is in the very next answer · [5] adding a folder while a walk holds the lock never waits and still arrives · [6] the user's registry files are restored byte for byte | 6 |
| `browser_freeze_sim.cpp` + `.sh` | **fb640 · THE WHOLE CLICK, SIMULATED** (`Tests/browser_freeze_sim.sh`, macOS, after a build; opens a window ~20 s). Max, after fb639: "it still does that little freeze … about a second." The real processor + editor + WKWebView page; a real `mousedown` on the wavetable name opens the browser 7 times (cold, again at once, after idle, cache wiped). A watchdog thread pings the message thread every ~1 ms: the longest unanswered gap is the host freeze. The panel's arrival is stamped by a MutationObserver (immune to WebKit timer throttling). BEFORE: 660–770 ms host stall on EVERY open, all inside `WebBrowserComponent::Impl::emitEvent` — JUCE escapes a result with a quadratic `String::replace`, and a JSON list sent as a string has a backslash per quote. AFTER (base64 payload): worst 16.9 ms, panel in 23–35 ms (75 ms cache wiped). Bars: host never stalls ≥ 25 ms; the panel is in the page within 120 ms in every trial. |
| `sample_fixes_fb641.cpp` + `.sh` | **fb641 · THREE SAMPLE-ENGINE FIXES, MEASURED** (`Tests/sample_fixes_fb641.sh`, macOS, after a build). Max: reverse Scan killed Spray; a pasted sample changed key; one-shots ignored glide. [A] SampleEngine: under reverse Scan the sprayed start MIRRORS the forward one (same seed; full Spray spans the region; Spray 0 still starts at region end − 1). [B] `copyOscSampleSlot` carries the native rate + paths; a 44.1 kHz sample pasted over a 48 kHz one plays at the source pitch, and a control rebuilding the old rate must measure sharp. [C] mono legato 60 → 72 on a ONE-SHOT with portamento passes through the pitches between (control: portamento 0 jumps). |
| `formant_latency_fb642.cpp` + `.sh` | **fb642 · FORMANT (AND STRETCH) ADD NO LATENCY** (`Tests/formant_latency_fb642.sh`, macOS, after a build). Max: "the formant adds latency to the sample." The warp engines are primed with the sample's own look-ahead (`WarpProcessor::primeOutput` → Signalsmith `outputSeek`). Probe = 20 ms bursts at IRREGULAR gaps (a periodic train aliases every period onto zero lag). [1] engine, per mode: UNPRIMED (the old path, the control) starts late (Tones +140 ms, Beats +96 ms), PRIMED within 3 ms (onset for all modes; whole-train cross-correlation for Tones/Beats — Texture jumps its read head by design, so onset only). [2] the plugin: Formant +0.5 starts within 3 ms of the direct path in every mode. [3] Formant turned up mid-note opens no hole. [4] the formant still works (vowel centroid moves > 15 %). |
| `fxfilter_drive0_fb642.cpp` + `.sh` | **fb642 · THE RACK FILTER AT DRIVE 0 IS CLEAN** (`Tests/fxfilter_drive0_fb642.sh`, macOS, after a build; names generated from index.html `FLT_ENGINES`). Max: "the filter saturates and drives the audio signal … even though the drive is at zero." A 220 Hz sine through `tw::FilterFxEngine` at Drive 0, each engine at the cutoff where the tone passes, −18/−12/−6/0 dBFS, THD over harmonics 2–12. The OLD path is emulated in the same binary (it fed the core +14 dB: old THD at L = new at L + 14 dB). Classified by `FilterFxEngine::liftRidesDrive` (the plugin's list). [1] default Ladder LP 24 at −12 dBFS ≤ −60 dB; [2] every filter model the old path distorted (worse than −60 dB) is ≥ 12 dB cleaner — measured 14–28 dB — and none is dirtier (the SVF/SEM/Multi family was already linear); effect types keep the old voicing and are listed only. |
| `hero_glow_gate.js` | **fb613 — THE UI MUST NOT GET SLOWER WHEN THE AUDIO GETS LOUDER.** The hero's height map is LINEAR in the live sample, so louder audio lifts more of 1024 cells over the glow threshold AND lifts them higher — count and area both grow and they multiply: **34.9 glows/frame at silence, 404.3 on a clipped square, 24,258 `createRadialGradient` calls a second, 169 % of the canvas repainted in gradient fills** — all in the WebView's renderer process, which the plugin's CPU meter does not measure. ⚠️ The obvious diagnosis (unclamped coordinates) was MEASURED AND REFUTED: 0 of 404 discs land off-canvas. Fixed with one pre-rendered sprite blitted and a 96-per-frame budget. Bars: **[1] the count is bounded AND THE BOUND DEMONSTRABLY FIRES** (the clipped case must sit exactly ON the cap — a bound nothing reaches proves nothing) · one gradient, not one per glow · a quiet signal still glows · louder still means more mountains. Control: `GLOW_MUT=nosprite`. | 5 |
| `dst_curve_gate.js` | **fb614 — THE DISTORTION CURVE DRAWS, ON THE RIGHT AXIS.** The painter ticked fine (`__tiFrame` is dispatched OUTSIDE the quiet gate) but `__dstVizPush` sat INSIDE it, so an editor opened on a silent, untouched session never defined the variable and fell to a poll with three silent death modes — `d=""` forever under a still-drawing dashed line. And the axis was wrong everywhere: `sampleCurve` sweeps ±`occSpan` (**4.5 FOLD** · 3.0 default · 1.6 DIGITAL) under a reference drawn corner-to-corner. Drives the REAL painter through the REAL push variable via fb446's `__fxAdd` hook. Bars include **[3] a unity-in-the-linear-region curve lies ON the reference — 28 points, worst deviation 0.09 px, which is "scaled properly" as a number**. ⚠️ **The last bar LIED on its first draft**: it brace-counted a C++ file that EMITS JAVASCRIPT (braces inside string literals) and matched the phrase in its own COMMENT. Comments and strings are stripped first now, and all four gates are checked. Controls: `DSTC_MUT=nospan\|staticunity\|inquiet`. | 7 |
| `fb608_gates.sh` | **fb608/fb609 — the header gate run once clean and once per control**, 10 that must go red and **one that must stay green** (`push`). | 12 runs |
| `fb610_gates.sh` | **fb610/fb611 — runs the BAKE BENCH FIRST** (the measurement that stopped the run going into the FFT), then the signature, staleness and locality gates against their controls. | 13 runs |
| `fb614_gates.sh` | **fb612/fb613/fb614 — the factory library, the loudness stall and the distortion curve**, every gate against every control. | 12 runs |
| `preset_roundtrip_cert.cpp` | fb617 | save → load → save is byte-identical against the installed AU; an empty slice list echoes no root slicesJson; a pre-fix session with two `<layers>` loads the NEWEST and saves one. Control `RT_MUT=stale` expects the old first-wins read and must go red. | `bash Tests/fb617_gates.sh` |
| `preset_null_cert.cpp` | fb618 | a preset loads clean, through the DAW's door (ClassInfo): the `<preset>` child survives once and first with `carries`; a fresh instance round-trips the chunk byte-identical; a chunk WITHOUT a property leaves nothing of the previous patch (cards, macro names); the first note on two fresh instances nulls below −100 dBFS. Controls `PN_MUT=inherit` and `PN_MUT=drift` must go red. | `bash Tests/fb618_gates.sh` |
| `preset_bank_cert.cpp` | fb619 | the bank file layer (`Source/PresetBank.h`) on a throwaway root with real juce_core: bank folder + bank.json, header-only catalogue scan, byte-clean meta rewrite, move, favourites sidecar, `.terrainpack` export → import into a fresh root byte-identical, rename re-files, delete, writes outside the user root refused, zip-slip dropped, caps report. Controls `BK_MUT=nocap` / `BK_MUT=escape` must go red. | `bash Tests/fb619_gates.sh` |
| `preset_surfaces_gate.js` | **fb620 — THE TWO PRESET SURFACES, DRIVEN LIKE A HAND.** The page headless with the fb618/fb619 natives stubbed over an in-memory catalogue; 14 bars click the real furniture: the header name → the quick menu (banks · Favourites · Import, the bank's list by type, hit-testable, clamped) → Browse all (🚨 elementFromPoint at the glass's centre lands INSIDE `#tp-b` — a hit test, never a z-index compare; painters frozen) → search + chips → select + inspector (factory note read-only) → two clicks = ONE `loadPatchFile(path)` and the page hears it ONCE through `onPatchLoaded` (header, glass closed, painters awake, EVERY re-seed hook fired once) → ‹ › step within the bank → + menu / Save as… sheet → `savePresetToBank('User', meta)` → Save enabled only on a user preset → heart → `setFavourite` → Delete asks with a SHEET → no `prompt/confirm/alert` anywhere (WKWebView has no panels) → Escape/outside closes. Controls: `TP_MUT=zorder` (#syn-panel raised over the glass → [3] red), `TP_MUT=nopush` (the courier never pushes onPatchLoaded → [6] [7] red). |
| `fb620_gates.sh` | **fb620 — the surfaces runner**: `ui_syntax.js` over the page, then `preset_surfaces_gate.js` normal + both controls; refuses OK unless each mutant went red. |
| `preset_assets_cert.cpp` | **fb621 — EVERYTHING A USER MAKES TRAVELS.** The asset envelope (`Source/PresetAssets.h`) against real libFLAC: round trip at −138 dBFS · a buffer peaking at 1.8 survives 24-bit quantisation (the `scale` field) · FLAC is **17–19% of the raw float32 base64 it replaces** · a wavetable keeps its frame count · garbage refused (empty, non-base64, a LEGACY float32 blob, a truncated envelope) · factory references parse and a changed file is detectable · mono/1-sample/96k/4-channel · deterministic · 🚨 **[9] it travels** — the asset written into a `.terrain` comes back out whole. `--emit <dir> [wtRoot]` writes the real encoder's payloads for the AU cert. Controls: `AS_MUT=noscale` (the scale field rewritten to 1.0 → [2] red), `AS_MUT=lenient` ([5] flips to demand garbage be accepted). |
| `preset_assets_au.cpp` | **fb621 — THE ARTIST'S MACHINE.** Injects the payloads `preset_assets_cert --emit` wrote into the INSTALLED AU through `kAudioUnitProperty_ClassInfo`. 🚨 The save side writes an asset only when the live buffer holds audio, so **survival is proof the decode reached the engine**. Bars: a one-shot survives a fresh instance byte-identical · a layer, an IR and a wavetable travel the same way · 🚨 **the named source file is absent from this machine and the audio arrives anyway** (`/Volumes/SomeoneElsesDrive/…`) · a factory wavetable resolves out of the bundle by reference and a missing one is refused · **it reaches the DSP** (the note changes; two fresh instances null at −300 dB) · absent means clear for all four kinds. Controls: `AA_MUT=corrupt` (a truncated envelope must NOT survive), `AA_MUT=inherit` ([6] flips). |
| `fb621_gates.sh` | **fb621 — the assets runner**: builds the file-layer cert (juce_core + juce_audio_basics + juce_audio_formats), runs it with both controls, then **emits the real payloads and crosses the seam** into the installed AU with the two artist controls. Refuses OK unless each mutant went red. |
| `fb631_gates.sh` | fb631 — the preset-change SPIKE, the LFO that kept moving, the vocab field that deleted itself: runs the four below with their controls. fb636 review: the spike's preset list is a quoted bash ARRAY (it was `$(ls ... \| head -8)` expanded unquoted, so names with spaces split — 17 "could not read", and the bar judged 2 presets) |
| `fb635_page_gate.js` | fb635 — THE UI ROUND: the LFO shape menu shows all eleven rows with no scroll and stays on screen · Custom wears the pen (a hollow node + handles) · a real wheel over a Style chip scrolls the inspector (the strips' vertical→sideways hijack is gone) · Cmd/Ctrl+S do nothing and the Save row has no ⌘S. Controls `PG_MUT=scroll·emblem·wheel·cmds`, each reddens its own bar. |
| `fb635_osc_gate.js` | fb635 — THE BACK OSCILLATORS KEEP THEIR ONE-SHOT PICTURE: a load pushes all four payloads while B/D are display:none; the slot letter (and any reveal) must paint them. Controls `OG_MUT=noheal` (the fb634 page: [1][2] red) · `noro` (only the canvas observer removed: [2] red). |
| `fb635_lfo_gate.js` | fb635 — THE PATH COMES BACK AS THE PATH: shape relay first, then the preset-load pull; a Path tab draws its six saved points (not the Tri seed), Custom still draws, nothing is pushed back, and a HOST restore through the editor's door (`onPatchLoaded(meta,'host')`) draws the path, re-pushes LFO1 as a Path with pm 1 and shows no Loaded toast. Controls `LG_MUT=custom-only` ([1][4]) · `hosttoast` ([4]). |
| `fb635_state_gate.js` | fb635 — A LOAD TURNS ON WHAT IT TURNS ON: the noise (on · type) and the filter back panel's routing pills (a Terrain filter's "on") re-read on `window.onPatchLoaded`, and a reverse load turns them off. Control `SG_MUT=nohook` ([1][2]). |
| `fb635_face_gate.js` | fb635 — WHATEVER WAS HIDDEN AT LOAD PAINTS WHEN SHOWN (the B/D skeptic's law): a 3D waterfall on a hidden B draws on its letter's reveal; with the synth page hidden during a load, osc A's waterfall and osc C's one-shot draw when the page opens. Row EXTENT (≥ 15 inked rows), not raw ink — a stale 2D flatline has plenty of ink and 7 rows. Controls `FG_MUT=nokick` ([1][2]) · `noro` ([3]). |
| `fb635_restale_gate.js` + `fb635_restale_harness.js` | fb635 — PRESET A→B SHOWS B (the sweep): a relay-faithful backend (817 relays, `Tests/fixtures/fb635_relays.json`) serves six of Max's presets (`Tests/fixtures/fb635_restale_presets.json`, audio stripped, ranges precomputed from all 50); every bar compares the A→B page with a fresh boot of B — blend pills, phase pills, the curve card, NO MIRROR WRITE after a browser load [3] AND after a host restore [3h] (every parameter vs what B stored; 38/50 presets had LOOP_MODE flipped by the old mirror), LFO motion on a stock shape, no phantom card states. ~2.5 min. Controls `RG_MUT=blend` ([0]) · `mirror` ([3][3h]). |
| `fb635_fav_gate.js` | fb635 — THE HEART IS REMEMBERED: the stub keeps `favourites.json` exactly as `tw::bank::setFavourite` writes it (`{"keys":["Bank/Name"]}`) — boot lights the saved hearts (every older gate answered `'{}'`, so the real format was never read back and a new instance drew every heart dark), a fresh page booted from the file a click wrote remembers it, a rename re-keys the heart, a heart written by another instance appears on reopen. Controls `FV_MUT=rawread` ([1]–[4]) · `nokey` ([3]) · `nopull` ([4]). |
| `fb635_gates.sh` | fb635 runner — the seven page gates above (page · osc · lfo · state · face · restale · fav) with every control, `lfo_park.js` (`LFO_PARK_MUTATE=2`; plays an ASSIGNED LFO since fb628 made an unrouted one rest), `preset_carries_cert` (the on/off carries rules, 21 bars incl. the Table distortion's import, `CR_MUT=ungated·noheal·flowblob·offcounts`), wiring tripwires (the load generation, the editor timer's host announcement, the carries asserts), and `preset_carries_au` through the INSTALLED AU (`CA_MUT=ungated·flowblob`). |
| `motion_law_gate.js` | **fb636 — EVERY ANIMATION WINDS UP, WINDS DOWN, AND ONLY MOVES WHILE MIDI SOUNDS** (page), **re-based at fb636 h1** (Max, 2026-09-12 02:34: "the 4 modes dont move AT ALL unless selected, i dont want that. i want them to move regardless of on/off, just about the MIDI my g, and the ARP should not have the dots still in motion when inactive, dots should be organized and stable not frozen on the screen like this same for everything else"). The lane is driven exactly as the C++ does (heard once, keepalive at rest; while playing a frame every 16 ms with `__notesActive`+stamp; at note-off ONE frame, then nothing); the stub answers the four FLOW feeds `{on:0}` (a card out of the chain) and `getSynParam` for the ten ROBIN cycle params only (bank on, order A B C D, Cycle, A-First off — otherwise the stub's 0 empties the cycle). 14 bars: [0] the rig; [1] rest is 0 painter frames in 1 s, the tiles' SMIL paused and held, no running CSS animation on any tile node, and ALL FOUR tiles on their designed HOME pose (boot IS home: ARP six dots op .75 / scale 1 with the fan SOLID at stroke-opacity .38 · CHOP four dots .6, ball docked on the big step · GLITCH dice top-left · ROBIN level, wings up, wind streaks at 0) — plus a per-tile screenshot hash of that boot picture; [2] the WIND-UP (envelope < 0.1 on the first frame, monotonic, full between 150 and 450 ms, ARP loop speed in the first 70 ms under 0.45x); [3] ALL FOUR TILES MOVE WHILE A NOTE SOUNDS, LIT OR NOT (only ARP lit: every tile runs > 0.9 s of loop in 1.5 s at rest weight 0, the unlit SMIL times move, ARP fires); [4] 🚨 THE ENDING — stopped MID-FLASH with no shipped frame after the note-off: the tail paints (> 10 frames), no tile's clock runs backwards (the landing fold excepted) or faster than 1.1x, each lands ≤ 5.2 s, ARP leaves the edge above 0.4x and its last step is under 0.35x, and all four are then EXACTLY home (`homeMiss`); [5] then 0 painter frames in 1.5 s and 0 `uiGesture` calls during the wind-down; [12] 🚨 HOME IS ONE PICTURE — three different stop points, every tile's screenshot hash and drawn state equal the boot's; [6] 🚨 #env-line stopped at the PEAK of its breath is still breathing 250 ms later and then rests PAUSED on its pose at opacity .900 (**fb636 h2**: the mid-ending sample is new — the breathers' cap now lands any straggler exactly on the pose (`brHomeAll`), so the end state alone let `brfreeze`, a breath frozen at the edge that snapped at 3.6 s, pass: measured BROKEN CONTROL, now red on 6); [7] a repaint at rest draws the same picture (filter emblems + topo); [8] the 3D waterfall rests / drifts with the notes / parks (its rest is measured once the tiles' wind-down tail has ended: each tail pass nudges its rAF, which draws nothing); [13] THE FOUR FLOW CARDS FOLLOW THE MIDI — each card out of the chain with a feed: still on its home at rest, ≥ 3 display changes in 2.4 s of play, stopped away from home, home again ≤ 7 s (ARP step 1 · GLITCH slice 1, Idle, no overlay · ROBIN the cycle's first station, arm on it, ring at rest · CHOP slice 1 at the window's edge) and then the very picture it showed before the note (the CHOP ribbon's waveform included); [9] the sequencer code is present byte for byte (the four FLOW cards' chain steps, feed polls, `TIC.runLoop` — they write parameters); [10] C++ source tripwire (Resynth scan dt 0 while quiet, heartbeat, reattach); [11] no page errors. `MOTION_MUT=noend·notail·nowindup·litonly·anyband·norest·cardrest·cardhome·brfreeze·wallclock·wfloop` (`litonly` = the reversed fb636 rule, only the lit tile moves → 3; `anyband` = the old rest bands, any pose → 4 12; `norest` = the rest weight never climbs, a frozen keyframe pose → 4 12; `cardrest` = the ARP card ignores the MIDI → 13; `cardhome` = the CHOP ribbon freezes mid-scroll → 13). **fb636 h3** (review of h1/h2): bar [4]'s fold excuse was too weak — any step back over 1 s that landed below 10 was excused, so a real rewind (ARP 3.0 → 1.5) passed. The probe now also records each tile's ending goal (`park.g`), and the only step back excused is the landing fold: the drop from the goal the clock just reached is a WHOLE number of the tile's periods (`|drop/P − round(drop/P)| < 1e-6`); new control `MOTION_MUT=rewind` (each ending starts 1.3 s back, then eases home exactly — green under the old excuse, measured) → 4. The record now also keeps the last live frames before the stop: the ending's first pass is a step FROM the last live frame, and a record that began at the stop could not see a rewind there (measured: `rewind` stayed green until this changed); 'leaves the edge' still reads the first step after the stop. `__cardSnap` now carries the GLITCH 'last FX' and ROBIN NOTES readouts, so bar [13]'s "the very picture it showed before the note" includes them. The `cardrest` anchor drops its closing parenthesis (the card-arp call gained a vmax argument). |
| `home_pose_gate.js` | **fb636 h2 — "SAME FOR EVERYTHING ELSE": EVERY OTHER ANIMATED ELEMENT RESTS ON ONE DESIGNED HOME** (page). Max, 02:34: "dots should be organized and stable not frozen on the screen like this same for everything else." motion_law_gate's lane/stub; the filter on SHIMMER (an animated MAG_ANIM type), a 3D waterfall with a churn band, the rack with a Delay + Saturate + Granular, the LFO on sine and assigned (`__tiLfoRouted`), and a probe `.mod-btn.has-routes` (the stub makes no routes). Three bursts of different lengths (1.3 / 2.15 / 2.95 s) → silence → the landing (tail + breathers' ending): each element must MOVE while playing and then be EXACTLY the boot picture — at rest and after a repaint. 10 bars: [0] the rig (all present, visible, inked; 0 frames at boot); [1] the four filter emblems' pixels (the DRV wave finishes its cycle onto phase 0 on its own `__tiWind('em-drv', 2π/3.2)` clock); [2] the filter curve (the travelling highlight fades with `MOT.e`; the animated types run on `curTF`, which resets once the ending lands, and blend onto their t = 0 shape by 1 − e); [3] the topo contour map (own clock + rest weight, home grid cached per dice roll); [4] the noise cloud (drift/wobble/twinkle/breath/bloom scaled by e, phases back to their seeds); [5] the waterfall churn line (swing × e, phase 0 on landing, one last draw on the centre); [6] the delay pulse (hidden at 6.5, no tap flash, a hand at rest leaves it there — it was on the wall clock); [7] the LFO curve breath paused ON .9 (≤ 0.5 ms off the boundary) and the LFO's little shape emblem (it scrolls with the phase) showing its phase-0 home path — `iconLive` puts the live and home paths in one svg and `.mv-idle` dissolves live → home on the head's .35 s curve; [8] `#env-line` · `.mod-btn.has-routes` · `.dst-curve` · `.grn-wave` paused ON their 0 % keyframe with the boot style (`brHome` snaps the iteration event's few ms back onto the boundary; the cap lands stragglers); [9] a hand at rest repaints every canvas onto the home pixels, then 0 frames, no page errors. `HOME_MUT=drv·sweep·magtau·topo·noise·churn·dly·lfobr·lfoic·brsnap` (→ 1 · 2 · 2 · 3 · 4 · 5 · 6 · 7 · 7 · 7 8). Full-page screenshots are NOT byte-compared: a region an element swept over is re-rastered with slightly different antialiasing (measured: 3-25 blocks of 8 px on the delay ticks and a ROBIN wing whose markup is byte-identical to boot), so the gate compares canvas bitmaps and drawn attributes instead. `HOME_SHOTS=<dir>` writes `h2_boot_home` / `h2_play1-3` / `h2_rest_after_stop1-3`. |
| `wind_edge_gate.js` | **fb636 h3 — THE MOTION LAW AT ITS EDGES** (page): the five findings of the h1/h2 review, each a case motion_law_gate / home_pose_gate did not exercise. motion_law_gate's lane, stub (FLOW feeds `{on:0}`: cards out of the chain; the ten ROBIN cycle params) and play/stop rig. 8 bars: [0] the rig (the cards' readouts boot on ROBIN NOTES `0` and GLITCH FX `—`); [1] 🚨 A NOTE MID-ENDING NEVER JUMPS THE POSE — three times (0.35 / 0.7 / 1.1 s after a stop, ≥ 3 of the 4 tiles still winding down) the note comes back: no tile's rest weight r falls faster than the wind-up's steepest (6/s, per pass `≤ 6.3·dt + 0.02`), the ARP fan's COMPUTED dash and stroke-opacity step no more than that allows (17× / 0.62×), and every tile clock stays forward and ≤ 1.1x (landing folds excepted, whole periods) — before, r was re-derived from the speed on the retrigger and fell up to 0.39 in one frame; [2] 🚨 A CARD REOPENED AT REST IS HOME, AND STILL — each FLOW card closed mid-play (all four) or mid-ending (ARP, ROBIN), the note ending while it is closed, reopened at rest: the first frame is home and the pre-note picture (readouts included), then 0 picture changes in 3 s — before, the owed ending resumed with no MIDI; [3] 🚨 THE SYNTH PAGE SHOWN AGAIN AT REST — `#syn-panel` hidden mid-play or mid-ending (drawEmblems skips while hidden), shown at rest and tapped once: `em-drv` home on the first frame, the four emblems the rest picture (canvas hash), 0 painter frames after the hand's 450 ms tail, nothing winding — before, the DRV wave walked on and re-armed the page tail from the hand's repaint; [4] 🚨 A SLOW CARD GETS HOME IN ≤ 2.5 s — the ARP card at 1/1 and the GLITCH card on a 1/1 grid (`FLOW_ARP_RATE` / `FLOW_GLI_RATE` normalised 0, 0.5 steps/s measured), stopped a step or two in: park T ≤ 2.5 s, home ≤ 2.9 s, steps forward only, the pre-note picture — before, T ≥ 1.5·D/rate ≈ 45 s; [5] 🚨 THE READOUTS COME HOME — ROBIN stopped while NOTES shows 1-3 rests on `0`; GLITCH stopped after a fire rests on FX `—` (the boot picture); the whole card picture is the pre-note one; [6] 0 painter frames at rest afterwards; [7] no page errors. `EDGE_MUT=rejump·unseen·cardslow·notes·lastfx` (→ 1 · 2 3 · 4 · 5 · 5). |
| `fb636_motion_gates.sh` | fb636 runner — `motion_law_gate.js` with all eleven controls (fb636 h1: `unlit` → `litonly`, plus `anyband`, `norest`, `cardrest`, `cardhome`), plus the rest/lane/canvas laws it must not break (`idle_gesture_gate`, `canvas_alive_gate` `CANVAS_ALIVE_MUTATE=1`, `lane_clock_gate` `LANE_CLOCK_MUTATE=1`, `lfo_park` `LFO_PARK_MUTATE=2`) the two comet gates re-based at fb636 (`flowmod_underline`, `fxmod_underline`: the injected LFO feed now carries its note, as the C++ always writes it — an LFO's comet winds out with the LFO head in silence), and `geode_follower_gate` re-based at fb636 (bar 6: a stalled feed with the note flag stuck on is exactly when the motion clock's watchdog grants ONE bounded wind-down tail — measured 43 frames — so the bar now demands ≤ 100 frames and then a PARKED clock; new control `GEODE_FOLLOWER_MUTATE=4`, an endless tail, → 6 red; re-based again at fb636 h1: every tile now winds onto ONE home, which can be ~4.7 s away, so the gate WAITS for the wind-down (≤ 8 s) and allows ≤ 420 frames). **fb636 h2**: plus `home_pose_gate.js` with its ten controls, and `motion_law_gate`'s `wallclock` control now also puts the DRV emblem's own clock on the wall clock (CUT/RES/ENV are fully blended out at rest, so without it bar 7 could no longer go red). **fb636 h3**: plus `motion_law_gate`'s `rewind` control and `wind_edge_gate.js` with its five controls (`rejump · unseen · cardslow · notes · lastfx`). Page gates only: no build, no install. |
| `preset_load_spike_au.cpp` | fb631 — times EVERY render block across a real .terrain load on the installed AU (Max's own presets; side-loads any build with `TERRAIN_AU_BUNDLE`); the bar is the meter: ≤ 50% of the buffer budget. `TERRAIN_PROFILE=1` makes processBlock print per-section µs for any block over `TERRAIN_PROFILE_US`. **fb636 bugA — RE-SCOPED TO THE WHOLE LOAD WINDOW.** The bar judged only the 24 blocks right after the load; the 24 after the post-load pump (the timer's tick: pooled pairs, bakes, engines) were printed and never judged. That is where a timer-built pooled pair put its first-render cost, one tick late — and bugA, which builds the pair during the load, moves the same cost into block 0, where the old bar saw it: a relocation, not a new spike. Max's intent is "loading a preset must not spike the CPU at any point while it settles", so [1] is now the MAX over block 0 through the last post-pump block, same 50% bar; both halves still print per preset, plus the old first-24 number (reported, not judged). Measured at the re-scope, 5 interleaved runs × the 52, bugA vs the build before it: **not worse** — per-preset median window max lower on 31, higher on 21 (sign test p 0.21), bank mean 36.9% vs 38.9%, worst block per run 74-118% vs 67-114% (median 90 vs 107). ⚠️ **Red on BOTH builds over the bank**: Max Voltage 66.5% / 107.2% at median (first 24 blocks), 7 / 4 presets above 50% at median; the higher-on-bugA presets (Brahman 55 vs 43, Bloodlust, Corinthians, Snare Jordan, Mario Kingdom) show the stacking the relocation implies (route push + the pair's first render in one block). `fb631_gates.sh`'s first-8 set, 3 runs: 60/56/48% vs 41/53/69%. Controls: `SP_MUT=lenient` inverts the bar; `SP_MUT=latespike` busy-waits 60% of the budget inside the first post-pump block of every preset — the relocated shape alone — so [1] must go red (it does: every preset's window reads ≥ 60%). **fb636 review — [0] EVERY ARGUMENT LOADED:** `fb631_gates.sh` expanded its list unquoted, so names with spaces split (17 "could not read") and [1] judged 2 presets; an unreadable argument is now a FAIL of its own, not inverted by `SP_MUT` (proved: the split list reads 0 of 6 loaded → red; the quoted array 8 of 8). Re-measured with all 8 loaded, 3 interleaved runs each, side-loaded (the machine also running Max's DAW session): **[1] red on BOTH builds in every run**, pre-existing — cand12 (installed) worst 61/65/69%, the review build 61/71/50.x%. Per-preset median window max, cand12 vs review: 4th Of July 18/15, Alice In Wonderland 15/40, All You Need 33/44, Bloodlust 31/36, Brahman 43/48, Broken Bitcrush 54/50, Can You Hear Me? 32/26, Clic Up 9/14; single runs swing 8-65% on one preset, so none of these differences is outside the noise. (One later `fb631_gates.sh` normal run on the review build read 46% green — the same noise, not a fix.) The earlier first-8 figures above predate [0]. |
| `pool_send_first_block_au.cpp` | **fb636 bugA — A POOLED SEND SOUNDS FROM THE FIRST BLOCK** (AU; side-loads any build with `TERRAIN_AU_BUNDLE`). The render keys a pooled send on its per-voice pair (`flt1`), the insert subtraction removes the routed dry as soon as the pill is lit, and the ONLY builder used to be the processor timer, one tick after the audio thread published the want — so after a load, a `prepareToPlay`, a pill click or a learned CC the first blocks lost the send and, on insert, the oscillator. The host here NEVER pumps between the load and the note (the timer cannot tick inside `AudioUnitRender`). The blob is synthetic and in memory only: the virgin state + Utility 1 (and Widen 3, the numbered-ID shape) ACTIVE at RANK 0, POWER, SRC_A, insert — both EAGER, so the pair is the only thing that can be late. Measured, fix vs the build before it: **[0]** virgin alive −16.39 dB · **[2]** CONTROL, the same blob warmed like `loadInto` = L0 (Utility block 1 −17.02 / blocks 1-4 −16.39 dB) on every build · **[1]** no pump: **−17.02 = L0** vs **−101.68 dB** · **[1b]** Widen 3 is TIME-VARYING (a warmed L0 carries 12 extra blocks of history: −19.30 no-pump vs −24.15 warmed with the pair present), so it is judged A/B over the same 12 silent blocks: memcmp-equal and −15.06 dB vs −95.12 and DIFFER · **[3c]** CONTROL, the virgin patch with vs without timer ticks: memcmp-equal on every build · **[3]** EXACT (`TERRAIN_DETERMINISTIC=1`), the Utility blob with vs without timer ticks: memcmp-equal vs DIFFER (chord blocks 1-4 −95.12 vs −11.16 dB) · **[4]** state set BEFORE `AudioUnitInitialize`: −17.02 = L0 vs −101.68 · **[6]** 🚨 THE OTHER HALF — a pooled reverb 2-6 / granular / tape slot PASSES THROUGH until its timer-built engine exists, so a pair built early feeds it the oscillator UNPROCESSED: the first build of this fix read **+10.0 dB** over steady state on Max's *Damaged Tapes* (the reference −0.45). `dropSendsAwaitingEngine` keeps those sends on the timer (pair and engine land in one tick, as before) unless the engine is already live; the bar asserts nothing reaches the engine-less slot: **−100.20 dB** on the fix and the reference, **−16.39** on the unfiltered build. A level bar was tried first and could not fail (that tape is near unity: −16.39 passthrough vs −17.38 processed) — kept in the source as the reason. **[5]** `--sweep`, REPORT ONLY, read-only over Max's 52: no-pump vs warmed blocks 1-4 of a chord, per preset. The pre-fix build: **32 of 52** differ by more than 1.5 dB, with holes down to **−113.70 dB** (Corinthians; Too Late −112.92, Jupiter PWM −108.42, Golden −107.23, Frequency Modulation −102.13, Reed Devil −91.29, Watchmen −88.07). The fix: every one of those within ~1 dB (Corinthians +0.77, Too Late −0.50, Golden −0.32, Frequency Modulation −0.98, Watchmen +0.01); **17 of 52** still move, and 8 of them read the SAME on the pre-fix build (Bloodlust −2.41, Electra −2.49, Jack Wya −3.99, Millie −3.39, Ozymandias −2.93, Razor Blade −4.00, both Code Veronicas) — block history (0 vs 12 blocks before the note), not the pairs. The tape/granular class is deliberately unchanged: Damaged Tapes −0.45 (as before), Little Nightmares −74.48 (its gap, as before). Of the skeptic's 37 presets with an in-chain pooled entry tap (`scratchpad/entrytap_scan.py`), 30 have a tap this closes; the 7 whose only taps are tape or granular (Damaged Tapes, Dream Police, Haunted, Little Nightmares, Overstimulated, Overstimulated V2, Watership Down) keep the timer window until those ENGINES are built off the timer too — a separate item. Mutation: the reference / the pre-fix build → **[1] [1b] [3] [4] red**, [0] [2] [3c] [6] green; a build without the engine filter → **only [6] red**. `c++ -std=c++17 -O2 -I Tests Tests/pool_send_first_block_au.cpp -framework AudioToolbox -framework CoreFoundation -o /tmp/psfb` |
| `pool_prebuild_gate.py` | **fb636 bugA — THE TWO PATHS AN AU HOST CANNOT REACH** (source gate, no build). A UI pill click (both `setSynParam` natives: the main view and the popped card) and a learned CC (`applyPendingMidiCc`) must build the pooled send's pair (`prebuildPoolSend`) BEFORE `setValueNotifyingHost`, or the audio sees the lit pill with no pair behind it. Comment-stripped, brace-balanced bodies. 10 bars: exactly two natives; each prebuilds first; the CC path prebuilds first and still runs BEFORE `timerCallback`'s `prepGuard` (`std::mutex` is not recursive); `setStateInformation` builds from the TREE before `apvts.replaceState` and from the LIVE pills after it; `prepareToPlay` builds after `cacheSendRefs` under its guard; the timer still builds from `poolWantMask_` (the fallback for host automation and Splitter lanes); `processBlock` never builds or takes `prepLock_`; every message-side build (setState x2, `prepareToPlay`, `prebuildPoolSend`) runs `dropSendsAwaitingEngine` first. **fb636 review — [9] THE TIMER KEEPS A PAIR BEHIND ITS ENGINE:** the tick used to build every wanted pair and only then the granular/tape engines, so on its path (host automation of a pill, and the sends the message side hands back via `dropSendsAwaitingEngine`) a tape/granular pair could publish while its engine was still being built and the slot passed the oscillator through meanwhile; the tick now snapshots `poolWantMask_`, builds it filtered by `dropSendsAwaitingEngine` before `buildPendingGranularEngines`/`buildPendingTapeEngines` and the same snapshot in full after them (what is built by the end of the tick is unchanged: Max's 52 static and automated goldens 0 of 52 differ). **fb636 review, second pass — [10] THE ENGINE WANT TRAVELS WITH THE MASK:** [9] still leaned on `tpeWantBuild_`/`grnWantBuild_`, which `applyTpe`/`applyGrn` raised only in the rack, LATER in the block than the fb631 push that release-stores `poolWantMask_` — so a tick in between saw the lit tape/granular send with no engine wanted, built no engine, and its full-snapshot build published the pair anyway (the slot passing the oscillator through until a later tick). The push now raises the want itself, on `applyTpe`/`applyGrn`'s own condition (POWER raw > 0.5 and the send lit, for the instances in the chain), relaxed, BEFORE the two release-stores, so the tick's acquire load of the mask sees it; lock- and allocation-free. The bar asserts both want stores sit between the route snapshot and the mask store, carry that condition, and that the push is the only place the mask is stored. 12 bars. Controls `POOL_PREBUILD_MUT=editor` / `cc` / `state` / `engine` / `timer` (the pre-review tick) / `want` (the want block moved AFTER the mask stores) each turn exactly their bar red (11 pass / 1 FAIL). |
| `tape_countin_hold.cpp` | **fb636 review — THE COUNT-IN HOLDS ON ITS LAST BEAT, IT DOES NOT COUNT ON** (plain C++, no JUCE, no build: `TapeLoopProcessor.h` driven directly). fb636 M4t made the 60 s ring lazy — the count-in may only complete once `arm()` has run — and while it waited its beat index kept counting (5, 6, ... one per beat) into `getCountInBeat()`. Now it stops at 4 (the UI's GO). 48 kHz, 120 BPM: **[1]** armed first, the count-in completes at exactly 4 beats (sample 95999), as before · **[2]** never armed for 8 beats: still counting in, max index **4** (the pre-fix header: **8**, red) · **[3]** the sample after `arm()` it completes · **[4]** record released while holding cancels it, nothing recorded. The same review wraps `armTapeLoop`'s `arm()` in try/catch and releases record if the ring cannot be allocated (the count-in would otherwise hold forever). Control: compile with `-I` a `Source` whose header predates the fix → only [2] red. `c++ -std=c++17 -O2 -I Source Tests/tape_countin_hold.cpp -o /tmp/tch` |
| `fb636_envknob_gate.js` | **fb636 bugC-b — THE FILTER'S ENV KNOB IS NEVER DEAD** (page, ~2 min). The Env knob (back panel) and the fk-em ENV emblem (the front: the emblems replace `.filter-knobs`) only mirrored into the depth of an envelope ALREADY routed to their filter, and 50 of 52 presets and every fresh patch route none (fb180), so turning them did nothing. Now a HAND's first real move (> 2 px) on a filter no envelope feeds calls `__envEnsureFilterRoute(slot)`: Env 2 (FLT) only, only while Env 2 is Off, DEPTH = the moved knob first, then DEST directly (never `setDest`, which would snap the knob to the old depth); a busy Env 2 (other filter · Mod · Pitch) is never re-targeted or widened to Filter 1 + 2 and the house toast says why. Relay-faithful backend (fb635's 817 relays) that ECHOES every page write like `WebSliderParameterAttachment` (the only way a SliderState's listeners fire), two of Max's presets in their tree's child order (`Tests/fixtures/fb636_envknob_presets.json`), a REAL mouse. Bars: [a] All You Need boots with zero Env-link writes · [b] host automation of either Env knob writes nothing · [e] a press, a 2 px nudge and a double-click (it does reset to centre) route nothing — fb636 review: the WHEEL sub-check is gone, the back Env knob has no wheel handler (a wheel writes nothing, not even the knob), so it was green by construction and no control could redden it · [c] back knob: DEPTH -0.0280 (All You Need's -0.088 + the 6 px move) then DEST = Filter 1, once, the knob never snaps, the depth follows the drag · [g] the emblem, same · [h] slot 2 routes to Filter 2 (emblem slot from `e.id`) and leaves Filter 1 alone · [f] Env 2 on Filter 2 / Pitch / Mod 1: no DEST, DEPTH or FILTER2_ENV write, hint shown · [b2] Paulo boots clean, automation writes no DEST · [d] Paulo (already routed): no DEST write, depth follows · [a2] Paulo ⇄ All You Need with relays in tree order AND reversed: no ENV2 write. ⚠️ [a2] reversed was RED on the pre-fb636 page (Paulo → All You Need wrote ENV2_DEPTH = -0.088 through Paulo's stale Filter 1 route: only the tree order, DEST before FILTER1_ENV, hid it). The knob→depth mirror now books itself and runs against the settled route. **fb636 review — [a2t]** the same four transitions with EVERY RELAY PUSH IN ITS OWN TASK (`H.loadTasks`: a setTimeout between pushes, `onPatchLoaded` in its own task after the last), because WKWebView runs each `evaluateJavascript` the native side sends as a separate task. It was RED on the first fb636 page, whose mirror ran one task later: Paulo → All You Need reversed wrote ENV2_DEPTH = −0.088 (FILTER1_ENV lands at push #164, ENV2_DEST at #180, and the mirror ran between them). The mirror now runs at a SETTLE: once an `FK_SETTLE_MS` (30 ms) window passes in which no relay NEW to this booking pushed (`fkNoteRelay`, wired in `wireFilterLink` to every slider relay but the two Env knobs; one flag test when nothing is booked) — a load pushes a different relay every task, so it waits the burst out; a hand's drag or host automation (the same few relays again and again) settles within one or two windows. 11 bars. Pre-fb636 page (HEAD): [c] [g] [h] [f] [a2] [a2t] red, 5/11. Controls, each measured red (exit 1) on this page, with the FULL red set: `ENVK_MUT=knob` [c] [h] [f] · `emblem` [g] [h] [f] · `order` [c] [g] · `steal` [f] · `down` [e] [c] · `listen` [b] [e] [c] [g] [h] [a2] [a2t] — NOT [a] (the boot writes nothing even with the mutation; this row used to claim [a] [b]); from [b] on, the route it claimed disturbs every later bar · `slot` [h] · `hint` [f] · `eager` [a2] [a2t] · `task` (the first fb636 page's one-task mirror) [a2t] only · `burst` (the settle ignores the other relays: a bare 30 ms debounce) [a2t] only. |
| `lfo_route_gate.js` / `lfo_route_card_gate.js` | fb631 — six bars on the LFO animation gate: an unrouted LFO parks, a removed route (docked ✕, a popped card, a stale in-flight reply) stops it; the card window stays green |
| `vocab_steal_gate.js` | fb631 — the inline "+" field in the save sheet survives the C++ focus steal (`VS_MUT=blurkill` re-installs destroy-on-blur → red) |
| `png_pixels.js` | fb629 — a 40-line PNG reader (zlib + the five scanline filters) so a colour claim can be a PIXEL claim; used by preset_surfaces_gate bars [21]/[23] |
| `wt_pages_cert.cpp` + `extract_import_life.py` | **fb636 F1 — A FREED WAVETABLE REALLY LEAVES THE FOOTPRINT, AND NOTHING READS ONE AFTER IT HAS LEFT** (macOS, plain C++ on the shim; no plugin build). Max's FL climbed to 4.2 GB browsing presets; M2 already freed the import tables a preset leaves behind, but libmalloc kept every freed LARGE block dirty and counted (MALLOC_LARGE (empty) 200 / 311 MB at the end of pass 1 / 2). F1: `tw::PageBackedAllocator` — a table of 256 KiB or more is mmap'd and munmap'd, so `mipData_` / `twinData_` (`tw::TableStore`) leave phys_footprint the moment they are freed. P1 a 128-frame table is a `Memory Tag 240` region of exactly 61.00 MiB · P2 `releaseStorage` takes the footprint from +61.0 back to +0.0 MB · P3 60 build/release cycles (64/128/256 frames) never ratchet · P4 a hash of every sampled float of three builds, which the runner demands be IDENTICAL from the malloc mutant. L1–L3 run the SHIPPING lifetime — `ImportSlot`, `ImportRead`, claim / publish / drop / free, sliced verbatim by `extract_import_life.py` (`TerrainAudioProcessor::` → `ImportLife::`, nothing else) — from four threads: a builder reallocating page-backed tables as fast as it can, a timer freeing every 250 µs (the ONE host value: the 500 ms hold is 1 ms here), an audio reader on the grace fence and an off-audio reader on an `ImportRead` pin. Measured (8 s): 131,230 pinned reads and 1,041,841 audio reads, 0 torn, over 2,612 builds; 2,235 / 2,231 of those reads held a buffer RETIRED while they read (L3 — without that L1/L2 would test nothing). A SIGSEGV is reported as the L1/L2 failure it is. 🚨 **The pin is the F1 skeptic's finding:** four readers sampled an import off the audio thread with no lock (display bake, WT→LFO, the Table distortion's source — also from `setStateInformation` on a host thread — and `oscSourceSpec`'s `toSpec`), and a second bake could claim and reallocate the table under them: a torn read with malloc, an unmapped page with mmap. Controls, all RED: `nopin` (the claim ignores pins → a pinned buffer rebuilt under the read) · `freepin` (the free ignores pins → SIGSEGV) · `nograce` (the claim ignores the audio block → SIGSEGV) · `malloc` (a `Wavetable.h` whose page door never opens → P1–P3 red, +61.0 MB kept, +152.6 MB worst over the cycles, SAME P4 hash). | `bash Tests/wt_pages_gates.sh` |
| `wt_pages_gate.py` | **fb636 F1 — WHO READS AN IMPORT TABLE** (source gate, no build). The cert proves the lifetime by running it; this proves nobody new reads around it. [1] `mipData_` / `twinData_` are `TableStore` and `releaseStorage` swaps both with one · [2] the allocator maps at `>= kMapMinBytes` (MAP_FAILED checked) and unmaps under the SAME test; `Wavetable.h` never includes windows.h (it reaches PluginEditor.cpp — the fb515 law; Windows keeps the CRT heap, which already VirtualAllocs/VirtualFrees a block this size) · [3] `getOscWavetableJson`, `getOscLfoWaveJson`, `setDistortionTableSrc` and `oscSourceSpec` each take an `ImportRead` and hold no raw live load; the waterfall resolves through the pinned `wavetableForDisplay` overload, the unpinned one only in the pointer-only `wtTableStamp` / `getOscNumFrames` · [4] **THE CENSUS** — every raw `importSlot_[..].live.load` sits in a function audited for it (audio: `processBlock`'s audition, `wavetableForOsc`; pointer/epoch only: `getImportStateJson`, `rebuildHarmTableIfNeeded`, `rebuildMorphIfNeeded`, `wavetableForDisplay`). A new one anywhere else is RED until audited and listed · [5] the claim waits pins out, the free skips them, `ImportRead` re-loads `live` after it counts. Controls `WTP_SRC_MUT=nostore\|windows\|unpin\|display\|noclaim\|nofree` all RED. | 5 |
| `wt_pages_gates.sh` | fb636 F1 runner — `wt_pages_gate.py` with its six controls, then slices the lifetime, builds `wt_pages_cert` healthy and as four mutants (headers passed by `-D`, never `-I`: the fb606 trap), runs every one (`WTP_SECS`, default 8; the design's long stress is `WTP_SECS=60`) and compares the P4 hashes. Source/ is never written. ~1 min. | — |
| `morph_pages_cert.cpp` + `extract_morph_life.py` | **fb636 F4 — THE MORPH HALF OF M2: A SPECTRAL-MORPH BUFFER NOTHING CAN REACH IS GIVEN BACK, AND NOTHING READS ONE AFTER IT HAS GONE** (macOS, plain C++ on the shim; no plugin build). Once a morph had run, both of an osc's `MorphSlot` buffers (7.63 MiB each, page-backed since F1) stayed resident for the life of the instance, Spectral off or not: Razor Blade and Watchmen / Watchmen V2 left up to 61 MiB behind. F4: `publishMorph` is the ONE publish door (the None branch and the build) — it stores `live` seq_cst and stamps the buffer it replaces with `audioSeq_`, only on a transition (the None branch runs EVERY tick, and a stamp per tick would push the hold out for ever: the F4 skeptic's first trap); `freeRetiredMorphs` (60 Hz timer, AFTER the four rebuilds) frees a pending, non-live buffer the audio thread is not reporting, past M2's hold AND `importGraceOver`; `wavetableForOsc`'s morph load is now seq_cst (the fence's audio half — the skeptic's second trap); a rebuild claims its target (`pending[target] = false`). `retireCooldown` is deliberately NOT a free condition — it only counts down while a rebuild is wanted, so on a static morph it sits at 2 for good and would free nothing — and the free writes no rebuild state, so when the next rebuild runs, and into which buffer, is what it was (Max's 52 static and automated goldens: 0 of 52 differ). The cert runs SHIPPING code sliced verbatim by `extract_morph_life.py` (`MorphSlot`, `ImportSlot`, `wavetableForOsc`, `rebuildMorphIfNeeded`, `publishMorph`, `freeRetiredMorphs`, `importGraceOver`; `TerrainAudioProcessor::` → `MorphLife::`, nothing else): the timer on the main thread as in the plugin, the audio read on another. M1 a morph switched on is one Tag-240 buffer of 7.62 MiB · M2 a rebuild retires the other: tag-240 +15.25 MiB, held THROUGH the hold, then +7.62 MiB with the footprint 7.6 MB lower and the morph still sounding · M3 switched off (the None branch every tick), BOTH given back: +0.00 MiB · M4 the rebuild INTO the freed buffer hashes the same as before the free and as the same spec built over resident pages · M5 0 rebuild-state changes across 103 free passes · L1/L2 with NO hold, only the fence between a free and the block — 60 s: 1,887,812 audio reads, 0 freed/zeroed/rebuilt under the block, over 1,346 rebuilds and 1,346 frees, every one of them of a buffer retired INSIDE a block (L2: without that L1 would test nothing). Controls, all RED at their bar: `nofence` (the free ignores the grace AND `audioReadingIdx` → SIGSEGV, L1) · `nohold` (M2) · `cooldown` (the free writes `retireCooldown` → M5) · `stampeach` (the None branch stamps both buffers every tick → M3). ⚠️ The grace ALONE cannot be exposed by a run: `audioReadingIdx` covers all but the few-ns window between the audio's `live` load and its store, so `morph_pages_gate.py` [4] holds it. | `bash Tests/morph_pages_gates.sh` |
| `morph_pages_gate.py` | **fb636 F4 — WHO REACHES A MORPH BUFFER, AND WHEN IT MAY GO** (source gate, no build). The cert runs the lifetime; this proves the plugin around it. [1] `MorphSlot` carries `pending` / `retiredAt` / `retiredMs`; `timerCallback` calls `freeRetiredMorphs` exactly once, AFTER the four `rebuildMorphIfNeeded` calls, and nothing else calls it; `processBlock` / `wavetableForOsc` never publish, rebuild or free · [2] every morph publish goes through `publishMorph` (seq_cst store; `was == nullptr \|\| was == to` returns BEFORE the stamp; the seq_cst `audioSeq_` load after the store); `rebuildMorphIfNeeded` makes exactly the two publishes and stamps nothing · [3] every `live` load in `wavetableForOsc` and `resolveMorphTable` is seq_cst · [4] the free's five conditions (pending, not live, not `audioReadingIdx`, the hold, `importGraceOver`) and it writes only `pending` · [5] the claim precedes the not-ready mark and the build · [6] **THE CENSUS** — every function that takes a `MorphSlot&` or names `morphA_..morphD_` is audited (audio: `processBlock`, `wavetableForOsc`, `resolveMorphTable`; message-thread display: `wtTableStamp`, `getOscNumFrames`, `getOscWavetableJson` → `wavetableForDisplay`; timer: `timerCallback`, `rebuildMorphIfNeeded`, `publishMorph`, `freeRetiredMorphs`). The free is safe against the display readers only because they share its thread, so a new reader anywhere else is RED until audited and listed; no other Source file reaches a `MorphSlot` · [7] `releaseStorage` has exactly two callers: `freeRetiredImports`, `freeRetiredMorphs`. Controls `MPG_SRC_MUT=stampeach\|direct` ([2]) · `acquire` ([3]) · `nograce\|cooldown` ([4]) · `noclaim` ([5]) · `early` ([1]) · `census` ([6]) · `extrafree` ([7]), each RED at exactly its bar (6 pass / 1 FAIL). | 7 |
| `morph_pages_gates.sh` | fb636 F4 runner — `morph_pages_gate.py` with its nine controls, then slices the lifetime, builds `morph_pages_cert` healthy and as four mutants (the header by `-D`, never `-I`: the fb606 trap), runs every one (`MPG_SECS`, default 6; the design's long stress is `MPG_SECS=60`). A control passes only if it exits 1 AND the bar it names is among the red ones. Source/ is never written. ~1 min. | — |
| `morph_free_au.cpp` | **fb636 F4 ON THE REAL PLUGIN** (macOS AU, `TERRAIN_AU_BUNDLE=<Terrain.component>`; nothing installed, no preset read or written — the blob is the virgin state with Osc A's Spectral type 1 and no cut, in memory). The render runs on ITS OWN THREAD, 32-sample blocks flat out with a chord held, while the main thread is the message thread: it pumps the run loop (the processor's own timer rebuilds and frees) and automates `OSC A Spectral Amount` through the host in cycles of MODULATED 0.8 s (a new amount every 40 ms) · STATIC 1.2 s · OFF 1.2 s, reading `Memory Tag 240` at the end of each. [0] switching the morph on grows tag-240 by one 7.62 MiB buffer · [1] 0 render errors, 0 non-finite samples, the chord audible, no crash · [2] in EVERY cycle STATIC ends one buffer below MODULATED, OFF two below, and the next MODULATED builds both back. Measured on the F4 build, 60 s: 2,213,904 blocks (1,475.9 s of audio), 0 errors, 15 of 15 cycles at exactly -7.62 / -15.25 / +15.25 MiB. **CONTROL: the pre-F4 build (F1 only) — [0] [1] green, [2] RED: 0 of 5 cycles, tag-240 never moves.** It proves the free happens in the shipping binary under a concurrent render; the crash half has no mutant control here (that would need a mutated plugin build) — `morph_pages_cert.cpp`'s `nofence` is that control. | 3 |
| `altwarp_cert.cpp` + `extract_altwarp.py` | **fb636 ALT WARP — 39-46 ARE SERUM 2's MEASURED LAWS** (DSP, no plugin build: `python3 Tests/extract_altwarp.py Source/SynthVoice.h /tmp/aw.h && clang++ -std=c++17 -O2 -I Tests/shim -I Source -include /tmp/aw.h Tests/altwarp_cert.cpp -framework Accelerate -o /tmp/aw_cert && /tmp/aw_cert`). Serum 2's missing Alt Warp modes fill the RESERVED warp indices 39-46 of all 8 warp-mode params (47 stays reserved; the count stays 48, so no lane or preset moves): Bend + / Bend - / Bend +/-, Asym + / Asym - / Asym +/-, Flip, Odd/Even. The extractor slices the SHIPPED statics verbatim (applyPhaseWarp, warpReadRate, applyAmpWarp, warpAmpNeedsDc, polyBlep, altSigned, altOddEvenGains) plus the same cut of git HEAD's file as the reference (`--ref` floors against another); the cert reads a real mip-mapped `tw::Wavetable` (sine / saw / square through `buildFromSpec`, the mip from `mipLevelForPhaseIncrement`). [1] dry points bit-exact · [2] the float amount glide stalls short of 50 % (0.49999821 … 0.5000143, 44.1-192 kHz) and the `\|s\|` < 1e-4 dead band keeps 41/44/46 EXACTLY dry there · [3a-f] Serum 2 as measured by the calibrator and the independent checker (Bend log2 r at 18 amounts, Asym knees, slopes and direction, Flip's law 0 of 49,520 bins off, Odd/Even gains 2 − 2a / 2a, the read rate) · [4a-e] monotone with fixed points, Bend point-symmetric, Bend − the exact inverse of Bend + (7.8e-16), Asym − the point-MIRROR of Asym + (not its inverse), the +/- modes bit-equal to + / − at dyadic amounts · [5] continuity in the knob and across the wrap · [6] 10 M random calls: finite, in range, `\|window\|` ≤ 1, no subnormal · [7a-e] aliasing at the extremes (OOHR): Flip's band-limited edges ≥ 12 dB (13.5 measured) over hard edges on a smooth table, never > +1 dB where a table jump sits on a flip edge (a fold, not a jump), after Sync / on a backwards FM carrier, ≥ 6 dB after Mirror (the mirrored residual); Bend +'s read rate ≥ 6 dB over the base mip where the table has content in its fast region; Asym on the base mip ≤ the shipped Skew · [7f] REPORTED, NOT GATED: Bend − on the base mip (Serum's measured choice) aliases 7.7 dB above Skew on a saw at C6, and Bend +'s r is conservative on a saw at C7 (the open r^0.8-0.9 point) · [8] Odd/Even parity exact in either slot; the narrowed guarantee's leaks (odd-only behind Tube VAR > 0 or Rectify) are ASSERTED; both slots on 46 cascade · [9] Flip arms the DC blocker exactly while live · [10] modes 0-38 and 47 bit-identical to the reference over 11.7 M calls · [11] every new mode is live. 14 controls (`--mutate`), run by `altwarp_gates.sh`. | 26 |
| `altwarp_gate.py` | **fb636 ALT WARP — THE PLUMBING AROUND THE KERNELS** (source gate, no build). [1] the eight names at 39-46 in `terrainWarpModeNames()` AND in the ONE JS `WARP_MODES`, 48 long, `'Reserved 47'` last · [2] 39-46 filed in exactly one family (the families' Serum order and contents are `altwarp_ui_gate.js` [1]'s) · [3] all 16 per-sample slot calls (WT + FM, 4 oscs × 2 slots) pass Flip's step history `&flipPrev_[osc*2+slot][u]` with their OWN index, and note-on resets it · [4] the Odd/Even pair read at all 8 osc sites (WT feedback + blend paths, FM carrier) and in the waterfall · [5] Flip's DC blocker on FM slot 1, scoped to mode 45 only · [6] the curve card routes 39-46 before the `mode >= 9` amp branch (else they are drawn and CAPTURED as flat amp curves) · [7] the mip pick asks `warpRateFan` for all 8 slots, which widens only Bend +/-'s lower fan end. `AWG_MUTATE=` names · family · flipidx · pair · fmdc · curve · mip each redden their own bar. | 7 |
| `altwarp_au.cpp` | **fb636 ALT WARP ON THE REAL PLUGIN** (macOS AU, `TERRAIN_AU_BUNDLE=<Terrain.component>`; every scenario a fresh virgin instance with `TERRAIN_DETERMINISTIC=1` — without it a voice's start phase follows its heap address; nothing installed, no preset read or written). [A0] reach · [A1] 48 value strings on all 8 warp-mode params, the Alt names at 39-46, 'Reserved 47', each round-trips · [A2] every Alt mode changes the sound; each static dry point renders BIT-IDENTICAL to None; finite, no subnormal · [A3] 41 / 44 / 46 moved 0 → 50 % mid-note are EXACTLY dry once the float glide settles — measured with the history held equal (both instances play the mode and make the move; then only the reference switches to None, and the two must stay BIT-IDENTICAL); 49.9 % (`\|s\|` = 0.002) is the control that must differ. A never-changed None is not a usable reference: after ANY warp change the voice keeps a ~−70 dB trace of the earlier signal (the shipped Skew moved 0.3 → 0 shows it too) · [A4] Odd/Even parity through the whole voice on the first of Osc A's own tables with both parities (the virgin table is odd-only) · [A5] Flip's DC removed on WT and in FM slot 1 (Skew 100 %'s unblocked DC is the control) · [A6] unison 16 on WT and FM, every Alt mode at its extremes: finite, no subnormal. No mutant control (it would need a mutated plugin build); its bars carry in-run controls. | 7 |
| `altwarp_gates.sh` | fb636 ALT WARP runner — `altwarp_gate.py` with its 7 controls, then slices the statics, builds `altwarp_cert` healthy and as 14 mutants, runs every one; with a `Terrain.component` argument also `altwarp_au`. A control passes only if it exits 1 AND the bar it names is among the red ones. Source/ is never written. ~1 min. | — |
| `altwarp_ui_gate.js` | **fb636 ALT WARP · THE PAGE: THE MENU IN SERUM 2's ORDER, THE 12 O'CLOCK PICK, THE READOUTS** (page, `NODE_PATH=Tests/node_modules node Tests/altwarp_ui_gate.js`). Max's answers: the menu files the Alt modes as Serum does; "jump to 50% only on a menu pick of a centred mode (41, 44, 46) when the knob is at 0; never on preset load or automation"; Bend (1) and Skew (5) keep their names; 47 stays reserved. 🚨 THE STUB IS JUCE'S OWN SEMANTICS: `js/juce/index.js:178` — a local `setNormalisedValue` emits to the backend and fires NO local listener (warp_menu's stub fires them, which would hide a knob that never repaints after a pick); `__push` is the backend (automation, preset load, state restore): it sets the value, fires the listeners and writes nothing. [1] `WARP_FAMILIES` = None · Sync [Sync, Formant, Fractalize] · Alt Warp [Bend +, Bend -, Bend +/-, PWM, Asym +, Asym -, Asym +/-, Flip, Mirror, P-Quantize, Odd/Even] · Draw [37, 38] · Geometry [Bend, Skew] · Filter · the six distortion families — indices FROM THE C++ NAMES; Filter and the distortion families equal git HEAD's; every live mode filed once, 47 nowhere, the guard green; the real browser renders that order, opens on Alt Warp for a slot on PWM, and searches "47 warp modes". Only the FILING moved: no index, lane or saved state changes meaning. [2a] a menu pick of 41 / 44 / 46 onto a slot reading 0 % writes the amount 0.5 once, BEFORE the mode (from None the amount is inert, so the new mode starts dry), on all 8 slots · [2b] 30 % and 0.6 % are kept · [2c] a non-centred pick (Bend +, Asym +, Flip, PWM) writes no amount · [2d] re-picking the slot's own mode writes none (a knob parked at 0 % on Bend +/- is a deliberate full Bend +) · [2e] the three centred modes pushed through all 8 mode relays onto 0 % — 24 singly plus a whole-bank burst — write ZERO amounts (the rule lives only in `warpPickerOpen`'s pick) · [2f] 0.4 % reads 0 % and counts as 0. [3a] `fmtSynReadout` on WARP and WARP 2 amounts: 41 / 44 signed, `kAltPmSign·(2a − 1)` ("+100%" · "0%" · "-100%"; MEASURED on Serum the "+" side is the knob's LOW half), 46 "Odd 100%" · "Orig" · "Even 100%", 45 the flipped band "Flip 0–74%" (the MEASURED law `[2a − 1, 2a)`, not the design's `1 − a`), every other mode the plain percent, 104 cells + the drag legend · [3s] the page's `ALT_PM_SIGN` equals `kAltPmSign` · [3f] the Flip text is the band law still in `applyPhaseWarp`, 41 amounts · [3r] the WARP ring is bipolar and reads from centre exactly while its slot is centred, after a backend push AND after a pick (the picker repaints: no local listener) · [3p] the WARP 2 pill prints the same words and each fits the 55.6 px pill · [3o] `oscqReadout` now goes THROUGH `fmtSynReadout` (its "matches exactly" was a comment nothing held) and prints the same text as before at 3 × 241 steps · [4] the warp card's body drag writes the amount and NO VAR on 39-46 (VAR is inert there and would come alive on the next switch to Mirror or a shaper) and shows "—"; on Bend (1) it still writes VAR · [5] no page error HEAD's page did not throw. 13 controls (`AWU_MUTATE=` order · filing · nojump · anyamt · repick · onload · readout · sign · ring · norepaint · pill · oscq · extvar). NOT DONE: the card drawing Odd/Even's harmonic stems and shading Flip's band (the C++ already sends `odd`/`even`/`win`); `fb562_warpfollow.js` (needs the installed AU). | 31 |
| `altwarp_ui_gates.sh` | fb636 ALT WARP page runner — `altwarp_ui_gate.js` normal, then its 13 controls (a control passes only if it exits 1 AND its named bar is among the RED BARS it prints; exit 2 = an anchor not found), then `ui_syntax`, `warp_menu.js` (the fb373 round trip over all 47 live modes on all 8 slots) and `altwarp_gate.py`. Page only: no build, no install, Source never written. ~4 min. | — |
| `capture_last_gate.py` | **fb636e THE CAPTURE IS THE LAST THING THAT TOUCHES THE OUTPUT** (`python3 Tests/capture_last_gate.py`; `--controls` runs every mutation). Max: the Terra capture missed the FLOW modes — it copied the block after the master limiter, before Chop and Glitch rewrote it. Reads `processBlock` with comments and string/char literals blanked by a scanner: [1] the capture after the FLOW dispatch calls `chopStage();`/`glitchStage();` (where the stages RUN); [2] nothing after the capture but the profiler tail — a stage added below it would be outside the export; [3] no processBlock-scope `return` from the first output write to the capture (lambda bodies excluded); [4] `writeToMasterFxRing` (the WET-stem reference) after the dispatch and before any later output write — the browser auditions stay out of the WET stems (fb636f). Controls: `CAPTURE_MUT=early/late/lategain/return/ringlate` exit 1 on their rule, `lambdaret` exit 0; exit 2 = stale anchors | 4 |
| `capture_flow_render.cpp` + `.sh` | **fb636e · THE CAPTURE HOLDS WHAT THE HOST HEARS — FUNCTIONAL** (`Tests/capture_flow_render.sh`, macOS, after a Terrain build; links `libTerrain_SharedCode.a`). Arms the Export ring as an editor does, plays a held chord through Arp → Chop → Glitch → Robin (Glitch chance 100 %), and requires the ring's last N samples to equal the host output BIT FOR BIT while Glitch really fired and Chop was really active — so a pre-FLOW tap cannot pass. `CAPFLOW_OFF=1` = the FLOW-off reference. First run: Glitch 63 fires, 0 of 384,000 samples differ | 3 |
| `msvc_string_literal_gate.py` | **fb636b MSVC C2026** (`python3 Tests/msvc_string_literal_gate.py`). MSVC caps ONE string literal at 16,380 bytes; the Mac compiles anything, so a long `R"TIHX(...)TIHX"` overlay piece broke only the Windows CI. Fails on any raw literal over the cap. Control: `MSVC_LIT_MUT=tight` must exit 1 | 1 |
| `fb637_resize_gate.js` | **fb637 NOTHING IS CUT OFF, NOTHING MOVES INSIDE ITS BOX** (Windows session; `node Tests/fb637_resize_gate.js`, `RG_FULL=1` for full lists). Seven window scales 0.65–1.90: the page always gets its 656, no ink past the line, no box clips its own content, boxed text dead centre within 0.25 px. fb637b: the knob-number nudge is measured live (`--kv-pad`), not a constant. Controls: `RG_MUT=gap/indent/knob/kvconst` (kvconst is GREEN on Windows, RED on the Mac — SF Pro wants a different nudge). Mac 2026-09-12: 6/6, worst boxed 0.215 px (`div.kv[-100]`) | 6 |
| `au_patcher_rules.cpp` | **tp41 THE PATCHER'S RULES ON THE REAL AU** (`clang++ -O2 -std=c++17 Tests/au_patcher_rules.cpp -o /tmp/aupr -framework AudioToolbox -framework AudioUnit -framework CoreFoundation -framework CoreAudio && /tmp/aupr`; `probe` prints levels). [TAP] a device's DIRECT TAP (`<device>_TAPS` bit) hears the raw oscillator: with A in a closed low-pass the post-filter tap is -83 dBFS and the direct tap -18, and the direct tap NULLS against A not being in the filter (steady state -240 dB; the first 100 ms carry the fb79 send glide) — for A (bit 0) and E (bit 6, bank B). [BANK B] E with its output cable cut still comes out through a Utility (the tp41 fix: the timer built send pairs for bank 0 only, so E-H had always BYPASSED every rack device — measured -240 before). [INLINE] a Chop with gate 0 cuts holes; a Reverb AFTER it (`Flow Chop In Rack` + a rank below the reverb's) fills them (window depth 224 → 14 dB) where the same reverb before it leaves them. [PASS] a mix-0 reverb after the Chop is the Chop alone (-240 dB). Run `au_flow_route.cpp` `check` against a `dump` from the previous binary alongside: no INLINE, no TAPS = bit-identical. |
| `_tp41_patcher_rules_gate.js` | **tp41 THE PATCHER'S RULES, THE PAGE** (page 5 headless, `node Tests/_tp41_patcher_rules_gate.js`). [1] a Chop spawned from the canvas has NO pills lit, and a new oscillator is not eaten by it; [2] osc A → reverb lights the pill AND sets bit 0 of `SYN_RVB_TAPS`, and the canvas draws A → reverb, not through the filter; [3] filter → reverb clears the tap and the canvas goes A → filter → reverb; [4] a pill clicked ON THE CANVAS card is a direct tap; [5] Chop → Reverb: `FLOW_CHOP_INLINE` = 1, its rank below the reverb's, the reverb takes A, the canvas draws A → Chop → Reverb, and leaving the rack puts the card after everything. |
| `_tp42_noise_module_gate.js` | **tp42 NOISE AS A MODULE, THE PAGE** (page 5 headless, `node Tests/_tp42_noise_module_gate.js`). [1] the add browser lists Noise and Noise 2 under Oscillators; [2] Noise 2 spawns (SYN_NOISE2_ON = 1), the parked clone strip sits in the node with an audio out and a note in, its three knobs are bank-1 mod dests 1962..1964, the canvas draws MIDI → Noise 2 → Out; [3] Noise 2 → reverb lights the ELEVENTH pill (route[10], SYN_RVB_SRC_N2) as a direct tap (bit 10 of the 11-bit TAPS) and the canvas follows; [4] the card shows the lit N2 pill (the route setter now finds a card on the canvas, not only in the rack); [5] the strip's own power pill switches it off and the node goes home (the strip's controls are in the Patcher's CONTROL list, or the press dragged the node); [6] Noise (1) spawns and cables the same way. The stub answers getSynParam/getSynParams and seeds the Out cables' shipped default (1). `au_patcher_rules.cpp` carries the DSP half ([NOISE 2]: on/level/cable/through-the-rack/adds to Noise 1). |
| `_tp43_eh_audit.js` | **tp43 E-H PARITY AUDIT** (page 5 headless, `node Tests/_tp43_eh_audit.js`, prints a diff). Drives oscillator B and oscillator E through the same actions on the canvas (engine select, wavetable arrows, the glass menu, the back panel, six knobs, five back-panel selects, the sample name, the seven sub-page arrows) and prints what each wrote. A probe, not a gate: read the two blocks side by side. What it found: identical — the one real defect was the canvas eating the wavetable arrows for EVERY oscillator (`.wt-nav` was not a CONTROL, so a press dragged the node). |
| `_tp43_wtstep_probe.js` | **tp43 THE WAVETABLE ARROWS STEP ON THE PATCHER** (tpexp, real WebView). Press › three times on osc A and on osc E: Sine → Triangle → Square → Pulse on both, nothing dragging. |
| `_tp43_capture_probe.js` | **tp43 DAW CAPTURE ON/OFF** (tpexp). `setCaptureEnabled(0)` reads back off, `(1)` back on; the settings row exists. Off releases the ~202 MB rolling ring, never arms it on a fresh instance (marker `~/Library/Caches/Terrain/capture-off`), and processBlock skips the write. |
| `_tp43_noise2_library_probe.js` | **tp43 NOISE 2 OWNS THE SAME LIBRARY** (tpexp). Scan the factory, load a sound into Noise 2 through the instance-aware natives (a trailing `2`), read its peaks and persisted selection; Noise 1's stay untouched; clear. Instance 2 of the noise strip routes every sample native through `nsJuceFn` with the instance appended; the two file dialogs stay Noise 1's. |
| `au_patcher_rules.cpp` [DECK] | **tp43b THE DECK IN THE CHAIN** (same binary/gate as above). With its transport idle the deck passes its cabled input through: osc A with its output cable CUT and `Deck In Chain` + `Deck SRC_A` lit is audible (through the deck), a lit pill with the deck out of the chain claims nothing (silent), nothing claiming a cut A is silent. The transport is natives-only, so the gate proves the routing, not a take. |

## 🚨 THE JS GATES — RUN THE SETUP ONCE, THEN THEY ARE PLAIN `node` (fb601)

Every `.js` harness here drives the shipped `index.html` through headless Chrome and needs
**puppeteer-core**. Until fb601 that module existed only in whatever `/private/tmp` scratchpad the
session happened to be using, which is the exact failure this whole directory was created to end
(see the opening line). The bill: `harm_header_gate`, `harm_waterfall_gate`, `harm_wtmenu_gate`,
`hue_track_gate` and `fx3_ui` all died on `MODULE_NOT_FOUND` from a bare `node Tests/x.js` and
were **silently skipped for an unknown number of sessions**. A gate nobody can run is the same
zero information as a red bar everybody steps over.

```sh
scripts/setup-ui-gates.sh                 # once per machine/clone — also runs inside scripts/setup.sh
cd plugins/Terrain
node Tests/harm_waterfall_gate.js         # no NODE_PATH, from any cwd
```

* `Tests/package.json` + `Tests/package-lock.json` are **committed**; `Tests/node_modules/` is
  **gitignored**. `setup-ui-gates.sh` runs `npm ci`, so the tree is byte-reproducible.
* **INSTALLED, NOT VENDORED, on purpose.** puppeteer-core 25.10.0 is 25 packages / ~5.4 MB of
  someone else's JS — the class of accident `.gitignore` already records ("175 MB slipped in
  once"). And `-core` ships **no browser**: the gates drive the Chrome already on the machine
  (`CHROME_PATH`, default `/Applications/Google Chrome.app/...`), so vendoring the JS would leave
  the real dependency un-vendored anyway — a bigger repo and the identical setup step.
* It lives in `Tests/`, not the repo root, because node resolves a bare `require()` from the
  **requiring file's** directory upward, not from the cwd. That is what makes the command above a
  plain `node Tests/<gate>.js`.
* The setup script **prints whether it fired** (the hard law) and names the Chrome it found, or
  says loudly that it found none.

The two fb530 gates are pure python, no deps, run from the plugin root:

```sh
python3 Tests/wt_list_gate.py            # 24 checks across the ten sites
python3 Tests/harmonic_ceiling_gate.py   # 8 checks; prints the frozen legacy debt
```

The fb605 IDENTITY LOCK — pure python, no deps, reads the CMakeLists, the BUILT artefacts and the
installed bundles. **Run it after every build**, and run it BOTH ways:

```sh
python3 Tests/identity_lock_gate.py                                   # 9 bars
for m in cmake cid au fx; do TI_ID_MUT=$m python3 Tests/identity_lock_gate.py; done
#   cmake -> [0][4]   cid -> [2][3]   au -> [4]   fx -> [1][5][6]
```

`TERRAIN_BUILD_DIR` re-points the artefact bars at another build tree; `TERRAIN_COMPONENTS_DIR`
re-points bar [7] at another Components folder (that is how [7] was proved able to go green
against freshly built bundles without touching the real one). Both are printed in the header, so
a re-pointed run can never be mistaken for a plain one.

The two fb601 DELAY SPREAD certs — one offline, one on the installed AU. Both must be run from
`plugins/Terrain`: they read `Source/PluginProcessor.cpp` for the registered default and
the migration's own constants rather than hardcoding either.

```sh
c++ -std=c++17 -O2 -Wall -I Tests/shim -I Source Tests/dly_spread_cert.cpp \
    -framework Accelerate -o /tmp/dly_spread_cert && /tmp/dly_spread_cert          # 6 bars

clang++ -std=c++17 -O2 -Wall Tests/dly_spread_state_au.cpp -o /tmp/dly_spread_state_au \
    -framework AudioToolbox -framework CoreFoundation -framework CoreAudio \
    && /tmp/dly_spread_state_au                                                    # 7 bars, needs the AU installed

# and each must be able to fail:
for m in 1 2; do c++ -std=c++17 -O2 -I Tests/shim -I Source -DDLY_SPREAD_MUT=$m \
    Tests/dly_spread_cert.cpp -framework Accelerate -o /tmp/dsc && /tmp/dsc; done   # 1 -> [2] · 2 -> [3][4]
DLY_MIG_MUT=1 /tmp/dly_spread_state_au                                             # -> [2][3]
```

The fb601 HARMONICS-TABLE lane — the audio gate and the migration cert. Both run from
`plugins/Terrain`; `harm_tbl_state_au` reads `Source/PluginProcessor.cpp` for bar [0]
and both need the AU installed. **Run each one BOTH ways** — a green bar that cannot go red is
not a gate:

```sh
clang++ -std=c++17 -O2 -Wall -Wextra Tests/harm_table_au.cpp -o /tmp/harm_table_au \
    -framework AudioToolbox -framework CoreFoundation -framework CoreAudio \
    && /tmp/harm_table_au                                                          # 7 bars
HARM_TBL_MUT=1 /tmp/harm_table_au                                                  # -> [2][3][4][5]

clang++ -std=c++17 -O2 -Wall -Wextra Tests/harm_tbl_state_au.cpp -o /tmp/harm_tbl_state_au \
    -framework AudioToolbox -framework CoreFoundation -framework CoreAudio \
    && /tmp/harm_tbl_state_au                                                      # 7 bars
HARM_MIG_MUT=1 /tmp/harm_tbl_state_au                                              # -> [3]
```

The fb601 JS mutation controls, for the gates changed this build:

```sh
for m in 1 2 3; do CHURN_BAND_MUTATE=$m node Tests/harm_waterfall_gate.js; done    # 1 -> [10][11] · 2 -> [9] · 3 -> [8]
for m in 1 2 3; do WTMENU_MUTATE=$m     node Tests/harm_wtmenu_gate.js;    done    # 2 -> [2]; 1/3 force the live / PENDING arm
```

Both carry their own MUTATION CONTROLS (fb421 — a gate that has never failed has never been
tested). Each of these must FAIL, and each does:

```sh
for m in enum case strD optD rename;  do WTLG_MUTATE=$m python3 Tests/wt_list_gate.py; done
for m in terra kernel legacy fixed;   do HCG_MUTATE=$m  python3 Tests/harmonic_ceiling_gate.py; done
```

**The floor is git's business (fb565).** `warp_menu.js`, `all_menus.js` and `flowmod_gesture.js`
measure a change against the page AS IT STOOD BEFORE it (a page error, a card footprint only
counts if the pre-change page did not already have it). They used to take that page from
`WARPM_PREPAGE` / `ALLM_PREPAGE` / `FLOWG_PREPAGE` and go red without it — three bars every
session stepped over as "pre-existing". `Tests/floor_page.js` now hands them git HEAD's
`index.html` when the variable is unset (the pre-change page for uncommitted work; on a clean
tree, the page itself). Set the variable only to floor against an older commit.

⚠️ `Tests/all_menus.js` now reads the wavetable count OUT OF `PluginProcessor.cpp` rather than
hardcoding it. It used to say 30; when the bank grew to 46 it reported a desync that did not
exist. A hardcoded roster length in a HARNESS is an eleventh site.

⚠️ **fb467 — anything that includes `Wavetable.h` now needs `-framework Accelerate`.** The bake's
transform moved to `Source/WtFft.h`, which calls vDSP where it exists (19.1 ms -> 2.3 ms per table)
and falls back to the shipped radix-2 where it does not. Affected today: `spec_cert.cpp`,
`wtfft_cert.cpp`, `au_spec.cpp`, `blur_align_audit.cpp`. `wtfft_cert.cpp` nulls the two backends
against each other over all 30 factory tables — it is the gate that says the swap changed the speed
and not the sound.

`shim/` is a ~20-line stand-in for the four JUCE symbols `TerrainFilters.h` actually uses
(`jlimit`, `jmin`, `jmax`, `MathConstants`). That is the whole reason the 94-engine filter core
can be certified offline without building the plugin.

## The law these cannot enforce

**A green DSP harness proves the ENGINE works. It never proves the plugin REACHES it** (fb373 —
Cassette ran Studio's machine, bit for bit, through four rounds of green measurements). The
UI→param→DSP round trip is gated separately and headlessly.

## Probe craft, learned the hard way in this file

* **The probe matters more than the metric.** A cutoff sweep measured on a bass-heavy chord reads
  flat, because the source has no top end to remove. Use noise for a response.
* **Give the probe headroom in both directions.** A 3 kHz probe under a 159 Hz cutoff already sits
  at −139 dB, so "closes further" fails on a filter that works.
* **Centroid inverts at the closed end** — once a filter shuts hard the survivor is measurement
  residue, which has a *high* centroid.
* **Measure what the control means.** Poles means slope, so fit dB/oct; centroid barely moved.
* **Every gate self-checks.** If a metric cannot see an injected fault it is decoration.
* **Run the new gate against the OLD code.** Every gate added in fb413–418 was first pointed at a
  deliberately-reverted copy and required to FAIL. The deep-chain test failed 2/2 on the uint32
  feed mask; the no-doubles/scale test failed 3/3 on the old dropdown writer. A gate that has
  never failed has never been tested.
* **An OUTLIER detector is blind to a CONTINUOUS fault** (fb416). `max|Δx| / p99.9|Δx|` finds a
  splice. The granular's micro-clicks were a 25 % duty cycle — the artifact was in the BULK of
  the distribution, not its tail, so the number never moved however bad it got.
* **Geometry is not hearing** (fb417). Bounce measured "57.8 % departure from the un-bounced
  path" — a true statement about a control signal nobody listens to, on a knob Max could not
  hear at all. Measure the OUTPUT spectrum, and print it beside knobs the user already agrees
  are obvious, or the dB has no scale.
* **Check your own detector before believing it.** A one-pole HP at 6 kHz leaks a 220 Hz sine at
  −28.7 dB, so it reported "−32 dB of HF" on every case — below what the bare probe scores. A
  detector that reads the same on clean and dirty is worse than none.

## fb567 — the loop rests (2026-09-02)

Max: *"The LFO pauses, and then it just keeps going... Stop the animation loop when the MIDI is not
inside and there's nothing being played. Put the animation loop back whenever we are playing."*

`mac_idle_frames.mm` measured the shipped fb566 build first: **53 frames/s at idle, 59 after a
note** — the editor's loop had never once rested, so every migrated painter ran on a silent page.
Four causes, found in this order, each named by measurement rather than guessed:

1. **The page's LFO painter simulated.** `lfoFrame` kept fb217's fallback, "stale feed → `ph +=
   dt*effHz()`". fb511 makes the feed go stale at idle *by design* and fb566 parks the DSP LFO
   there, so the dot swept on the page's own clock while the DSP held. `lfo_park.js` bar 2.
2. **The legacy ModulationEngine bank.** Three front-page LFOs at a default 1 Hz, advanced every
   sample from prepare with no assignment in any patch, and their phases pushed every tick *ahead*
   of the quiet gate — so no idle frame was ever byte-identical. Now: pushed only with a live
   assignment and audible output; the engine skips the bank with nothing assigned.
3. **The hero scope's residue.** After a note the scope array printed `-0.0000` one tick and
   `0.0000` the next (±1e-6 leftovers). `SF()` now snaps anything under half the last printed digit
   to a clean zero, and the scope segment rides inside the quiet gate.
4. **The capture strip's seconds.** "Seconds available" grew every tenth of a second in silence —
   10 frames/s on its own. It rides only while audible, or when the export STATE changes.

**THE FRAME-DIFF PROBE.** Set `TERRAIN_CPU_PROBE=1` (or the beacon's marker file) and the editor
appends the first bytes that differ between consecutive shipped frames to
`<tempDirectory>/terrain-frame-diff.txt` (twice a second at most). Causes 3 and 4 were invisible to
code reading and took one run each to name. Zero cost when off.

**THE LAW.** An idle frame is a contract: everything appended outside the quiet gate must be
constant in silence, and a value below the page's printed resolution is zero. When idle frames
ship, run the probe — do not reason about which segment it might be.
